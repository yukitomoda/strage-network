# Storage Network 設計ドキュメント

Bedrock版Minecraft向けの倉庫（ストレージネットワーク）アドオン。個人利用のため、バージョン互換性やクラフトバランスは考慮しない。UXのためなら将来のアップデートで壊れうる手法（JSON UIのバニラ画面上書きなど）も許容する。Scripting API (`@minecraft/server` / `@minecraft/server-ui`) を使用し、実験的機能も許容する。

## 1. 概要

「コントローラ」「ターミナル」「接続ツール（レンチ）」の3つを導入する。

- **コントローラ**: 倉庫ネットワークにつき1つ。ネットワークの引き出し処理を実行する。
- **ターミナル**: 倉庫ネットワークへの引き出しUIを開くブロック。自身は状態を持たず、引き出しされたアイテムは設置面に応じて張り付いた先(6方向のいずれか)のストレージへ直接搬入される。1ネットワークに複数接続可能。詳細は6章参照。
- **ストレージ**: アイテムの実体を保管する場所。**専用ブロックは作らず、`minecraft:inventory`を持つバニラのコンテナブロック（チェスト・樽・シュルカーボックス等）をそのまま使う**。
- **接続ツール（レンチ）**: コントローラ・ストレージ・ターミナルをネットワークに接続/切断するためのアイテム。将来的には汎用設定ツールに拡張予定だが、MVPではネットワーク構築専用。

1ワールド内に複数の独立したネットワーク（＝複数のコントローラ）を同時に存在させられる。ネットワークの接続範囲は**同一ディメンション内に限定**する。

## 2. データモデルと永続化

Bedrock Scripting APIでは、ブロック単位の永続データを直接持てない（動的プロパティが使えるのは `World` / `Entity` / `ItemStack` のみ）。そのため、ネットワークの構造情報はすべて `world` の動的プロパティにJSON文字列として保存する。個人利用規模なので、一覧の線形走査で十分と判断し、逆引きインデックスは作らない。

```ts
// wh:network_ids -> string[]  (存在するネットワークIDの一覧)

// wh:network:<id> -> JSON
type NetworkData = {
  id: string;
  dimensionId: string;           // 同一ディメンション限定
  controller: Vector3;
  storages: Vector3[];
  terminals: Vector3[];
};

// wh:orders:<networkId> -> JSON (引き出しキュー, FIFO)
type OrderLine = {
  itemTypeId: string;
  itemName?: string;   // nameTag(銘)がある場合のみ。詳細は5章参照
  requested: number;
  delivered: number;   // これまでに実際に届けた数
  exhausted: boolean;  // 在庫不足/搬入先(ターミナルの張り付いた先)が満杯等で「これ以上は無理」と確定した
};
type Order = {
  id: string;           // 短い表示用ID(英数字4桁)。マルチプレイでの識別用途なので厳密な一意性は不要
  requestId: string;    // キャンセル指定用の厳密な一意ID(generateId()。表示には使わない。4章「キャンセル」参照)
  playerName: string;   // 完了通知の送り先を後から探すため(Entity.idはセッションをまたいで安定しない)
  terminal: Vector3;
  lines: OrderLine[];
};

// wh:issuing:<networkId> -> JSON (ターミナルからの発行待ちリスト。4章参照)
type IssuingEntry = {
  order: Order;          // lines は delivered=0, exhausted=false で初期化済み
  readyAtTick: number;   // 発行操作時の system.currentTick + ISSUE_DELAY_TICKS(MVPでは0)
};

// wh:order_cancels:<networkId> -> string[] (キャンセル対象のrequestId一覧。4章「キャンセル」参照)

// wh:partial:<networkId> -> JSON (部分成功/失敗の退避リスト)
type PartialResult = {
  orderId: string;
  terminal: Vector3;
  shortfall: { itemTypeId: string; itemName?: string; amount: number }[];
};
```

### ネットワークのライフサイクル

- コントローラブロックが設置された瞬間（`onPlace`相当）に新しいネットワークが自動生成され、自身が`controller`として登録される。
- コントローラが破壊されたら、ネットワークごと解体する（`storages`/`terminals`/`orders`/`issuing`/`order_cancels`/`partial`、および預け入れ・整理の同種のキュー`deposits`/`deposit_issuing`/`deposit_cancels`/`deposit_partial`/`organize`/`organize_cancels`もすべて削除。`network.ts`の`destroyNetwork`）。
- ストレージ・ターミナルが破壊された場合（`world.afterEvents.playerBreakBlock`）、破壊位置がいずれかのネットワークの`storages`/`terminals`と一致すればそのエントリを除去する。

## 3. 接続ツール（レンチ）

- カスタムアイテムコンポーネントの `onUseOn` でブロック右クリックを検知する。
- プレイヤーの「現在編集中のネットワークID」は `player.setDynamicProperty("wh:editing_network", id)` で保持する（EntityはDynamic Property使用可）。
- コントローラを**Shift+右クリック** → 編集モード開始（既に別ネットワークを編集中なら、そのネットワークに切り替える）。**同じネットワークを編集中に再度Shift+右クリックすると終了する**トグル動作。開始と終了で操作を分けず統一した方が分かりやすいという理由で、当初はShiftの有無を問わないトグル動作にしていたが、後述のモード切替式ツール化に伴い「コントローラの操作は常にShift+右クリック」に統一した（モードが増えてもコントローラの操作感だけは変わらないようにするため）。
- 編集モード中、ストレージ/ターミナルを右クリック → 接続済みなら切断、未接続なら接続(新規接続には範囲制限がある。後述)。
- **強調表示（MVP）**: `system.runInterval`で、`wh:editing_network`を持つプレイヤーがいれば、そのネットワークの`controller`/`storages`/`terminals`の座標にパーティクルを定期的にスポーンするだけの簡易実装。既知の課題として、パーティクル自体の寿命(1〜2秒)により切断直後も一瞬残る。修正案（自作の短寿命パーティクル、または`@minecraft/debug-utilities`の`DebugBox`）は10章参照。

### モード切替式ツールへの一般化(ネットワーク構築以外のメンテナンス操作の追加口)

ネットワーク構築以外のメンテナンス操作(Drain指定等)を今後も追加していく方針を決めた際、「用途ごとに別アイテムを実装する」か「1つのツールをモード切替で使い回す」かを検討し、**モード切替方式を採用した**。

- 理由: このアドオンには既に「プレイヤーごとの状態を動的プロパティで持つ」(`wh:editing_network`)仕組みと、DDUI(`CustomForm`)によるメニュー表示の仕組みが両方あり、モード切替はその延長で実装できる。別アイテム方式は個々の実装はシンプルだが、機能を増やすたびにBP/RPのアイテム定義・テクスチャ・lang登録一式が必要になり、ホットバーも圧迫する。
- **`onUse`(空中で使用)でモード選択メニューを開く**: `CustomForm`でモード一覧をボタン表示し、選んだモードを`player.setDynamicProperty("wh:tool_mode", mode)`に保存する(`toolMode.ts`/`toolModeUi.ts`)。デフォルトは`"build"`(従来のネットワーク構築モード)なので、このメニューを一度も開かなくても今までどおり使える。
- **メニューから編集モードを終了できる**: 編集モードの終了は本来コントローラをShift+右クリックする操作だが、コントローラから離れた場所にいると戻るのが面倒という要望を受け、このモード選択メニューにも同じ終了操作を追加した。編集中(`wh:editing_network`を持つ)の場合のみ、モード一覧の下に`divider`を挟んで「編集モードを終了する」ボタンを表示する(非編集中は何も追加されない)。`wh:editing_network`の読み書き(`getEditingNetworkId`/`setEditingNetworkId`)は元々`wrench.ts`内に閉じていたが、`toolModeUi.ts`からも参照する必要が生じたため、`toolMode.ts`と同じ考え方で`editingSession.ts`という専用モジュールに切り出した(`wrench.ts`は`toolModeUi.ts`をimportしているため、切り出さずに直接importすると循環importになる)。
- **`onUseOn`(ブロックに対して使用)は現在のモードで分岐する**: `"build"`ならストレージ/ターミナルの接続/切断処理、`"drain"`ならDrain指定のトグル処理(後述)。ただし**コントローラに対する操作(編集セッションの開始/終了)だけはモードに関係なく共通の処理**(`handleControllerUse`)で扱い、常にShift+右クリックで開始/終了する。「どのモードでもコントローラの操作感は同じにしたい」という要望を受けて、モード分岐より前にコントローラかどうかを判定するよう変更した。これに伴い、Drainモードでのストレージ操作も構築モードと同様に**事前にコントローラで編集セッションを開始しておく必要がある**ように変更した(以前は編集セッション不要でどのストレージにも直接Drain指定できたが、「セッションを開始/終了する操作はあるのに、開始してもしなくても挙動が変わらない」のは一貫性が無いと判断したため)。強調表示(後述)もこれに合わせて、Drainモードでは「編集中のネットワーク」のDrain指定ストレージだけを対象にするよう変更した(以前は全ネットワーク横断)。

**(実機で発見された不具合の修正済み)** 当初「`onUse`と`onUseOn`は排他的に呼び分けられる(ブロックを対象にした場合は`onUseOn`だけが発火する)」と想定していたが、実機では**`onUse`はブロックに対して使った場合も(`onUseOn`とは別に)発火する**ことが判明した。そのため何もせずブロックを右クリック/Shift+右クリックするたびにモード選択メニューが誤って開いてしまっていた。`onUse`側で`player.getBlockFromViewDirection({ maxDistance: 8 })`により視線の先のブロックをレイキャストし、ブロックが見つかった場合(=ブロックに対する使用だった場合、`onUseOn`が別途処理する)はメニューを開かないようにして回避した。距離はゲーム本来の到達距離と厳密に一致している必要はなく、「ブロック操作の右クリックでメニューが誤って開かない」ことだけが目的の緩い判定。

### ネットワークの接続範囲(コントローラを中心とした立方体、Tierに応じて可変)

当初、ストレージ/ターミナルの接続には距離の制限が一切無かった(ワールドのどこにあってもレンチで接続できた)。これを、**コントローラを中心とした一辺`2×range+1`の立方体の中**に制限した。当初は固定8マス(コントローラ自身のブロックを挟んで両側に8マスずつ=合計17マス四方、当初16マスで実装したが実機での確認を経て8マスに変更した)だったが、後述の「範囲」アップグレード軸の追加により**Tierに応じた可変値**(`controllerAxes.ts`の`getRangeForTier(tier)`、`[4, 6, 9, 12, 16]`)になった。

- **判定**(`network.ts`の`isWithinNetworkRange`): 当初は3次元ユークリッド距離(球)で判定していたが、(a) Minecraft本来の距離判定(ワールドボーダー等)は立方体状のものが多く感覚に合う、(b) 判定が`Math.abs(dx) <= R && ...`という単純な比較で済みsqrtが不要、(c) 後述の可視化がどの高さでも同じ大きさの断面になり単純化できる、という理由で立方体(各軸の距離が全てrange以内)に変更した。`network.ts`自体はアップグレード軸の仕組みを知らない(循環import回避、4章「グレード管理」参照)ため、`range`は呼び出し側(`wrench.ts`)が`getRangeForTier`で都度引いてから渡す。
- **適用箇所**: `wrench.ts`の`handleBuildModeUse`で、ストレージ/ターミナルを**新規接続**しようとした瞬間にのみチェックする。**切断は範囲外でも常に許可する**(この制限より前に接続済みだったメンバーが、後からこの制限が入ったことで切断すらできなくなる、という事故を避けるため)。範囲外だった場合は他の接続拒否ケース(二重登録防止、ターミナルの多重接続防止)と同じように、専用のメッセージ(「コントローラから各方向にNマスを超えているため接続できません。」)を出して`toggleStorage`/`toggleTerminal`を呼ばずに終了する。
- **既存ネットワークへの影響**: 過去にこの制限より遠くへ接続していたメンバーがいても、既存の接続自体はそのまま生きる(遡って強制切断はしない)。次に何か操作(再接続等)しようとした時に初めて制限がかかる。**ただし「範囲」アップグレード軸のTierダウンだけはこの方針の例外**(後述)。

#### 接続範囲の可視化(半透明の壁)

編集セッション中、接続可能な立方体の範囲を**半透明の板6枚(側面4枚+天面/底面)**で可視化する(`rangeIndicator.ts`)。

- **点(パーティクル)ではなく面にした経緯**: 当初はパーティクルで範囲の輪郭を描く案(コントローラの高さを基準に、上下1マスごとに輪切りの円/正方形を1本ずつ描く案)を検討したが、輪郭線だけでも点の数が数百〜千のオーダーになり負荷が心配だったこと、そもそも「点の集合」ではなく「塗りつぶした面」として見せたいという要望があったことから、方針を変えた。パーティクルには塗りつぶした平面という概念が無いため、代わりに**半透明のテクスチャを貼った薄い箱状のジオメトリを持つ非表示エンティティ**を使う。このアドオンには既に「非表示エンティティ+専用ジオメトリ+レンダーコントローラー」の仕組み(設定エンティティ等)と、アルファチャンネル対応のテクスチャ生成基盤(`tools/generate-placeholder-icon.mjs`)が揃っていたため、追加の技術リスクは小さかった。`@minecraft/debug-utilities`(プレビュー限定API、10章参照)のような、パック全体が通常版Minecraftで読み込めなくなるリスクがある手段には頼っていない。
- **ジオメトリ**(`RP/models/entity/range_wall.geo.json`/`range_ceiling.geo.json`): どちらも「真の2次元平面」ではなく「薄い箱」にしている(2次元平面はMinecraftのエンティティジオメトリでは片面しか描画されない=裏側から見ると透けて見えなくなるため、両面とも面を持つ薄い箱にすることでこの問題を回避した)。ジオメトリ自体は基準距離8.5マス(旧`NETWORK_RANGE_BLOCKS+0.5`相当)の1種類だけを持ち、Tierごとの実際のサイズは後述のスケールで表現する。
  - `range_wall`(側面用): エンティティのスポーン地点(ローカル原点=コントローラ中心)からローカルX方向に少し離れた位置に、Y方向・Z方向とも同じ距離だけ広がる板を1枚だけ持つ。4枚の壁は**全て同じスポーン地点(コントローラ中心)に、Yaw 0°/90°/180°/270°で回転させてスポーンする**ことで4方向をカバーする(ジオメトリ自体は1種類で済む)。
  - `range_ceiling`(天面/底面用): ローカルY=0を中心とした薄い板。天面/底面の使い分けは回転ではなく、**スポーン位置のYそのものをコントローラ中心から上下にずらす**ことで実現している(Y軸回転では上下方向の位置は変えられないため)。
  - テクスチャ(`RP/textures/entity/range_indicator.png`)は全面同じ半透明の水色(`[80,160,255,90]`)、マテリアルは半透明合成の`entity_alphablend`(既存の非表示エンティティが使う`entity_alphatest`は完全な不透明/透明の二値でグラデーションが無いため使えない)。
  - **境界のオフセットは`range`ちょうどではなく`+0.5`(`rangeIndicator.ts`の`rangeEdgeBlocks(tier)`)**: `isWithinNetworkRange`は「コントローラからブロック座標でrange以内」を接続可能とする(=そのブロック自体を含む)。壁をコントローラ中心からちょうど`range`の位置に置くと、範囲内最後のブロックのちょうど真ん中(ブロックは整数座標を起点に1マス分の幅を持つが、中心はそこから0.5マスずれた位置にある)を壁が横切ってしまい、そのブロックが範囲に含まれるのか分断されているのかが視覚的に紛らわしくなる不具合が実機で見つかった。境界ブロックの外側の面(=中心から`range+0.5`マス)に壁を置くことで、境界ブロックが範囲にきちんと含まれて見えるように修正した。
- **Tierごとのサイズ変更(`minecraft:scale`のcomponent_groups)**: 「範囲」アップグレード軸の追加に伴い、壁/天面のサイズがTierごとに変わる必要が生じた。エンティティのスケールは一様(X/Y/Z同じ倍率)にしか変更できない(`EntityScaleComponent`はスケール値を1つしか持たない)ため、**水平方向と垂直方向で別々の範囲を持たせる案は不採用にした**(意味シンボルの相談時にユーザーへ提示: 非対称にするとエンティティプロパティ+レンダーコントローラーのmolang配列参照+Tierごとに異なるジオメトリ生成という新技術一式が必要になり実装コスト・実機不具合リスクが大きく増えるため、水平の値に統一した)。水平=垂直であれば一様スケールだけでTierごとのサイズを正確に表現できる(壁ジオメトリのX方向オフセット・Y/Z方向の広がりは全て同じ基準距離8.5マスから作られているため、一様スケールで整合的に伸縮する)。`BP/entities/range_wall.json`・`range_ceiling.json`にTierごとの`minecraft:scale`を持つ`component_groups`を5つ用意し(スケール値=`(range+0.5)/8.5`)、`events`(`wh:set_range_tier0`〜`4`)でスポーン直後に1回だけ`entity.triggerEvent()`する(同一エンティティは生涯1つのTierしか持たないため、`remove`は不要)。天面/底面のYオフセット(スポーン位置そのもの)はスケールとは独立に、スクリプト側で`rangeEdgeBlocks(tier)`から直接計算する。
  - **既知の制約**: `SpawnEntityOptions`にスケールの初期値を指定する口が無いため、`triggerEvent`が反映されるまでの1〜2フレームだけ既定スケール(=スケール未指定、等倍)で描画される可能性がある(回転で見つかった現象と同種、後述の`initialRotation`参照)。半透明の一時的なインジケータであり実害は軽微なため、回避策は入れず既知の制約として記録するに留めている。
- **表示/非表示の同期**(`rangeIndicator.ts`の`syncRangeIndicator`): 参照カウントは持たず、呼ばれるたびに「表示されているべきか」を**都度再計算**して実際のエンティティの有無と一致させる(既存の設定エンティティの「無ければ作る」パターンと同じ考え方)。誰かが表示中かどうかの判定(`editingSession.ts`の`isAnyoneEditingNetwork`)は、複数人が同時に同じネットワークを編集していることもあるため、「自分が編集を終了した」だけでは即座に隠してよいか分からず、ログイン中の全プレイヤーを見て誰か1人でもまだ編集中なら表示を続ける。**「範囲」軸のTierが変わった場合の追従も同じ仕組みに乗せている**: 表示中のインジケータが記録しているスポーン時Tier(動的プロパティ`wh:owner_tier`)が現在のコントローラのTierと一致しなければ、一旦削除して現在Tierで再スポーンする。新たな即時同期は追加せず、既存の定期実行(次項)による都度収束に任せることで、編集セッション中にキットを付け替えた場合も最大`HIGHLIGHT_INTERVAL`(0.5秒)以内に表示が追従する。
- **編集セッション終了処理の一本化**(`editingSession.ts`の`endEditingSession`): インジケータの表示同期をし忘れるバグを防ぐため、`setEditingNetworkId(player, undefined)`を直接呼ぶのをやめ、同期処理も含めた`endEditingSession(player)`に一本化した。呼び出し元は4箇所: コントローラでの終了(`wrench.ts`の`handleControllerUse`)、レンチメニューからの終了(`toolModeUi.ts`、3章参照)、**プレイヤー退出時の強制終了**(`wrench.ts`の`registerWrenchPlayerLeaveWatcher`、`world.beforeEvents.playerLeave`)、そして**コントローラ破壊時の強制終了**(`controllerBlock.ts`の`onPlayerBreak`、次項)。
- **(実機で発見された不具合の修正済み) 編集中にコントローラを破壊すると範囲表示が壁として永久に残る**: 編集セッションを終了しないままコントローラを破壊すると、(a) プレイヤーの編集状態(`wh:editing_network`)が、もう存在しないネットワークIDを指したまま残ってしまい、(b) 範囲表示の壁/天面エンティティも片付けられずに残ってしまう不具合が実機で見つかった。原因は`reconcileAllRangeIndicators`(定期ループ)が`getAllNetworks()`(**現存する**ネットワーク一覧)を巡回して同期する仕組みのため、`destroyNetwork`でネットワークが解体され一覧から消えた瞬間、そのネットワークに紐づく壁エンティティはもう誰からも「片付け対象」として見つけてもらえなくなる、というものだった。対処として、`controllerBlock.ts`の`onPlayerBreak`で`destroyNetwork`を呼ぶ**前**に、そのネットワークを編集中の全プレイヤー(`world.getPlayers()`を`getEditingNetworkId(p) === network.id`で絞り込み)に対して`endEditingSession(p)`を呼ぶようにした。ネットワークがまだ存在するうちに終了処理を行うことで、`endEditingSession`内の`syncRangeIndicator`が正しくエンティティを削除でき、`wh:editing_network`も正しくクリアされる。破壊したプレイヤー自身が編集中だった場合はもちろん、(将来的にマルチプレイで)別のプレイヤーが編集中に誰かがそのコントローラを壊した場合もこの経路でカバーされる。
- **(実機で発見された不具合の修正済み) ログアウトしても壁が消えない**: 上記の`playerLeave`ベースの後片付けだけでは不十分だった。シングルプレイでワールドを終了する場合など、`beforeEvents.playerLeave`内の処理が完了するより先にワールド自体が終了してしまい、後片付け(エンティティ削除)が反映されないケースがあった。この手のイベントタイミング依存の問題を確実に解消するのは難しいため、`playerLeave`側は「できれば即座に」というbest-effortの位置づけにとどめ、代わりに**`startWrenchHighlightLoop`の定期ループ(`reconcileAllRangeIndicators`)で全ネットワーク分のインジケータを毎回再計算する**方式にした。`isAnyoneEditingNetwork`はオンライン中のプレイヤーしか見ないため、ログアウトした時点でそのプレイヤーの分は(`wh:editing_network`を消し忘れていても)自動的に「編集中ではない」扱いになり、次の周期(`HIGHLIGHT_INTERVAL`=10tick=0.5秒)以内に確実に壁が消える。個々のイベント処理の成否に依存せず「今の状態を見て合わせる」定期実行に寄せることで解消した。
- **(実機で発見された不具合の修正済み) スポーン直後に壁が「移動する」ように見える**: 側面4枚は当初、`dimension.spawnEntity()`でスポーンした直後に`entity.setRotation()`で回転させていたが、回転が反映されるまでの1〜2フレームはYaw0(未回転)の位置で描画されてしまい、そこから正しい位置へ一瞬で移動するように見えていた。`SpawnEntityOptions.initialRotation`でスポーンと同時に回転を指定する形に変更し、常に最初から正しい向きで描画されるようにした。

### Drainモード: ストレージを預け入れ対象外にする

倉庫レンチをDrainモードにした状態でストレージを**Shift+右クリック**すると、そのストレージの「Drain指定」がトグルされる。

- **効果**: Drain指定されたストレージは、①預け入れ・自動預け入れの預け入れ先として選ばれなくなり、②倉庫の整理では逆に中身を他の(Drain指定でない)ストレージへできる限り退避させる対象になる(退避先がどこにも無ければ残る)。引き出しには影響しない。
- **永続化**: `network.storages`(構造情報)は変更せず、ターミナル設定と全く同じ発想で、ストレージの座標に紐づく非表示エンティティ(`wh:storage_settings`)の動的プロパティ(`wh:drain`)に持たせる(`storageSettings.ts`)。理由もターミナル設定と同じで、今後ストレージ側の設定項目が増えても`network.storages`のJSON構造自体は変えずに済むため。
- **二連チェストの扱い**: 二重登録防止の都合上、二連チェストは物理的に2ブロックあっても`network.storages`には片方しか登録されない。Drain指定はプレイヤーがどちらの物理ブロックをクリックしても同じ効果になってほしいため、`resolveStorageMembership`で「クリックした場所が未登録でも、中身を共有する登録済みの隣を探して所属ネットワークを解決する」処理を行い、実際の設定は登録済みの座標に対して行った上で、`findPhysicalStoragePair`で見つかるもう片方の物理座標にも同じ値を書き込む(常に両方が同じDrain状態を持つ)。
- **預け入れ・整理側の絞り込みは「ネットワークにつき1tick1回」**: Drain判定(`getDrain`)は非表示エンティティの検索を伴うため、`insertIntoStorages`の呼び出しごと(=ラインごと)に毎回問い合わせるとコストが積み重なる。既存の`buildStorageIndex`(預け入れの優先スタッキング索引)と同じ考え方で、`processNetworkDeposits`/`processNetworkOrganize`がネットワークごとに1tick1回だけDrain指定ストレージの一覧を作り、`insertIntoStorages`に絞り込み済みの候補先リストとして渡す。
- **倉庫の整理でのDrain退避**: `compactKeyInStorages`を2段階にした。フェーズ1でDrain指定ストレージそれぞれの中身を`insertIntoStorages`(候補先を非Drainストレージに絞ったもの)で退避し、フェーズ2で残った非Drainストレージ同士を従来通り圧縮する。フェーズ1はデータの流れとしては「Drain指定ストレージを預け入れ元、他のストレージを預け入れ先」とする預け入れ処理そのものなので、既存の`insertIntoStorages`をそのまま再利用できた。
  - **(実機で発見された不具合の修正済み)** 退避先(非Drainストレージ)がその時点で満杯だと退避が0件で終わるケースがあるが、修正前は`compactKeyInStorages`がこれを「(予算切れではなく)処理完了」として扱い、そのラインを即座に`done`確定させていた。`submitOrganize`はネットワーク全品目を一度に1件ずつのラインとしてキューに積むため、`processNetworkOrganize`のループはある1品目のラインを処理した直後に別の品目のラインへ進むことがある。後続の品目のフェーズ2圧縮(部分スタックの統合)によって非Drainストレージに新しい空きスロットが生まれても、既に`done`確定させてしまった品目は二度と退避を試みないため、退避先の一部がまだ埋まりきっていないにもかかわらずDrainストレージにアイテムが残ったまま整理全体が完了扱いになってしまっていた(実機で、Drain指定ストレージにスタック可能なアイテムが残っているのに整理が終了する不具合として報告された)。
  - 修正: `compactKeyInStorages`の戻り値に`budgetExhausted`(予算切れで中断したか)を追加し、「予算切れ」と「退避先が(その時点で)満杯で1個も退避できなかった」を区別できるようにした。後者の場合はラインを`done`にせず、その代わり`processNetworkOrganize`内のtickローカルな`Set`(`stuckThisTick`)に加えて今回のtickでは後回しにする(同じラインを選び直す無限ループを防ぐため)。`stuckThisTick`はtickごとに新規に作るので、次tickでは必ず再挑戦する。これにより、他の品目の圧縮で空きスロットが生まれた後に、行き詰まっていた品目の退避が自然に再開されるようになった。
  - **(実機での要望を受けた追加修正)** 上記の修正だけだと、退避先に二度と空きが生まれようが無い場合にまで無限に再試行し続けてしまい、「Drain対象のストレージが空にならない限り整理が終わらない」状態になっていた。当初は「退避先(非Drainストレージ)全体が、空きスロットも積み増せるスタックも無いという意味で完全に満杯かどうか」をtickに1回判定し、真なら諦めるという実装(`isRegularStorageCompletelyFull`)を試みたが、これは不十分だと判明した: 例えば非Drainストレージが「丸石64個(満杯)」「鉄50個(端数、14個分の空きあり)」の2枠で、Drain側に丸石20個が残っているケースを考えると、鉄のスタックに空きがあるというだけで「まだ完全には満杯ではない」と判定されてしまうが、その空きは鉄でしか埋まりようがなく丸石には永久に使えないため、結局同じ無限リトライ不具合が別の形で再発する。「ストレージ全体に何らかの空きがあるか」という粗い判定では、その空きが今行き詰まっている品目にとって実際に意味があるかを区別できないことが原因。
  - 正しい修正: `isRegularStorageCompletelyFull`は削除し、代わりに「そのtickで残っている未完了ラインを一通り試して(行き詰まったラインは`stuckThisTick`に積んで後回しにしつつ)、1個も動かせなかった(`movedThisSweep === 0`)」ことをもって初めて「本当にもう手段が尽きた」と判断するようにした。この場合、その時点で`stuckThisTick`に残っている全ラインを`done`にして諦める。1個でも動いた品目があれば(=退避先の状態が変化した可能性がある)、`stuckThisTick`のラインは`done`にせず、次tickで(丸ごとリセットされる`stuckThisTick`により)再挑戦させる。この方式は品目ごとの空き状況を直接的な「進捗があったかどうか」で判定するため、鉄のような「動かしようのない端数」に惑わされず、丸石が本当に手詰まりであることを正しく検出できる。
- **Drain指定ストレージのハイライト**: 編集セッション中のプレイヤーに対し、構築モードの強調表示(`minecraft:villager_happy`、接続済み全メンバー)とは別に、Drainモードでは編集中のネットワークの**コントローラ+接続済み全ストレージ**を対象に、Drain指定済みのストレージは`minecraft:villager_angry`、それ以外(コントローラ、未指定のストレージ)は`minecraft:villager_happy`で示す(`highlightEditingNetwork`がモードで分岐)。Drain指定済みのものだけを表示する案も検討したが、「どれがそもそもDrain指定の対象になるのか(=接続済みなのか)分からない」「コントローラの位置も常に見えてほしい」という指摘を受け、構築モードと同じ範囲(コントローラ含む全接続済みストレージ)をベースに、Drain指定の有無だけを色で区別する形にした。

**(実機で発見された重大な不具合の修正済み)** Drain指定した(=設定エンティティが実在する)ストレージを破壊すると、条件によっては**その跡地に二度とブロックを設置できなくなる**不具合があった。原因は`storageLifecycle.ts`の破壊監視(`registerStorageBreakWatcher`)が、設定エンティティの削除(`removeSettingsEntity`)を`findMembership`が「登録済みストレージだ」と判定できた場合**のみ**行っていたこと。これが失敗する(=エンティティが削除されずに残る)ケースが2つあった。

1. コントローラを先に破壊してネットワークデータごと解体した後、登録済みストレージを破壊した場合 → `findMembership`がネットワーク自体を見つけられず、判定が失敗する。
2. 二連チェストの「登録されていない方の半分」(Drain指定は反映されているが`network.storages`には載っていない側。3章参照)を破壊した場合 → `findMembership`はそもそもこのブロックをストレージとして認識できないため、判定は常に失敗する。

残った設定エンティティ(`minecraft:collision_box`を持つ)が、その後ずっとブロックの再設置を妨げていたと考えられる。修正: 設定エンティティの削除は`findMembership`の成否に関係なく、破壊されたブロックの座標に対して**常に**試みるようにした(`removeSettingsEntity`は対象が無ければ何もしない安全な操作なので、常に呼んでよい)。ネットワークデータの削除(`removeStorage`)は引き続き`findMembership`の判定結果に従う(ネットワークが既に無ければ元々何もしない)。

なお、この修正は今後の再発を防ぐものであり、**この不具合が発生した時点で既に置けなくなっている場所は、コード修正だけでは直らない**(残留エンティティ自体を個別に削除する必要がある。例:`/kill @e[type=wh:storage_settings,x=<X>,y=<Y>,z=<Z>,r=1]`のようなコマンドで対象座標に絞って削除する)。

### 二連チェストの二重登録防止（実装・実機で発見された不具合の修正済み）

バニラの二連チェストは隣接する2つの独立したブロックだが、中身(コンテナ)は実質1つを共有している。両方を別々にストレージとして登録すると、カタログスキャンや抽出処理で同じ中身を二重にカウントしてしまう。これを接続時に検知して防止する。

判定方法は何段階か失敗を経て以下に落ち着いた。

1. **(誤り)隣接する同種ブロックの有無で判定** → 連結していない単体チェスト同士が偶然隣接しているだけでも誤検知した。
2. **(誤り)コンテナのスロット数(27超か)で判定** → `[A1 A2 B1 B2]`のように、別々の二連チェストが隣接して並んでいる場合、A2とB1はどちらも54スロットで隣接しているが実際には中身を共有していない、という偽陽性を防げなかった。
3. **(採用)実際に中身を共有しているかを直接確認する**: 対象コンテナのスロット0の中身を退避し、一意な`nameTag`を持つ目印アイテムを一時的に置いて、隣接コンテナ側から同じ目印が見えるか確認し、確認後は必ず元の中身に戻す。同期的に完結するため見た目には一切影響しない。

検討した代替案（ブロック状態から直接読み取る、コンテナオブジェクトの参照同一性`===`を使う）はどちらも採用しなかった。前者はBedrockのチェストに連結情報を持つブロック状態が存在しない（Java版の`type: left/right/single`に相当するものがない）ため不可能。後者は挙動が非公開でドキュメントに保証がなく、他の未検証の思い込みで既に2回つまずいた反省から避けた。

この判定は「レンチで未接続のコンテナを接続しようとした瞬間」にのみ呼ばれる。定期実行のループ（強調表示・コントローラのtick処理）では一切呼ばれない。

```ts
function containersShareStorage(a: Container, b: Container): boolean {
  if (a.size !== b.size || a.size === 0) return false;
  const probeSlot = 0;
  const original = a.getItem(probeSlot);

  const marker = new ItemStack("minecraft:stick", 1);
  marker.nameTag = `wh:probe:${Date.now()}:${Math.random()}`;
  a.setItem(probeSlot, marker);

  let shared = false;
  for (let i = 0; i < b.size; i++) {
    if (b.getItem(i)?.nameTag === marker.nameTag) { shared = true; break; }
  }
  a.setItem(probeSlot, original); // 必ず元に戻す
  return shared;
}
```

### ターミナルは同時に1つのネットワークにしか接続できない（実機で発見された不具合の修正済み）

ストレージは複数のネットワークに重複接続されていても実害が無い（単に両方のネットワークから在庫として見える・両方の整理/預け入れ対象になるだけ）ため許容しているが、**ターミナルは違う**。ターミナルは`submitOrder`/`submitDeposit`で「どのネットワーク宛か」を1つに決めて引き出し/預け入れを送信する一方、自動端末の定期チェックやレンチのハイライトは`network.terminals`を各ネットワークごとに独立して走査するため、同じ物理的なターミナルが複数のネットワークの`terminals`に同時に登録されると、どちらのネットワーク相手として処理すべきかが曖昧になり誤動作した(実機で確認済み)。

修正: レンチでターミナルを接続しようとした時、そのターミナルが**既に別のネットワークに接続済み**であれば(`findMembership`で判定)、新規接続を拒否してメッセージを表示する。ストレージの二重登録防止(`findAdjacentConnectedStorage`)とは別の問題（こちらは物理的に同じ収納を二重カウントする話、ターミナルの方は同じ1台が2つの論理ネットワークに属することの曖昧さの話）だが、どちらも「レンチで接続しようとした瞬間にチェックする」という対処のタイミングは共通。

この修正より前に誤って複数ネットワークに接続してしまったターミナルは、コードの修正だけでは直らない。該当ネットワークをそれぞれ編集モードで開き、不要な方からレンチで切断(Shift+右クリック)して、最終的に1つのネットワークだけに接続された状態に手動で戻す必要がある。

## 4. コントローラのtick処理

`system.runInterval`によるグローバルなポーリングで実装する。ブロック自体はティックを持たず、受動的なデータ構造として存在する。MVP当初は固定20tick間隔の単一ループだったが、後にコントローラの「周期」アップグレード軸によりネットワークごとに実際の間隔が変わるようになった(詳細は本章「グレード管理」参照)。

### スループット制（グレード拡張性のため）

「引き出しを一括即時処理」ではなく、**コントローラのグレードに応じた搬送予算(budget)** を消費しながらキューを進める設計にする。これにより、将来「グレードの高いコントローラほど速い」という演出が、フェイクの待ち時間ではなく実際のスループットとして自然に実現できる。MVPでは`throughputPerTick`を十分大きい固定値にすることで、実質的に即時処理と変わらない挙動になる。

**用語の注意（実機での指摘を受けて整理）**: `ORDER_THROUGHPUT_PER_CYCLE`/`DEPOSIT_THROUGHPUT_PER_CYCLE`/`ORGANIZE_THROUGHPUT_PER_CYCLE`という名前が示す通り、この予算は**Minecraftのサーバーtick単位のレートではなく、コントローラの処理ループが1回実行されるたびの予算**である。処理ループ自体は`networkProcessing.ts`の`NETWORK_PROCESSING_INTERVAL_TICKS`(=20)により、サーバーtick20回につき1回だけ実行される。当初は`*_PER_TICK`という名前で、コントローラUIの「状況」タブにもそのまま「/tick」として表示していたが、これは実際のサーバーtickの20倍速く見えてしまう誤解を招く表示だった。修正: 定数名を`*_PER_CYCLE`に変更して意味を明確にした。UI表示側は、値をサーバーtickあたりのレートに割り算して換算する案も検討したが、割り算した数値だけを見せても結局「これは何のtick単位なのか」が伝わらないという指摘を受け、`controllerUi.ts`の`throughputLabel()`で**「値/サイクルのtick数」**（例:「10/20tick」）とサーバーtick数の分母をそのまま併記する表示にした。

```
runInterval(20 ticks) {
  for (network of allNetworks) {
    moveReadyIssuingEntriesIntoQueue(network); // wh:issuing -> wh:orders

    let budget = getThroughput(network.controller.tier); // MVP: 十分大きい固定値

    while (budget > 0 && queue(network) not empty) {
      const order = queue(network).front();
      const line = order.lines.find(l => !l.exhausted && l.delivered < l.requested);

      if (!line) {
        finalizeOrder(order); // shortfallがあれば wh:partial:<id> に記録し、キューから除去
        continue;
      }

      const attempt = min(line.requested - line.delivered, budget);
      const extracted = extractFromStorages(network, line, attempt, terminalContainer);
      // extractFromStorages はストレージからの抜き取りとターミナルへの格納を
      // 同一関数内でアトミックに行う。「宙に浮いた」アイテムは発生しない。
      line.delivered += extracted;
      budget -= extracted;

      // extracted < attempt は「予算不足」ではなく「真の在庫 or 受け皿不足」を意味する
      // (attempt自体が budget で既に絞られているため)
      if (extracted < attempt) line.exhausted = true;

      persistQueue(network); // ← ラインを1つ処理するたびに書き戻す(後述)
      if (budget <= 0) break; // 続きは次tickへ持ち越し
    }
  }
}
```

**搬入先（ターミナルの張り付いた先のストレージ）が満杯で格納できない場合も、ストレージ側の在庫不足と同じ「不足」として扱う**（`exhausted = true`）。搬入先に有効なコンテナが無い場合（張り付いた先に何も無い、コンテナでない等）も同様に、全ラインを不足として`wh:partial`に記録した上で引き出しを打ち切る。

**(実機で発見された不具合の修正済み) `wh:partial`が際限なく増え続け、動的プロパティの文字数上限を超える**: `appendPartial`(`network.ts`)は元々`getPartial`で読んだ配列に`push`するだけで、上限を設けていなかった。この記録は現状どのUIからも読み出されていない(将来のUI表示のための記録、9章参照)ため気付きにくいが、**自動端末等の定期チェック(5秒間隔)が慢性的な品薄品目に対して新しい引き出しを発行し続けると、`finalizeOrder`のたびに`wh:partial`へ際限なく積み上がっていく**。長時間稼働させたネットワークで実際に動的プロパティの1件あたり文字数上限(32767)を超え、`ArgumentOutOfBoundsError`で`writeJson`が失敗するようになった(配達ターミナルの実装・検証作業とは無関係に、以前から潜在していた不具合)。`appendPartial`・預け入れ側の`appendDepositPartial`の両方を、直近`PARTIAL_HISTORY_LIMIT`(20)件だけ保持するローリングウィンドウに変更して解消した。書き込みのたびに上限以下へ切り詰めるため、既に上限付近まで育っていた既存データも次回の記録時に自動的に縮小される(手動でのデータ移行は不要)。

### 永続化の粒度: ラインごとに書き戻す

ウォッチドッグ（実行時間が長すぎるスクリプトを強制中断する仕組み）が処理途中で発火しても、「実際にアイテムが移動した分は必ず`delivered`にも反映済み」という不変条件を保つため、**ラインを1つ処理するたびに`world.setDynamicProperty`で書き戻す**。個人利用規模なら書き戻し回数が増えてもパフォーマンス上の問題にはならない。

これにより、ゲームを閉じる/クラッシュするタイミングがどこであっても、再読み込み後は`wh:orders:<networkId>`に残っている引き出しがそのまま「続きから」処理再開される。特別な復旧処理は不要。ターミナル・ストレージのコンテナ内容自体は、通常のチェストと同じくMinecraft標準のブロック永続化に乗っかるだけでよい。

### 「ターミナルが消えた」判定はネットワーク登録データを正とする（実機で発見された不具合の修正済み）

`THROUGHPUT_PER_TICK`を小さくして複数tickにまたがる処理を実機で試したところ、処理途中でワールドを出て入り直すと、引き出しがプレイヤーに見える形では消えてしまう不具合があった。原因は、引き出しラインの処理時に「ターミナルの位置に今`dimension.getBlock()`でブロックを取得できるか」だけを見て、失敗したら即座に「ターミナルが破壊された」とみなして引き出しを打ち切っていたこと。ワールド再読み込み直後は、プレイヤーの現在地に近いチャンクから先に読み込まれるため、ターミナルのあるチャンクがまだ読み込まれていないタイミングでtickが走ると、ブロックがまだ取得できず誤判定していた（`wh:partial`には記録されるが、ターミナル側はそれを読み出さないため、プレイヤーからは「消えた」ように見える）。

修正: 「ブロックが今取得できるか」ではなく、**`network.terminals`(ネットワークへの登録データ)を正とする**。

- `order.terminal`(または`request.terminal`)が`network.terminals`に含まれていなければ、本当に切断/破壊されたとみなして打ち切る(このリストの更新は`wh:terminal`ブロックの`onPlayerBreak`かレンチによる明示的な切断でしか起きないため、信頼できる)。
- 含まれているのに`dimension.getBlock()`で取得できない/型が違う場合は、チャンク未読み込み等の一時的な状態の可能性が高いため、**打ち切らずにそのtickの処理を諦めて次tickに再試行する**(FIFOを守るため、後続の引き出しの処理には進まずbreakする)。

### ターミナルの発行遅延（グレード拡張性のため）

「確定」を押した瞬間に本キューへ積むのではなく、いったん`wh:issuing:<networkId>`に積み、ターミナルのグレードに応じた遅延(`getIssueDelay(tier)`、MVPでは0)の後に本キューへ移す。`system.runTimeout`ではなく上記の同じポーリングループに乗せることで、ワールド再読み込みをまたいでも状態が失われない。

### 引き出し・預け入れ・整理のキャンセル(専用の優先キュー)

コントローラUIの「状況」タブ(`controllerUi.ts`)から、進行中の引き出し・預け入れ・整理をキャンセルできる。**巻き戻しは行わない**: 既に配送/格納/整理済みの分はそのまま処理済みの状態を維持し、まだ処理していない残りだけが無かったことになる(過去に遡って在庫を戻す処理は複雑さの割に得るものが少ないと判断し、MVPでは対象外とした)。引き出し側(`orderProcessing.ts`)・預け入れ側(`depositProcessing.ts`)・整理側(`organizeProcessing.ts`)は全く同じ設計をそれぞれ独立に実装している。

- **厳密なIDが必要**: 通知等の表示に使っている`Order.id`(`generateOrderId()`、英数字4桁)は「マルチプレイでチャット上に表示された時に見分けられればよい」という表示用途のための緩いIDで、厳密な一意性を保証しない(4章冒頭、5章参照)。誤って別の引き出しを取り消してしまわないよう、キャンセルには`Order.requestId`という別の一意ID(`generateId()`、他のリクエストIDと同じ仕組み)を新設し、こちらだけをキャンセル指定に使う。**預け入れ・整理は元々`DepositRequest.id`/`OrganizeRequest.id`自体が`generateId()`による厳密な一意ID**(表示用の緩いIDを持たない設計だった)なので、`requestId`を新設する必要は無く、`id`をそのままキャンセル指定に使える。後から表示用ID(`displayId`)をそれぞれ追加した際も、この`id`(厳密)/`displayId`(表示用)の役割分担は変えていない。
- **専用の優先キュー**: キャンセル要求は通常の`wh:orders`/`wh:issuing`とは別の`wh:order_cancels:<networkId>`(`requestId`の配列)に積むだけの軽量な操作(`cancelOrder`)。`processNetworkOrders`は毎tickの先頭で(`moveReadyIssuingEntries`より前に)このキューを消費し、`wh:issuing`・`wh:orders`の両方から該当`requestId`を除去してから、通常の引き出し処理に進む。「専用キュー+毎tick先頭で優先処理」という構成により、次にその引き出しが処理される前に確実に取り除ける(通常のFIFOに割り込ませて優先度を上げる、というような複雑な仕組みは不要)。預け入れ側も`wh:deposit_cancels:<networkId>`/`cancelDeposit`/`processDepositCancels`として、整理側も`wh:organize_cancels:<networkId>`/`cancelOrganize`/`processOrganizeCancels`として全く同じ構成で実装した(整理は発行待ち(issuing)相当のステージングキューが無く`wh:organize`一本なので、そこだけ除去すればよい分、実装はやや単純)。
- **UI側の即時反映**: 実際の除去は次tickまで反映されないため、コントローラUIの「状況」タブでは、キャンセルした`requestId`をUIセッション内の`Set`で覚えておき、一覧の再取得時にそこから除外することで、体感的には即座に一覧から消えるようにしている(引き出し・預け入れ・整理のセクションそれぞれが独立した`Set`を持つ)。

### グレード管理: 複数軸に対応したアップグレード機構

実際に使ってみると、機能ごとに1グレードのアイテムしか無いのは窮屈だという要望を受け、以前の構想(整数ブロックステート`wh:tier`によるグレード管理)を、**1つのブロックが複数の独立した強化系統を同時に持てる**形に一般化して実装した。

**汎用の「アップグレード軸」抽象**(`upgrade.ts`)を用意し、ブロック種別に依存しない共通の型・関数にした。

```ts
type UpgradeAxis = {
  id: string;            // 内部識別子(例: "controller_speed")
  label: string;         // UI/メッセージ表示用の名前(例: "速度")
  blockTypeId: string;   // このアップグレードが有効なブロックのtypeId
  stateKey: string;      // このブロックが持つカスタムブロックステート名(例: "wh:speed_tier")
  kitItemIds: string[];  // index i の要素 = Tier i -> i+1 に使うキットのアイテムtypeId
};
```

- **Tierはネットワークデータではなくブロック自体に持たせる**: 「ターミナルごとのローカル設定」(6章)と同じ発想で、所属情報(`network.controller`等の座標)と個々の状態を分離している。
- **壊さずにその場でTierを上げる**: コントローラを破壊すると`destroyNetwork`でネットワークごと解体される(2章)ため、アップグレード操作は`block.setPermutation()`でブロックステートだけを書き換える(破壊イベントは発生しない)。専用の消費アイテム(アップグレードキット)を対象ブロックに`onUseOn`で直接使う方式にした。レンチのモード切替とは別にした理由は、レンチが「何度でも使える設定ツール」という設計思想を持つのに対し、アップグレードは本来消費される性質の操作で噛み合わないため。
- **実装上の注意**: `BlockPermutation.getState`/`withState`の型は`minecraftvanilladata.BlockStateSuperset`(バニラの既知ステートのみ)を対象にしたジェネリックで、`"wh:speed_tier"`のようなカスタムステート名は型に存在しない。読み取りは`getAllStates()`(型が緩く安全)を使い、書き込みは`withState`の引数を`as any`でキャストしている。このアドオンでカスタムブロックステートを使うのはこれが最初のため、この制約がここで初めて表面化した。

**現在実装済みの軸はコントローラの「速度」「周期」「範囲」の3つ**(`controllerAxes.ts`の`CONTROLLER_SPEED_AXIS`/`CONTROLLER_CYCLE_AXIS`/`CONTROLLER_RANGE_AXIS`)。発行遅延・自動チェック間隔は今回も対象外(将来、同じ枠組みで別の軸として追加する想定)。

**速度軸**: 引き出し/預け入れ/整理のスループット(旧`ORDER_THROUGHPUT_PER_CYCLE`等の固定定数)を、各処理モジュール(`orderProcessing.ts`/`depositProcessing.ts`/`organizeProcessing.ts`)がTier0〜4の5段階のテーブル(`getOrderThroughput(tier)`等)として持つように変更し、`processNetworkOrders`等が毎サイクル`getAxisTier(dimension, network.controller, CONTROLLER_SPEED_AXIS)`で実際のTierを読んで予算を決める。搬入出(引き出し`ORDER_THROUGHPUT_BY_TIER`/預け入れ`DEPOSIT_THROUGHPUT_BY_TIER`)は`[128, 192, 384, 1024, 4096]`で共通。整理(`ORGANIZE_THROUGHPUT_BY_TIER`)はネットワーク外部とやり取りしない内部処理という性質上、搬入出のちょうど4倍`[512, 768, 1536, 4096, 16384]`にしている。

**周期軸**: サイクル(引き出し/預け入れ/整理をまとめて1回処理する単位)自体の間隔(tick数)を変える軸。`networkProcessing.ts`の`CYCLE_TICKS_BY_TIER = [100, 80, 50, 25, 10]`(値が小さいほど頻繁に回る=速い)。MVPでは`system.runInterval`を固定20tickの単一間隔で回し、全ネットワークを毎回まとめて処理していたが、ネットワークごとに間隔を変えるには単一の`runInterval`では表現できない(間隔はコールバック登録時に固定されるため)。そこで、**基準ループを`BASE_LOOP_INTERVAL_TICKS`(=5、最短のTier4=10tickより十分細かい)で回し、ネットワークごとに「前回処理からの経過tick」を`Map<networkId, lastProcessedTick>`(メモリ上のみ、永続化しない)で管理し、`system.currentTick - last >= cycleTicks`になった時だけそのネットワークを処理する**方式にした。`currentTick % cycleTicks === 0`のような剰余判定も検討したが、基準ループの間隔とTierごとの間隔の両方が互いに割り切れる関係である前提に暗黙に依存してしまい、将来どちらかの値を変えた時に静かに周期がずれる恐れがあったため、経過tickの直接比較にした(基準ループの間隔より短い誤差は生じるが、無視できる程度)。`lastProcessedTick`はワールド再読み込みで消えても、次の基準ループですぐ再計測されるだけで実害が無い。コントローラUIには`getCycleIntervalTicks(tier)`から実際の値を計算した表示があり(アップグレードタブの周期軸の行。後述)、以前固定`NETWORK_PROCESSING_INTERVAL_TICKS`定数を表示していた箇所を置き換えている。

**範囲軸**: コントローラの接続可能範囲(3章「ネットワークの接続範囲」の`getRangeForTier(tier)`、`[4, 6, 9, 12, 16]`)を変える軸。速度軸・周期軸とは異なり、この軸だけ**Tier変更に伴う副作用**(接続範囲が縮んだ結果、既存の接続先が範囲外になった場合の自動切断)を必要とするため、`UpgradeAxis`型に任意の`onTierChanged?: (dimension, block, player?) => void`フックを追加した(範囲軸以外は`undefined`のままで既存コードは無変更)。呼び出し元は`upgradeKit.ts`の`onUseOn`(`setAxisTier`直後)と`controllerUi.ts`の「取り出す」ボタン(`setAxisTier(block, axis, 0)`直後)の2箇所。範囲軸の`onTierChanged`(`controllerAxes.ts`の`pruneRangeAxisMembers`)は、新しいTierの範囲で`network.ts`の`pruneOutOfRangeMembers`を呼び、範囲外になったストレージ/ターミナルを`removeStorage`/`removeTerminal`で切断する。**これは3章で明記した「切断は範囲外でも常に許可する(遡って強制切断はしない)」という一般方針に対する、範囲軸のTierダウン時に限定した意図的な例外**である(ユーザーからの明示的な要求)。Tierが上がる場合もこの処理を素通りさせている(範囲外に出るメンバーは存在せず無害なため、増減を区別する分岐をあえて設けていない)。切断したストレージについては手動切断(`wrench.ts`)と同じく設定エンティティ(`storageSettings.ts`)も掃除するが、ターミナルは手動切断でも掃除していない既存の非対称性に揃えて何もしない。`network.ts`自体はアップグレード軸の仕組みを知らない(循環import回避のため引数で範囲を受け取るだけ)。可視化の詳細(Tierごとのサイズ変更)は3章参照。

**アップグレードキット**: 軸ごと・Tierごとに別アイテムにした(Tier1〜4の4段階。クラフトレシピを実在の鉱石格上げ順に無理に合わせる必要がないよう、汎用的な`Tier N`命名にしている。速度軸は`wh:speed_kit_tier1`〜`wh:speed_kit_tier4`、周期軸は`wh:cycle_kit_tier1`〜`wh:cycle_kit_tier4`、どちらも汎用コンポーネント`wh:upgrade_kit`を共有)。目標Tierに到達するための専用アイテムなので、ブロックが今何Tierかが一目で伝わる。表示名は`T{n} 速度強化キット`のようにTier番号を先頭に付ける形式にしている(`速度強化キット(Tier n)`という後置形式から変更)。既存の`ATM-`/`INV-`/`端末-XXXX`(6章・7章)と同じ「短い接頭辞で種別を一目で示す」慣習に揃えたほか、先頭がTierになることでインベントリ内で同じTierのキットが軸を跨いで近くに並ぶ副次的な利点もある。

キットは**どのTierのものでも直接使え(飛び級可)、使うとそのキットのTierに一気に設定される**。軸ごとに同時に装着できるキットは1つだけだが、既に何か装着済みの状態で別のキットを使った場合は「取り外してから装着し直す」のではなく、**その場で置き換わる**(元のキットは`giveOrDropKits`でプレイヤーへ返す。インベントリ優先・満杯なら足元へドロップという、破壊時・UIの「取り出す」ボタンと同じ返却挙動に統一している)。同じTierのキットを使おうとした場合は何もしない(メッセージのみ)。使用すると`setPermutation`でTierを直接書き換え、手持ちのスタックを1個消費する(`upgradeKit.ts`)。

当初は「現在のTierがちょうどそのキットの`fromTier`と一致する場合にのみ使え、1段ずつしか上げられない」という順送り方式だったが、レシピ側で上位キットが下位キットを消費する構成(後述)にすると、ブロックへ直接使う分とレシピ消費用の分とで下位キットを二重に用意する必要が生じてしまうことに気づき、飛び級可の方式に変更した。この変更により、プレイヤーは目的のTierのキットをレシピの階段を上って1個作るだけでよく、道中のTierをブロックにも別途装着する必要がなくなった。

判定・消費対象アイテムのテーブルは、各ブロックが持つ`UpgradeAxis`の配列から`kitItemIds`を辿って自動的に導出しているため、軸(あるいは別のブロック種)が増えても`upgradeKit.ts`自体は変更不要で、参照するaxes配列を合成するだけでよい(実際、周期軸の追加時もこのファイルは無変更だった)。周期キットは速度キットと同じ素材色だが、アイコンの形をリング状にして軸違いを判別できるようにしてある(`tools/generate-placeholder-icon.mjs`の`upgradeKitRingColorAt`)。範囲キットは同様に四角い枠(額縁、「囲む範囲」の比喩)の形にしている(`upgradeKitSquareColorAt`)。

**循環import回避のための`controllerAxes.ts`分離**: `CONTROLLER_SPEED_AXIS`は当初`controllerBlock.ts`に直接定義していたが、`controllerBlock.ts`は`showControllerUi`のために`controllerUi.ts`に依存し、`controllerUi.ts`は表示のために`orderProcessing.ts`等の処理モジュールに依存している。そこへ処理モジュール側からスループット計算のために`controllerBlock.ts`のaxis定義をimportすると、`orderProcessing.ts → controllerBlock.ts → controllerUi.ts → orderProcessing.ts`という循環importになってしまう。そのため、軸の定義(`CONTROLLER_BLOCK_ID`/`CONTROLLER_SPEED_AXIS`/`CONTROLLER_AXES`)だけを、`upgrade.ts`にしか依存しない末端モジュール`controllerAxes.ts`に切り出した。

**破壊時の挙動**: 対象ブロックを破壊すると、design.mdの既存方針(2章、破壊するとネットワークごと解体される)と一貫させ、Tierは0にリセットされる。ただし素材を無駄にしないため、`onPlayerBreak`で`event.brokenBlockPermutation`から破壊直前の軸ごとのTierを読み取り、それまでに消費したキットを軸ごとに返してから(`giveOrDropKits`、ネットワークの有無に関わらず必ず行う)、既存のネットワーク解体処理を行う。失われるのは「積み上げたTierという状態」だけで、素材そのものは失われない。

**UI表示**: コントローラUIに専用の「アップグレード」タブを追加した(「状況」「整理」「アップグレード」「設定」の4タブ構成)。`CONTROLLER_AXES`をループするだけの実装なので、軸が増えても自動的に「Tier表示+効果の内訳+取り出すボタン」の組が1つ増える。各軸につき、現在Tierを`{軸名} Tier {n} / {最大Tier}`の形式で表示するラベル、その軸の実際の効果の内訳(速度軸なら「引き出し／預け入れ／内部」の3行、周期軸なら「周期」の1行)、「取り出す」ボタンを表示する。**スループット/周期の表示は当初「状況」タブに独立して置いていたが、アップグレードの効果を確認する場所として一本化した方が分かりやすいという要望を受け、各軸のTier表示の直下に移動した**(「状況」タブは引き出し/預け入れ/整理の進行中リストのみになった)。表示単位も、以前は`throughputLabel()`で「値/サイクルのtick数」(例:「128/20tick」、分母は当時グローバル固定だった`NETWORK_PROCESSING_INTERVAL_TICKS`)としていたが、周期が軸ごと・Tierごとに変わるようになったため、速度側の内訳は分母を持たず`{値}/cycle`という単位だけの表示にし(`perCycleLabel()`)、実際のtick数は周期軸自身の`{tick数}tick/cycle`という行(`cycleIntervalLabel()`)で独立して示す形にした。こうすることで、速度軸の内訳表示が周期軸のTierに依存しなくなり(=別の軸の状態を跨いで参照しない)、片方の軸だけ取り出した時に**もう一方の表示が古いままになる不整合が構造的に起きない**。Tierラベル・内訳ラベルはいずれも`ObservableString`にしてあり(DDUIの`CustomForm`は素の文字列を渡すと開いた時点の値のまま固定されてしまうため)、取り出すボタンを押した瞬間に`setData()`で更新し、表示がその場で切り替わるようにしている。ボタンの`disabled`にも同様に`ObservableBoolean`(Tier0かどうか)を渡し、アップグレードが1段も入っていない間は非活性になる(取り出した直後もtrueに戻す)。ブロックの見た目(Tierごとのテクスチャ切り替え)は今回のスコープに含めていない(8章の既存方針「グラフィックの作り込みは後回し」を踏襲。プレイヤーへのフィードバックはUI上のTier表示で最低限確保している)。

**アップグレードの取り出し**(`giveOrDropKits`、`upgrade.ts`): ブロックを破壊しなくても、UIのボタン1つで装着済みのキットを取り外せるようにした。破壊時のドロップと同じ関数を再利用し、`setAxisTier(block, axis, 0)`でTierを0に戻す(`setPermutation`によるその場書き換えなので、こちらもブロックの破壊やネットワークの解体は伴わない)。**返し方はブロックの隣への単純ドロップではなく、プレイヤーが分かる場合はまずインベントリへ直接渡し、入りきらなかった分だけをプレイヤーの足元にドロップする**(`Container.addItem`の戻り値=入りきらなかった分をそのまま`dimension.spawnItem`する)。UIのボタンは常にプレイヤーが分かるのでインベントリ優先になり、ブロック破壊時も`event.player`が分かればインベントリ優先、分からない場合(まれ)のみ従来通りブロック位置にドロップする。

**実機で発見された不具合(修正済み): 手持ちアイテム操作APIの混在**。キットの置き換え(既に装着済みの軸に別のキットを使う、前述)を実装した際、「インベントリが満杯の状態でキットを使う→返却されたはずのキットが見当たらないが、ホットバーの1枠が中身不明のまま操作不能になり、Shift+クリック等で触れるとようやく中身が現れる」という不具合が実機で見つかった。当初はイベント処理中の同tick内でのインベントリ操作を疑い`system.runTimeout`で1tick遅らせたり、ドロップ位置をプレイヤーの足元からブロック付近に変えたりしたが改善せず、バニラアイテムの拾い直しや素手でのQ落下→拾い直しでは再現しないことから絞り込んだ結果、**`giveOrDropKits`が使う`player.getComponent("inventory")`のコンテナAPIと、手持ちの消費に使っていた`player.getComponent("equippable")`(`EquipmentSlot.Mainhand`)という、同じ手持ちスロットを指す2つの異なるコンポーネントAPIを同じ処理内で混在させていたこと**が原因だった。手持ちの消費側も`inventory`コンテナ経由(`player.selectedSlotIndex`)に統一したところ解消した(`upgradeKit.ts`の`consumeOneFromMainHand`)。1tick遅延やドロップ位置変更は原因ではなかったため元に戻している。

**新しい軸を追加する時にやること**(`upgrade.ts`自体は変更不要。範囲軸の追加で実際にこの手順通りだったことを確認済み):
1. 対象ブロックのJSONに新しいカスタムブロックステートを追加する(例: `"wh:range_tier": [0,1,2,3,4]`)。
2. そのブロックの軸定義モジュール(例: `controllerAxes.ts`)に`UpgradeAxis`をもう1つ追加し、対応する`*_AXES`配列に加える。Tier変更時に副作用が必要な軸だけ`onTierChanged`も指定する。
3. 新しいキットアイテム(BP item json + アイコン + lang)を用意する。
4. その軸のTierを実際の効果(例: `getRangeForTier`)に反映する箇所を1箇所追加する。

### クラフトレシピ: アップグレードキット・コントローラ

8章で「コントローラ/ターミナル/レンチのクラフトレシピは無し(`/give`/クリエイティブのみ)」としていたMVP方針を、アップグレードキット8種とコントローラ本体については覆した。単に希少な素材を積むのではなく、**素材そのものにTierやブロックの実際の挙動と結びつく意味を持たせる**ことを狙った(`BP/recipes/*.json`、すべて`minecraft:recipe_shaped`、`crafting_table`タグ)。

**キットの「タイプ」と「進行シンボル」**: アップグレードキットは`C`/`S`の2つの「タイプ」に分類され、タイプごとに1本の「進行シンボル」(ゲームの進行度そのものを象徴する素材)の階段を共有する。速度軸・周期軸はどちらも`S`タイプで、進行シンボルはTier1〜4で`石炭ブロック→ダイヤモンド→ネザライトインゴット→ネザースター`(採掘→ネザー資源開発→ボス討伐、という難易度の階段)。範囲軸は`C`タイプ(進行シンボル`鉄→ダイヤモンド→ネザライトインゴット→ヘビーコア`)を初めて使う軸になった。**進行シンボルを軸ごとに別の終盤コンテンツに紐付ける案(当初検討: 速度=ネザー系、周期=Deep Dark系)は不採用にした**。軸が増えるたびに新しい終盤コンテンツを探すのは種切れするため、「タイプ」という中間の粒度で共有する方式に落ち着いた。これは`UpgradeAxis`が「軸ごとの差分は最小限、残りは共通の枠組み」という設計だったのと同じ発想をレシピにも適用したもの。

**Tierごとに固定された形**(`コア`=中央、`意味`=その軸固有の機能を象徴する素材、`進行`=タイプ共通の進行シンボル):

- **Tier1/Tier2**: 四隅=進行×4、左右=意味×2、中央=コア。
- **Tier3**: 上・左・右=意味×3、下=進行×1、四隅は空。四隅が埋まっている状態(T1/T2)から急に空くことで、Tierが上がるごとに「量より質」に切り替わる感覚を表現している。
- **Tier4**: 左右の列(6マス、四隅+左右中央)=**そのキットのTier1の意味シンボル**×6、上=そのTierの意味シンボル×1、下=進行×1、中央=コア。9マス全てが埋まる唯一のTierで、「Tier1の意味シンボルが完成形を額縁のように囲む」形になっている。要求により、**Tier4だけは意図的に重く**してあり(進行シンボルはネザースター、意味シンボルもエンチャント済みの金リンゴ/エコーシャードのような入手困難な素材)、全ネットワークに気軽に付けられるものではない前提にしている。

**コア**: Tier1は棒。Tier2以降は前Tierのキットそのもの(=キットは飛び級可だが、レシピ上は積み上げ式)。将来、途中のTierが存在しない軸(例: Tier2が無い)ができた場合は、存在する直近のTierまでTier1側へ遡ってコアに使う想定。Tier1自体が存在しない軸ができた場合は、コアを額縁で代用する想定(いずれも未実装、方針のみ)。

**確定した中身**:

| | 意味シンボル(Tier1〜4) |
|---|---|
| 速度軸 | 金インゴット → ピストン → ホッパー → エンチャント済みの金リンゴ |
| 周期軸 | レッドストーンダスト → 時計 → レッドストーンリピーター → エコーシャード |
| 範囲軸 | ガラス → 竹 → TNT → コーラスフルーツ |

速度軸は「ピストン(力ずくの推進)→ホッパー(自動搬送)→エンチャント済みの金リンゴ(格上げの極致)」、周期軸は「時計(時間そのもの)→リピーター(タイミング制御)→エコーシャード(反響=周期の象徴)」という並びにしてある。範囲軸は「ガラス(見通せる=視界が届く範囲)→竹(まっすぐ・速く伸びる)→TNT(爆発が届く範囲=効果範囲そのもの)→コーラスフルーツ(エンド次元でのテレポート=距離を超える)」という、いずれも「遠くまで届く/伝わる」ことを連想させる並びにしてある(Tier3は当初スカルクセンサー(離れた振動を検知する)だったが、ユーザー要望によりTNTへ変更した)。意味シンボルは当初「軸ごとに固定1種類、Tierが上がるごとに個数だけ増やす」案(速度=動力レール固定等)だったが、Tierごとに別の意味シンボルに変える案の方が「Tierが上がるほど機能が高度化していく」物語性を表現できるため、そちらを採用した。

**コントローラ本体**(`BP/recipes/controller.json`): 四隅=鉄インゴット(キット群と共通の構造材)、上=レッドストーンリピーター、下=製図台、左右=チェスト、中央=レッドストーンブロック。リピーター(周期を刻む)は「一定間隔でネットワークを監視する」というコントローラの実際の挙動(本章「スループット制」のtickポーリングループ)の比喩。製図台は「ネットワーク全体の構成(どこに何が繋がっているか)を把握する」というコントローラの役割の比喩。チェストは倉庫という機能そのものの直接的な比喩で、中央のレッドストーンブロックが処理を駆動する動力源にあたる。**T1アップグレードキットの方がコントローラ本体より安いという逆転が生じている**が、これは意図的な判断: T1は「コントローラ入手時にまず試してみる」程度の軽い位置づけであり、実際の強化幅(スループット等)も他のTierより控えめにしてあるため、入手コストが低くても違和感がないと判断した。

**ターミナル系列(基本ターミナル/自動端末/在庫管理ターミナル)**: 自動端末・在庫管理ターミナルは、基本ターミナルそのものをレシピの中央に消費する積み上げ式にした(アップグレードキットと同じ発想。「基本ターミナルを改造して自動化する」という物語性も出る)。基本ターミナルは上=ガラス、下段左右=ガラス、下段中央=レッドストーンパウダー、最下段=鉄インゴット×2+ホッパー(20章で中央のコンパレーターをレッドストーンパウダーへ、上段四隅の鉄インゴットを撤去する見直しを行った。ホッパーは「実際にチェストとアイテムをやり取りする」機能を素材面でも裏付ける狙いで、最下段に配置している)。自動端末・在庫管理ターミナルは共通の骨格(中央列に縦一列で、上の1マスだけ(自動端末=ドロッパー/在庫管理ターミナル=エンダーアイ)で機能を差別化し、中央=基本ターミナル、下=時計)を持つ。ドロッパーは「信号を受けて自動でアイテムを排出する」バニラ機構そのもの(自動端末の自動搬送)、エンダーアイは「遠く離れた場所を見通す」道具(在庫管理ターミナルがネットワーク全体を見渡す点)の比喩。全て作成数1固定(「基本ターミナルを素材にしてカスタマイズする」という位置づけ上、1クラフトで複数出るのは不自然なため)。**21章のレシピ見直しで左右のレッドストーンリピーター2個を削除**しており、当初あった「リピーターと時計はどちらも『時間』を象徴する素材」という左右対称の意図は無くなっている(定期チェックで動く機器であることは時計単体が引き続き語っている)。

**レンチ**: 「毎回持ち歩く消耗品ではなく、最初に1個作れば十分な設定用ツール」という位置づけのため、他のどのレシピよりも軽くしてある(銅インゴット×2+棒×1、意味シンボルなし)。ネットワーク機器群(鉄ベース)とは異なり銅を使うことで、「ネットワークの一部ではなく、個人が持つ道具」という立ち位置の違いも素材で表現している。

### 預け入れ（格納）: 引き出しと対称な逆方向の処理

引き出し（ネットワークのストレージ → ターミナルの張り付いた先）と対称な、逆方向の処理を追加した。ターミナルの張り付いた先のコンテナに入っているアイテムを、ネットワーク内のストレージ群へ配って格納する。

- データモデル・処理モデルは`Order`/`OrderLine`と完全に対称（`DepositRequest`/`DepositLine`、`wh:deposits:<networkId>`、`wh:deposit_issuing:<networkId>`、`wh:deposit_partial:<networkId>`）。`DepositRequest`は当初、表示用ID・実行者情報を持っていなかった（完了通知の仕組みが無いため、そもそも表示する機会が無かった）が、後に追加したコントローラUIの「状況」タブ(4章参照)で引き出しと同様の表示をしたいという要望を受け、`Order`と同じ形（`displayId`は`generateDepositId()`による表示用ID、`playerName`は実行者。自動預け入れは空文字列で「自動」表示になる、`AUTO_ORDER_PLAYER_NAME`と同じ扱い）を追加した。厳密な一意ID(`id`、キャンセル指定用)とは別に`displayId`を持つ点は、`Order`(`id`=表示用/`requestId`=厳密)と役割の対応が逆になっている（`DepositRequest.id`は元々`generateId()`による厳密な一意IDだったため、そちらを変えずに表示用を追加する形にした）。
- ただし**別系統として実装**し、スループット・発行遅延の定数（`DEPOSIT_THROUGHPUT_PER_TICK`等）を引き出し側とは独立して調整できるようにした。処理自体は同じ`system.runInterval`ループの中で、引き出しの処理に続けて呼び出す（`networkProcessing.ts`が両方を束ねる）。
- `DepositRequest`/`DepositLine`自体は引き出しと同様「何を何個」を持てる形にしてあるが、MVPのUIでは検索・カート選択はさせず、**張り付いた先のコンテナに今入っている物を全部まとめて1回のリクエストにするワンボタン**にした（実用上、細かい数量指定のニーズがほぼ無いため）。将来UIを変えたくなってもデータ構造の変更は不要。
- 搬入先(=ネットワーク内のどのストレージに入るか)は、`network.storages`を順番に試し、入りきらない分は次のストレージへ回す（1つのストレージで満杯なら次へ、を全て試して入らなければ「不足」として`wh:deposit_partial`に記録）。
- **既にその品目を持っているストレージへの優先スタッキング**: 素朴に品目ごとに全ストレージを毎回スキャンすると、大規模なネットワーク(例: ラージチェスト30個=1620スロット)で品目数に比例して重くなりすぎる(最悪、1回の預け入れで数万スロット読み取りになり得る)。そのため「どのストレージに何があるか」の索引(`buildStorageIndex`)を**ネットワークごとに1tickにつき1回だけ**作り、そのtick内の全ラインの処理で使い回す。索引はtick開始時点のスナップショットなので、同じtick内で新しく届いた品目までは優先先として反映されないが、次のtickには自然に反映されるため実用上問題ないと判断した。ラージチェスト30個規模で実機検証済み、体感できるパフォーマンス問題は無かった。

### 倉庫の整理: コントローラUIからのワンショットタスクを同じtickループに乗せる

コントローラを(レンチではなく)素手等で右クリックすると`showControllerUi`(`controllerUi.ts`)が開く。「状況」「整理」「設定」の3タブ構成で、タブの選択はターミナルUIと同じく`dropdown`で行う。ドロップダウンの並び順・開いた直後のデフォルトタブのどちらも「状況」(value=0)。「整理」タブは「倉庫の整理」ボタンのみを持つ。押すと、接続されているストレージ群の部分スタックをまとめて空きスロットを増やす処理がキューに積まれる。引き出し/預け入れと同じ「キュー+予算制のtickループ」に乗せることで、実装・永続化・ウォッチドッグ耐性の仕組みを使い回す。

「状況」タブは、進行中の引き出し(`listActiveOrders` = `wh:issuing`+`wh:orders`)・進行中の預け入れ(`listActiveDeposits` = `wh:deposit_issuing`+`wh:deposits`)・進行中の整理(`listActiveOrganize` = `wh:organize`。同時に1件までなので実質0件か1件)を、それぞれ独立したセクション(見出し+ページャー+行ボタン+ページャー、間に`divider`)として上下に並べて表示する。他の一覧UIと同じくROW_COUNT=8のページャー付き。実装本体(`setupCancellableList`/`setupOrderStatusSection`/`setupDepositStatusSection`/`setupOrganizeStatusSection`)は当初`controllerUi.ts`に直接書いていたが、後にターミナルUIにも同じ「状況」タブを追加する際、そのままだと丸ごと重複することになるため`statusListUi.ts`という共通モジュールに切り出した(7章のターミナルUI「状況タブ」参照)。

- **引き出しセクション**: 各行のラベルは「#表示ID 端末名 (プレイヤー名 or 自動発注なら「自動」)」。
- **預け入れセクション**: 引き出しと同じ表示にしてほしいという要望を受け、`DepositRequest`にも`displayId`/`playerName`を追加した(前節参照)ため、引き出しと全く同じ「#表示ID 端末名 (プレイヤー名 or 自動預け入れなら「自動」)」形式で表示する。
- **整理セクション**: `OrganizeLine`は引き出し/預け入れの`OrderLine`/`DepositLine`と違い数量ベースの進捗(`requested`/`delivered`)を持たず、品目ごとの`done`フラグしか無い。また整理には搬入出先のターミナルという概念も無い。そのため表示は品目数ベースの進捗にし、ラベルは「#表示ID 整理(完了品目数/全品目数) (プレイヤー名 or 自動なら「自動」)」、ツールチップも同じ進捗を示すだけの簡潔なものにしている(整理対象はネットワーク全体のカタログなので、引き出し/預け入れのように品目を1行ずつ列挙すると長くなりすぎるため)。整理は自動発注/自動預け入れのような自動トリガーが無く常にプレイヤー操作から始まるが、`OrganizeRequest`自体は`Order`/`DepositRequest`と型を揃えるため`playerName`を持たせてあり、コードは一貫して「空文字列なら自動」という表示ロジックにしている。
- **共通実装**: 引き出しの`OrderLine`と預け入れの`DepositLine`は品目ごとの進捗を全く同じ形(`itemTypeId`/`itemName`/`requested`/`delivered`/`exhausted`)で持つため、一覧・ページャー・キャンセル・ツールチップの生成ロジックは`setupCancellableList<T>`という1つのジェネリック関数にまとめ、引き出し用・預け入れ用それぞれの薄いラッパー(`setupOrderStatusSection`/`setupDepositStatusSection`)から呼び出している。
- **スループット表示とヘッダーの兼用**: 各セクションの一覧の直前に、現在のスループット(処理ループ1回あたりの予算。`ORDER_THROUGHPUT_PER_CYCLE`/`DEPOSIT_THROUGHPUT_PER_CYCLE`/`ORGANIZE_THROUGHPUT_PER_CYCLE`をそれぞれexportして参照。4章「スループット制」参照)を「{引き出し/預け入れ/内部} §7{値}/{サイクルのtick数}tick」(`throughputLabel()`、例:「引き出し 10/20tick」)という固定テキストの`label`で表示する。整理は搬入出先のターミナルを介さずストレージ間でアイテムを動かすだけ(=ネットワーク外部とやり取りする引き出し/預け入れとは性質が違う)なので「内部」と表示している。MVPでは全て固定値でtickごとに変化しないため、値の表示はObservableではなく素の文字列で十分。このラベルは、`setupOrderStatusSection`等が内部で描画するセクション見出し(「引き出し（タップでキャンセル）」等)を兼ねている: 当初は両方を別々に表示していたが、見出しとスループット表示が並んで冗長という指摘を受け、コントローラUIから呼ぶ時だけ`includeHeader`引数(`setupOrderStatusSection`/`setupDepositStatusSection`/`setupOrganizeStatusSection`の最後の引数、デフォルト`true`)を`false`にして、`setupCancellableList`内蔵の見出し行を省略するようにした(ターミナルUI/自動端末UIの「状況」タブはスループット表示自体を持たないため、`includeHeader`は省略してデフォルトの`true`のまま=内蔵の見出しをそのまま使う)。

各行のツールチップには品目ごとの「配送済み/要求数」を色分け(未完了は§e、完了は§a、品切れ確定は§c)して表示する。行をタップするとそのリクエストをキャンセルする(前節「引き出しのキャンセル」参照。預け入れのキャンセルも`cancelDeposit`/`wh:deposit_cancels:<networkId>`/`processDepositCancels`として全く同じ専用優先キュー方式で実装した)。`system.runInterval`でタブ表示中は各セクションの一覧を定期的に自動更新し、`form.show()`(DDUIのCustomFormはプレイヤーが閉じるまで解決しないPromiseを返す)の完了時に両セクション分のタイマーを`system.clearRun`で止めることで、タイマーを残さず安全に自動更新を実現している。更新間隔は当初1秒(20tick)にしていたが、更新のたびにボタンのTooltipが一瞬消えて再表示されチラつく(実機で確認)という指摘を受け、5秒(100tick)に緩めた。

- **リクエストは同時に1件まで**: ボタンを押した時点のネットワーク在庫カタログ(`scanCatalog`)をスナップショットして`OrganizeRequest.lines`(品目ごとに`done`フラグ)を作る。処理中に再度ボタンを押しても、既存のリクエストが終わるまで新規キューイングはしない(`submitOrganize`が`wh:organize:<networkId>`の既存有無を見て弾く)。整理はやり直しても実害が無いワンショット処理なので、複数リクエストを積む意味が無いため。
- **`processNetworkOrganize`**(`organizeProcessing.ts`)は`processNetworkOrders`/`processNetworkDeposits`と並んで同じ`system.runInterval`ループから呼ばれる。未処理の品目(`!line.done`)を1つ取り出し、`ORGANIZE_THROUGHPUT_PER_TICK`(MVP: 十分大きい固定値)の予算内でその品目のスタック整理(`compactKeyInStorages`)を行い、ラインごとに書き戻す。引き出し/預け入れと同様、予算切れなら`done`を立てずに次tickへ持ち越す(退避先の空き不足で行き詰まった場合の扱いは後述)。
- **整理アルゴリズム(`compactKeyInStorages`)**: 呼び出しのたびに対象品目(displayKey一致)が入っている全スロットを`network.storages`の順にスキャンし直し、先頭側(dst)と末尾側(src)からの2ポインタで「dstの空きにsrcの中身を詰める」を繰り返す。専用の再開カーソルは持たず、予算切れで中断しても、次回呼び出し時に現在のコンテナ状態を再スキャンするだけで自然に続きから再開できる(整理は単調に収束する操作なので毎回スキャンし直しても安全)。
- **見た目が同じでも実際には積めない組は無理にマージしない**: displayKey(typeId+nameTag)は「検索UIで区別できない属性は区別しない」という表示上の同一性基準(5章)であり、耐久値やエンチャントなど実際のスタック可否には影響する属性まで無視するわけではない。そのため実際のマージ判定には`ItemStack.isStackableWith`(Minecraft本来のスタック可否判定、耐久値等も見る)を使う。積めない組が見つかった場合はsrcはそのまま維持し、dstだけ進めて別の組み合わせを試す。
- **完了通知**: `submitOrganize`はボタンを押したプレイヤー名(`OrganizeRequest.playerName`、`Order.playerName`と同じ考え方)を記録しておき、リクエストの全ラインが`done`になった時点(`processNetworkOrganize`が`queue`から取り除くのと同じタイミング)で、コントローラの設定タブの「整理完了時に通知する」(コントローラごとの非表示エンティティ`wh:controller_settings`の動的プロパティ、`controllerSettings.ts`。ターミナル設定と同じ発想でネットワーク全体のJSONを肥大化させない)がONなら、ログイン中ならそのプレイヤーに通知する(オフラインなら無視、MVP)。整理には「不足」という概念(引き出しの`shortfall`のような数量の過不足)は無いが、Drain指定ストレージの退避先が足りず一部の品目を諦めて完了扱いにしたケース(前節参照)はありうるため、通知メッセージはその区別を行う: 完了時点で改めてDrain指定ストレージの中に対象品目が残っているか(`anyMatchingItem`)を調べ、残っていれば「一部の品目は整理できませんでした」と付け加える(専用のフラグを持たせず、完了時点の実際の状態から都度判定することで、諦めたラインの検知漏れを防いでいる)。

## 5. アイテム同一性の判定

**原則: 「検索UIの表示で判別できない属性は区別しない」。** 比較関数は表示ロジックから導出する一枚岩の関数にし、カタログのグルーピングにも引き出しの抽出マッチングにも同じ関数を使う。表示と区別の基準がズレることを構造的に防ぐ。

### MVPでの最低限表示

- 標準表示名（`{translate: itemStack.localizationKey}` というRawMessageでクライアント側のlang解決に任せる。自前の翻訳テーブルは持たない）
- カスタム名（銘、`nameTag`）がある場合はそちらを優先表示
- 在庫数

耐久値・エンチャント・lore・旗の模様・本の中身・地図IDなどは**表示しない＝区別しない**（将来、表示要素を追加したらその都度、比較関数にも同じ属性を追加する）。

### 比較関数

```ts
function displayKey(stack: ItemStack): { typeId: string; name?: string } {
  return { typeId: stack.typeId, name: stack.nameTag };
}
```

`typeId`と`nameTag`はどちらも単純な文字列なので、そのままJSON化して`OrderLine`に埋め込める。これにより、複雑な属性をtickをまたいで正確に覚えておくための特別な仕組み（非表示エンティティによる「見本帳」など）は不要と判断し、設計から除外した。将来、表示に出ない複雑な属性（本の中身、地図IDなど）で区別したいという要件が生まれた場合の拡張手段として、アイデアの記録だけ残しておく（8章）。

## 6. ターミナルの実体（設計変更: 非表示バッファエンティティ方式から「張り付いた先のストレージへ直接搬入」方式へ）

当初は、ターミナル自身に9スロットの受け取りバッファを持たせる設計だった。しかし**ブロックには汎用の`minecraft:inventory`コンポーネントが存在しない**（チェスト等のコンテナ機能はエンジンにハードコードされた特別扱いで、データ駆動のカスタムブロック定義に後付けできない。実機検証でも`BP/blocks/*.json`に`minecraft:inventory`を書くと`child 'minecraft:inventory' not valid here`エラーでブロック定義自体がロードされないことを確認済み）ため、非表示・非当たり判定の専用エンティティ（`minecraft:inventory`コンポーネント持ち）をターミナルに紐づけて代用する方式を実装し、スポーン/検索/読み書き/永続化/ブロック破壊時のドロップ/無敵化/意図しない死亡時の自己修復まで**すべて実機検証で正しく動作することを確認していた**（検証内容は本章末尾に記録として残す）。

その後、「ターミナルは右クリックで引き出しUIを開くだけにし、引き出しされたアイテムはターミナルの張り付いた先に置いたストレージへ直接届ける」という方針に変更した。この方式には次の利点がある。

- **非表示エンティティの仕組みが丸ごと不要になる**。搬入先が実在するバニラコンテナになるため、不可視化・無当たり判定・無敵化・自己修復といった対策そのものが要らない（実在のチェストは元々永続化もブロック破壊時のドロップもエンジンが自然に処理する）。
- **引き出し専用UIも不要になる**。プレイヤーは張り付いた先のチェストを普通に右クリックして開けばよく、バニラのドラッグ&ドロップ操作がそのまま使える。
- ターミナルブロック自身は状態を持たない、純粋な「UIの入口」になる。

### 搬入先の解決方法

ターミナルには`minecraft:placement_position`トレイト(`enabled_states: minecraft:block_face`)があり、プレイヤーが設置時にクリックした面がそのままブロックステートの値になる(6方向)。`Order.terminal`(ターミナルの座標)から、処理のたびに動的にこのステートを読み、クリックされた面と逆側の隣接ブロック(=設置の際に支持ブロックとして使われた面)を`getAttachedStorageLocation`(`terminalBlock.ts`)で求め、その`minecraft:inventory`コンポーネントを搬入先として使う。ネットワークへのレンチ接続は不要（ストレージ一覧＝供給元、ターミナルの張り付いた先＝そのターミナル専用の納品先、という役割分担）。張り付いた先にコンテナが無い、または満杯で入らない場合は、既存の「不足」の仕組み（`wh:partial`への記録）にそのまま乗せる。

### (参考記録) 非表示バッファエンティティ方式で実機検証していた内容

現在は不採用だが、得られた知見は将来また同様の課題（ブロックにカスタムの状態を持たせたい）に直面した際の参考として残す。

- ブロックには`minecraft:inventory`が使えないが、**エンティティには正式に存在する**（`container_type`, `inventory_size`等を指定可能。`container_type: "container"`は自動UIオープンなし＝完全にスクリプト制御下）。`entity.getComponent("inventory").container`で`getItem`/`setItem`/`addItem`が使える。
- `minecraft:custom_components`を持つカスタムブロックの`onPlayerInteract`は、通常右クリック/Shift+右クリックの両方で確実に発火し、`event.player.isSneaking`で正しく判別できる（実機確認済み）。
- `dimension.spawnEntity()`でのエンティティ生成、`dimension.getEntities({ type, location, maxDistance })`での再取得、`addItem`/`getItem`の読み書き・スタッキング、ワールド再読み込みをまたいだ永続化を実機確認済み。
- ブロック破壊時は`world.afterEvents.playerBreakBlock`で検知し、対になるエンティティの全スロットを走査して`dimension.spawnItem()`でドロップさせてから`entity.remove()`。`remove()`は「死亡」を経由しない即時除去なので、`minecraft:inventory`の`private`設定に関わらずエンジンは自動ドロップしない(常に自前でドロップ処理が必要)。
- `minecraft:damage_sensor`（`{"triggers": {"deals_damage": "no"}}`）であらゆるダメージを無効化し、通常は`remove()`以外の経路では死なないようにできる(実機確認済み)。
- 保険として`private: false`（死亡時の自動ドロップを有効のまま）にしておき、`world.afterEvents.entityDie`を監視。対応するブロックがまだ存在するなら新しいエンティティを再生成し、死亡時にこぼれた実アイテムを周辺スキャンで回収して詰め直す自己修復を実装・実機確認済み。`EntityDieAfterEvent.deadEntity`はハンドラ内で`location`等が読み取り可能であることも確認できた。

### ターミナルごとのローカル設定（非表示エンティティ方式）

ターミナルごとにローカルな設定（例: 引き出し完了時に通知するか）を持たせたくなった。ブロックが状態を持てない制約は変わらないため、以下の2案を検討した。

1. **(不採用) `network.terminals`(所属情報)に設定を埋め込む** — 一度この形で実装したが、設定を1つ変えるだけでもネットワーク全体のJSON(ストレージ一覧・他のターミナル情報等すべて)を読み書きすることになり、設定項目が増えるほど「所属情報」と「個々の設定」が絡み合って肥大化していく。
2. **(採用) ターミナルごとに専用の非表示エンティティ(`wh:terminal_settings`)を持たせ、そのエンティティの動的プロパティに設定を保存する** — 設定の読み書きがそのターミナル1台分で完結し、`network.terminals`は所属情報(座標)だけのシンプルな`Vector3[]`のまま保てる。設定項目を増やしたくなっても、エンティティに動的プロパティを追加するだけで済み、ネットワーク全体のデータ構造に影響しない。

6章のバッファ用エンティティと同じ仕組み（非表示・無当たり判定・`onPlace`で生成・`onPlayerBreak`で削除）を流用しているが、保持するのは軽量な設定値だけで実物のアイテムは無いため、ドロップ処理や、万一エンティティが失われた際の自己修復のような作り込みはしていない（失われても「設定がデフォルトに戻る」程度の実害で済むため）。事故を減らす目的で`minecraft:damage_sensor`による無敵化だけは入れてある。

**(実機で発見された不具合の修正済み)** 設定エンティティを探す`findSettingsEntity`は`dimension.getEntities({ type, location: centerOf(loc), maxDistance })`で位置検索するが、当初`maxDistance: 2`にしていた。ストレージ用に同じ仕組み（`wh:storage_settings`、Drain機能。3章参照）を作った際、チェストを1ブロック間隔で隣接配置したところ、あるストレージのDrain設定を調べるはずが**1ブロック隣の別のストレージの設定エンティティを誤って拾ってしまう**不具合が発生した（隣接ブロックの中心同士の距離は1.0で、`maxDistance: 2`の範囲に余裕で収まってしまうため）。ターミナルはこれまで密に隣接設置されるケースが無く顕在化していなかっただけで、同じ構造的欠陥を抱えていた。

修正案として、まず`maxDistance`を隣接ブロック中心間の最小距離(1.0)より小さい`0.5`にする案を試したが、「距離のしきい値のチューニングに正しさを依存させる」設計そのものが脆いのではという指摘を受け、より根本的な方式に変更した: **各設定エンティティに、自分がどのブロック座標に属するか(`wh:owner_loc`、`spawnEntity`直後にJSON文字列で書き込む)を自己申告させ、`getEntities`の近傍検索(`maxDistance`はどのみち球状の絞り込みにしか使えず、「その座標そのもの」を厳密取得するAPIが存在しないため必要)で候補を絞り込んだ後、`wh:owner_loc`が要求座標と厳密一致するものだけを採用する**。これにより正しさは距離の数値に依存せず、厳密な座標一致という自明な不変条件だけに依存するようになった。

なお、この修正より前に設置されていたターミナル/ストレージの設定エンティティには`wh:owner_loc`が書き込まれていないため、修正後は「見つからない」扱いになり、次に設定を変更した時点で新しいエンティティが作り直される(名前やDrain指定は一度リセットされ、再設定が必要)。古いエンティティは孤立するが、実害の無い非表示エンティティなので放置してよい(このファイル冒頭で述べている自己修復不要の方針と同じ)。

現在の設定項目:

- `wh:notify_on_complete`（真偽値）: 引き出しの配送完了時に通知するか。デフォルトtrue。
- `wh:name`（文字列）: 通知に表示するターミナル名。設定タブの`textField`からいつでも編集できる。`onPlace`時に`generateTerminalName()`（区別用の仮名。`端末-XXXX`という、引き出しID(素の4桁)とひと目で見分けられる接頭辞付きフォーマット）で自動的に初期値を入れる。
  - (不採用) 金床で銘を付けたアイテムから設置した場合に、その名前を自動で引き継ぐ案も検討したが、`beforeOnPlayerPlace`(プレイヤー情報はあるが位置はまだ確定しない)と`onPlace`(位置は確定するがプレイヤー情報が無い)の間で情報を橋渡しする一時マップが必要になるなど複雑度が見合わないと判断し、設定タブでの手動編集のみに絞った。

## 7. ターミナルUI

UIフレームワークは `@minecraft/server-ui` の新しいリアクティブフォームAPI（Microsoft公式名称「DDUI」、`CustomForm`/`ObservableString`等）を使う。

**MVPではテキストラベルの`CustomForm`のみ**で核となる処理（ネットワークのデータ構造、引き出し処理）を作り切ることを優先する。アイテムアイコン付きグリッド表示（JSON UIによるフォームの見た目上書き。参考: [Chest-UI](https://github.com/Herobrine643928/Chest-UI)。ただし同プロジェクトもインベントリ部分は見た目だけでドラッグ&ドロップは実現していない＝本質的にはクリック駆動のボタンを化粧しているだけ）は、UX向上のための後日の視覚強化として保留する。個人利用アドオンとして将来のMinecraftアップデートとの互換性は考慮しなくてよい、というスタンスなので、この種の手法自体は将来的な採用候補として排除しない。

- **右クリック（Shift問わず）**: 常に引き出しUIを表示する。引き出し操作は張り付いた先のストレージを直接開くことで行うため、専用UIは無い。「引き出し」「預け入れ」「状況」「設定」の4タブ構成（DDUIに専用のタブ部品は無いため、全コントロールに`visible`/`disabled` Observableを付けて出し分けている）。**タブの選択自体は`dropdown`で行う**（当初はボタン3つを並べてトグルする方式だったが、選んだ瞬間に切り替わる方が分かりやすいという要望を受けて変更した。`ObservableNumber`(選択インデックス)の`subscribe`で各タブの`visible`用`ObservableBoolean`に反映している）。
  1. **引き出しタブ**: 開いた瞬間にそのネットワーク内の全ストレージをスキャンし、`displayKey`でグルーピングしたカタログ（サーバー側の一時変数）を作成する。検索用`textField`と共有の数量`slider`（1〜64）で結果を絞り込み、行ボタンでカートに追加、「確定」で`Order`として送信する。確定時、生成された引き出しID（`generateOrderId()`、英数字4桁）をチャットに表示する。絞り込み結果が表示件数（`ROW_COUNT`=8）を超える場合は、一覧の先頭に「現在のページ数 / 全ページ数」のラベルと「▲ 前のページ」・末尾に「▼ 次のページ」ボタンが（前/次ページが実在する時だけ）現れるページャーで送り行き来できる。ページ数ラベル自体も、2ページ以上ある時だけ表示する（1ページしか無いのがほぼ常なので、常時表示すると煩わしいという指摘を受けた。他のページャー付き一覧＝自動端末のウィッシュリスト検索・コントローラの「状況」タブでも同じ扱いにしている）。検索条件を変えた時は1ページ目に戻す。各行のラベルは「{カートに入れた数}/{在庫数} {品名}」の形式で表示し、クリックのたびにそのスロットの表示だけを更新する（下部にまとめてカート内容を表示する`cartLabel`は、この行内表示で確認できるようになったため廃止した）。カートに入れる数量は`Math.min(..., entry.total)`で在庫数を上回らないようにクランプしている。数量スライダーの下に増減モードのトグル（`increaseMode`、初期値ON=増やす/OFF=減らす）があり、OFFの状態で行をクリックすると、指定した数量分だけカートを減らす（0以下になったらカートから除去する。カートに無い品目を減らそうとした場合は何もしない）。トグルのラベル自体も「増やす」「減らす」と状態に応じて動的に変わる（`ObservableString`をトグルの`subscribe`で更新する。単純な文字列を渡すと初期表示のまま固定されてしまうので注意）。
  2. **預け入れタブ**: 8章参照。
  3. **状況タブ**: コントローラUIの「状況」タブ(4章参照)と全く同じ実装(`setupOrderStatusSection`/`setupDepositStatusSection`)を、`statusListUi.ts`という共通モジュールに切り出して両方から使い回している。違いは一覧の絞り込み範囲だけ: コントローラはネットワーク全体が対象なのに対し、ターミナルの「状況」タブは`fetchAll`に渡すクロージャで`locEquals(order.terminal, block.location)`のフィルタを追加し、**この端末が送信元/宛先になっている引き出し・預け入れだけ**を表示する。タブの並び順は「設定」の1つ上（引き出し/預け入れ/状況/設定）で、コントローラの「状況」タブと違い既定タブにはしていない（既定は従来通り引き出しタブのまま）。
  4. **設定タブ**: 6章の「ターミナルごとのローカル設定」参照。現状は「引き出しの配送完了時に通知を表示する」トグルのみ。
  5. **完了通知**: コントローラが引き出しを処理し終えた時点（全ライン`delivered=requested`または`exhausted`確定）で、その設定がONなら、`order.playerName`と同名のログイン中プレイヤーに引き出しIDを含むメッセージを送る（オフラインの場合は何もしない。MVP）。

### 自動端末(`wh:auto_terminal`)

通常のターミナルとは別のブロック。プレイヤーが手動で操作するのではなく、**張り付いた先のストレージを定期的に確認し、事前に設定した「維持したい在庫量」リスト(ウィッシュリスト)に基づいて過不足を自動でネットワークとやり取りする**。ID接頭辞は他と見分けやすいよう`ATM-`。

- **共通化**: 「張り付いた面から搬入先を求める」(`getAttachedStorageLocation`)、「設定エンティティの生成/削除」(`terminalSettings.ts`)、「ターミナルとして扱ってよいブロックか」(`isTerminalLikeBlock`)は通常のターミナルと共通のモジュールを使う。レンチでの接続対象・引き出し/預け入れ処理側の生存確認(`network.terminals`に登録されているか)も、`wh:terminal`/`wh:auto_terminal`のどちらでも同じ扱いになる(預け入れ処理側も当初`wh:terminal`固定で判定していたため、`wh:auto_terminal`からの預け入れがチャンク未読み込み扱いのまま止まってしまう不具合があり、`isTerminalLikeBlock`に統一して修正した)。
- **ウィッシュリスト**: `WishlistLine[]`(品目+目標数)を、通常のターミナル設定と同じ非表示エンティティ(`wh:terminal_settings`)の動的プロパティ(`wh:wishlist`, JSON文字列)に保存する。UIは「自動引き出し」「状況」「設定」の3タブ構成で、**タブの選択は通常のターミナルUIと同じく`dropdown`で行う**(選んだ瞬間に切り替わる)。「自動引き出し」タブは、ネットワークの在庫を検索して目標数を設定/更新する上段と、現在のリストを一覧して削除できる下段の2段構成。
  - 上段は引き出しUIの検索結果一覧と同じ構成に揃えてある: 検索欄・数量スライダー(1〜64)・「増やす/減らす」トグル(状態に応じてラベルのテキスト自体も動的に変わる)・ページャー(「▲ 前のページ」/「▼ 次のページ」、8件を超える場合のみ)。各行は「{現在の目標数}/{在庫数} {品名}」の形式で表示する。ただし**引き出しUIと違い、目標数は在庫数を上回って設定できる**(在庫を将来的に増やしたい、という用途のため上限を設けていない)。「減らす」モードでクリックすると目標数を減らし、0以下になったらリストから除去する(リストに無い品目を減らそうとした場合は何もしない)。
  - 下段(現在のリスト)は1クリックでその品目をリストから削除する(目標0扱いに戻す)点は変わらないが、表示を上段と同じ形式(カスタム名が無ければlocalizationKeyで解決)に揃えた。目標だけ設定されていて現在庫が0の品目は`networkCatalog`のスキャン結果に出てこないことがあるため、カタログ経由ではなく`new ItemStack(line.itemTypeId, 1).localizationKey`のように、typeIdから直接作った(実在庫を必要としない)`ItemStack`から`localizationKey`を取得している。
  - **状況タブ**: 通常のターミナルUIの「状況」タブ(7章)と全く同じ実装(`statusListUi.ts`)を、この端末に絞り込んで使う。自動端末が自動的に発生させる引き出し・自動預け入れ(`playerName`が空文字列)も、他のターミナル発のものと同じ`Order`/`DepositRequest`として同じキューに乗るため、`locEquals(terminal, block.location)`のフィルタだけで自然に対応できた(自動端末専用の特別扱いは不要)。タブの並び順は通常のターミナルUIと同じく「設定」の1つ上で、既定タブにはしていない。
- **定期チェック**: 専用ループ(`autoOrderCheck.ts`)を、引き出し/預け入れのtickループとは別に**独立した間隔で**回す。当初はMVPとして固定100tick(5秒)だったが、後にユーザー要望でコントローラの周期短縮キットのTierに応じた間隔へ変更した(`networkProcessing.ts`の`startCycleAlignedLoop`、18章参照)。
  1. **不足分の自動引き出し**: ウィッシュリストの目標を下回っている品目があれば、不足分を引き出しする(従来の「自動引き出し」相当)。
  2. **自動預け入れ**(設定タブのトグル、デフォルトON): ウィッシュリストに無い品目、またはリストの目標を上回っている品目があれば、その超過分(目標に無い品目は全量)をネットワークへ預け入れる。不要なら設定タブで個別に無効化できる。
- **重複送信の防止**: チェック周期ごとに毎回「目標数との差分」を送ると、処理がまだ完了していないうちに次のチェックが走って同じ差分を何度も送ってしまう。そのため、引き出し/発行待ちキュー(`wh:orders`/`wh:issuing`)・預け入れ/発行待ちキュー(`wh:deposits`/`wh:deposit_issuing`)を見て、同じターミナル・同じ品目の未完了ラインが既にあればスキップする(`hasPendingOrderFor`/`hasPendingDepositFor`)。この2関数は元々`autoOrderCheck.ts`にしか無かったが、後述の在庫管理ターミナルの定期チェックからも全く同じ理由で必要になったため、それぞれの対象キューを扱うモジュール(`orderProcessing.ts`/`depositProcessing.ts`)にexportされた形で移し、両方の定期チェックから共有している。
- **通知機能は無い**: 自動発注/自動預け入れには人間の操作が介在せず「誰に通知するか」という宛先概念自体が無いため、設定タブに完了通知トグルは置いていない。当初は`notifyOnComplete`トグルを(常に無効化される前提で)置いていたが、これは「トグルをONにしても実際には一切通知されない」という実機での不具合報告を受けて発覚した設計ミスだった。自動発注は共通の`submitOrder`/`finalizeOrder`(`orderProcessing.ts`)を経由するため、`order.playerName`をダミーの空文字列にして送信しており、`finalizeOrder`側の`world.getPlayers().find(p => p.name === order.playerName)`が常に該当者無しになる(=通知トグルの状態に関わらず無条件で無視される)ことが原因。「送信元プレイヤー」ではなく「直近UIを開いたプレイヤー」等を宛先として新たに記録する代替案も検討したが、ニッチな需要のためにその複雑さを持ち込む価値は無いと判断し、トグル自体を撤去した(預け入れ側はそもそも完了通知の仕組み自体が無い)。
- **手動の引き出し・預け入れUIは無い**: 受け取りは張り付いた先のストレージを直接開けばよく(通常のターミナルと同じ)、手動での引き出し・預け入れ操作もこのターミナル自身は提供しない(用途がウィッシュリストに基づく自動運用に限定されるため)。

### 在庫管理ターミナル(`wh:inventory_terminal`)

自動端末と似ているが、目標の**基準が根本的に違う**第三のターミナル。自動端末は「このターミナルのアタッチ先(張り付いた先のストレージ)に置いておきたい量」を基準にするのに対し、在庫管理ターミナルは**「ネットワーク全体で維持したい在庫数」**を基準にする。例えば「丸石640」と指定すると、ネットワーク全体の丸石在庫が640を超えていれば超過分を引き出してアタッチ先へ退避させ(ネットワーク側を減らす)、640未満ならアタッチ先から不足分を預け入れて補充する(ネットワーク側を増やす)。ID接頭辞は`INV-`。

- **既存資産の再利用が大きい**: 実際にアイテムを動かす手段(`submitOrder`でネットワーク→アタッチ先、`submitDeposit`でアタッチ先→ネットワーク)はどちらも既存のものをそのまま使う。UI側も、自動端末のウィッシュリスト検索結果一覧が元々**ネットワーク全体のカタログ**(`scanCatalog(dimension, network)`)を対象に「目標/在庫数」を表示していたため、在庫管理ターミナルの「目標設定UI」(`inventoryTerminalUi.ts`のsetupStockTargetTab)は`autoTerminalUi.ts`のsetupWishlistTabとほぼ同じ実装をそのまま流用できた(行の意味づけだけが「アタッチ先の目標」から「ネットワークの目標」に変わる)。「状況」タブも他の2種と同じく`statusListUi.ts`をこの端末に絞り込んで使う。
- **新設したデータ**: `WishlistLine`と構造は同じ(品目+目標数)だが意味が違うため、混同を避けて別の型`StockTargetLine`・別の動的プロパティキー(`wh:stock_targets`、`terminalSettings.ts`の`getStockTargets`/`setStockTargets`)として持たせている。
- **定期チェック**(`inventoryCheck.ts`、`startInventoryTerminalCheckLoop`): 自動端末(`autoOrderCheck.ts`)と同じ専用ループ(コントローラの周期Tierに追従。18章参照)。品目ごとに「ネットワーク在庫 > 目標」なら引き出しライン、「ネットワーク在庫 < 目標」なら預け入れラインとして集め、それぞれ`submitOrder`/`submitDeposit`にまとめて渡す(1回のチェックで両方向のラインが同時に発生することもある。品目Aは超過、品目Bは不足、ということは普通にありうるため)。重複送信の防止は自動端末と同じ`hasPendingOrderFor`/`hasPendingDepositFor`をそのまま使う。ちょうど目標と一致している(`current === targetAmount`)場合は何もしない安定状態。
  - **ネットワーク全体スキャンのコスト対策**: 自動端末の判定はアタッチ先1個だけのスキャン(`scanContainerCatalog`)で済むが、在庫管理ターミナルは判定にネットワーク全体のスキャン(`scanCatalog`)が要る分コストが高い。そのため、ネットワークにつき**チェック周期ごとに1回だけ**`scanCatalog`し、そのネットワーク内の全在庫管理ターミナルで使い回す(`depositProcessing.ts`の`buildStorageIndex`と同じ、ターミナルごとに舐め直さない設計)。
- **通知トグルは無い**: 自動端末と同じ理由(自動発注/自動預け入れに通知先という概念が無い)。在庫目標に基づく双方向搬送(超過なら引き出し・不足なら預け入れ)自体も常時一体で動く仕様のため、個別に無効化するトグルは無い。
- **自動預け入れ(設定タブのトグル、デフォルトOFF)**: 自動端末の「自動預け入れ」と紛らわしいが対象範囲が違う。在庫管理ターミナルの自動預け入れは**「リストに無い品目がアタッチ先にあれば全量預け入れる」**だけを指す(リストにある品目の扱いは、ON/OFFに関わらず常時有効な在庫目標ロジック側が既に担っている)。デフォルトOFFにしたのは、意図せずアタッチ先の物を全部持っていかれる事故を避けるため(自動端末の対応する設定はデフォルトONなので、ここは意図的に方針を変えている)。動的プロパティは自動端末の`wh:auto_deposit`とは別に`wh:inventory_auto_deposit`を新設した(`terminalSettings.ts`の`getInventoryAutoDeposit`/`setInventoryAutoDeposit`、意味が違うので流用しなかった)。実装(`inventoryCheck.ts`のcheckUnlistedItems)は、アタッチ先のカタログ(`scanContainerCatalog`)を取り、在庫目標リストに載っている品目を除外した上で、残り全量を`submitDeposit`する(自動端末のcheckExcessのうち「リストに無い品目」のケースと同じ考え方)。
- **ブロック定義**: 見た目・当たり判定・設置トレイト(`minecraft:placement_position`、6方向)は自動端末/通常ターミナルと共通のジオメトリ(`geometry.terminal_panel_north`)をそのまま使い、テクスチャの画面部分の色だけ紫系にして見分けている(`BP/blocks/inventory_terminal.json`、`tools/generate-placeholder-icon.mjs`の`inventoryTerminalColorAt`)。`isTerminalLikeBlock`(`terminalBlock.ts`)にも追加し、レンチでの接続対象・引き出し/預け入れ処理側の生存確認のどちらも他の2種と同じ扱いになる。
- **目標在庫数を明示的に0に設定できる「空に設定」トグル**(ユーザー要望): 通常の「増やす/減らす」操作では、目標を0まで減らした瞬間にリストから除去される(=もう管理しない状態に戻る)ため、「ネットワーク在庫が0になるまで引き出し続ける」という状態を維持できなかった。検索欄の「増やす」トグルの上に「空に設定」トグルを追加し、ONの間は検索結果のタップが増減ではなく**その品目の目標在庫数を0に上書き設定(無ければ`targetAmount: 0`のエントリを新規追加)**する専用の動作に切り替わるようにした。「空に設定」がONの間は「増やす」トグルを`disabled`にして操作の混同を防いでいる。`checkStockTargets`(`inventoryCheck.ts`)は元々`targetAmount`に0を想定していなかったわけではなく、単に0を設定する手段がUI側に無かっただけなので、処理側の変更は不要だった(`current > 0 = targetAmount`である限り超過分を引き出し続け、0に達すると安定状態になる)。

### 精密ターミナル(`wh:precision_terminal`)

自動端末はアタッチ先のコンテナ**全体**を対象に品目単位で在庫を維持するが、これではかまど(入力/燃料/出力がスロットごとに役割を持つ)やクラフター(9スロットの配置がそのままレシピになる)のような機器を自動化できない。精密ターミナルは**スロット番号ごとに「維持したいアイテム・数量」と「回収するか」を設定できる**ターミナル。スロット番号の意味(どれが燃料か等)はユーザー自身が把握する前提とし、UIは汎用的な「スロットN」表記に留める。ID接頭辞は`PRC-`。

- **設計判断: 既存の引き出し/預け入れキューにそのまま乗せる**。品目でコンテナ**全体**を探す現在の実処理(`extractFromStorages`/`insertIntoStorages`)では「特定スロットだけ」を対象にできない(コンテナの空いているスロットへ`addItem`する/コンテナ全スロットを走査して同じ品目を探す、という前提のため)。そこで`OrderLine`/`DepositLine`(`state.ts`)に任意の`slotIndex?: number`を追加し、指定スロットのみを対象にする新しい搬入出関数`extractFromStoragesIntoSlot`/`insertSlotIntoStorages`(`storageScan.ts`)を追加した。`processNetworkOrders`/`processNetworkDeposits`(`orderProcessing.ts`/`depositProcessing.ts`)は`line.slotIndex`の有無で従来の関数とこれらを呼び分けるだけで、budget消費・FIFO・完了通知・キャンセル機構は既存のまま変更していない。**タイミングのズレ**(依頼作成時点と実際の処理時点で対象スロットの中身が変わりうる)は、自動端末のウィッシュリストと同じ既存の許容方針のまま(巻き戻し等は行わない)。
  - **既存のキューに乗せるメリット**: ネットワーク共有のスループット予算(4章)・FIFO・完了通知の仕組みをそのまま使い回せる。速度アップグレード軸のTierが上がれば精密ターミナルの搬入出も自動的に速くなる。
  - **重複送信の防止は品目ベースではなくスロットベース**: `hasPendingOrderFor`/`hasPendingDepositFor`は「同じ品目の未完了ラインがあるか」で判定するが、回収対象のスロットはチェックのたびに中身(品目)が変わりうるため、これでは正しく判定できない。そのため`(terminal, slotIndex)`ベースの`hasPendingSlotOrderFor`/`hasPendingSlotDepositFor`を新設した。
- **新設したデータ**: `PrecisionSlotLine`型(`state.ts`)。`{ slotIndex, itemTypeId?, itemName?, targetAmount, collect }`。`targetAmount`は0の場合「目標なし」を表す(この場合`itemTypeId`は未設定)。`collect`は目標外の品目・目標を超えた余剰分をネットワークへ回収するかで、**デフォルトtrue**(ユーザーからの要望)。1台につき最大16件(クラフター9スロットに余裕を持たせた数、UI側で強制)。同じ`slotIndex`のエントリは1つまで(新規設定時に上書き)。
  - **設計の変遷**: 当初「スロット付き希望リスト+リスト外を回収するかの全体トグル」案、次に「スロットごとに入力/出力モードを切り替える」案を検討したが、最終的に「1つのスロットが目標(補充)と回収(余剰・目標外の掃き出し)を両方持てる」形に落ち着いた。目標を「なし」(`targetAmount: 0`)にした上で`collect: true`にすると、そのスロットは中身が何であれ常に全量回収される動きになり、旧来の「出力スロット」相当を表現できる。
- **定期チェック**(`precisionTerminalCheck.ts`の`checkSlot`、`startPrecisionTerminalCheckLoop`): 自動端末と同じ専用ループ(コントローラの周期Tierに追従。18章参照)。エントリごとに次の2つを評価する(同じtick内で両方評価されるが、後述の理由で同時に発火することは無い)。
  1. **不足分の補充**(自動端末の`checkShortfalls`と同じ発想): 目標(`targetAmount > 0`)があり、現在の中身が空または目標品目と一致する場合のみ、不足分を`slotIndex`付きの`OrderLine`として送信する。**品目が目標と矛盾する場合(手動で別の物を置いた等)は補充しない(上書きしない、安全側)**。
  2. **目標外・余剰分の回収**(自動端末の`checkExcess`と同じ発想だが、品目ではなくスロット単位): `collect`がtrueかつ中身がある場合、目標品目と一致していれば超過分だけ、一致しなければ全量を`slotIndex`付きの`DepositLine`として送信する。品目が矛盾する場合、この回収によってそのスロットが空になり、**次回のチェックで初めて正しい品目の補充が始まる**(1で「上書きしない」とした分の帳尻は、2の回収を経て複数チェック周期にまたがって解消される)。
  - 補充と回収が同時に発火しないのは、補充が起こる条件(中身が目標と一致 かつ 不足)では余剰(現在数−目標)が必ず0以下になり、回収の対象にならないため。
- **UI**(`precisionTerminalUi.ts`): 自動端末の`setupWishlistTab`とほぼ同じ構成(タブ: スロット設定/状況/設定)だが、以下の点が異なる。
  - スロット番号の指定が必要なため、`textField`(テキスト入力)を1つ追加した。スロット番号は離散的な整数の指定であり「値の連続的な調整」に向くスライダーは適さないと判断し、`slider`ではなく`textField`にした。
  - **(実機で発見された不具合の修正済み) 検索欄自体が表示されていなかった**: `searchText`という`ObservableString`は用意され`refreshSearch`の絞り込みにも使われていたが、他の3種のUI(自動端末/在庫管理ターミナル/オブザーバー)と違い、これを実際に画面へ出す`form.textField("検索", searchText, ...)`の呼び出し自体が無かった(検索ロジックはあるのに入力欄が存在しない、という実装漏れ)。ユーザー報告を受けて追加して解消した。
  - **(実機での指摘を受けて修正) 検索欄は「検索結果」一覧の直前に配置する**: 「検索」「個数などの設定(数量/トグル類)」「検索結果一覧」の3ブロックがあるUI(このターミナルに加え、通常のターミナル・自動端末・在庫管理ターミナル・オブザーバーの各検索UIすべて)は、検索欄を個数などの設定より先に置くと視線の流れ(まず何を検索するか決めてから量を決める、という順)と、実際にその場で使う順(検索して一覧を見る直前に検索語を打つ)がずれるという指摘を受け、**検索欄を必ず結果一覧の直前**(個数・トグル類の設定より後)に統一した。
  - スロット番号の下に「回収」トグル(デフォルトON)を置く。検索結果をタップする、または最下部の「目標をなしに設定する」ボタンを押すと、その時点のスロット番号・回収トグルの状態で、現在入力されているスロット番号のエントリをその場で上書き設定する(1スロット1エントリのため、自動端末の「増やす/減らす」トグルは無い)。
  - 設定タブは名前欄のみ(回収可否はスロットごとの設定なので、自動端末のような「リスト外は預け入れ」というグローバルなトグルは無い)。
  - 状況タブは他の3種と同じく`statusListUi.ts`をこの端末に絞り込んで使う。`slotIndex`付きのラインは、ツールチップの品目名の前に「スロットN: 」を付けて表示する(`progressTooltip`の小さな拡張。他の3種のターミナルは`slotIndex`を持たないため表示は変わらない)。
  - **実在しないスロットの警告表示**(実機での指摘を受けて追加): 設定したスロット番号がアタッチ先コンテナに実在しない(スロット数が足りない、コンテナが見つからない等の)ケースは、搬入出が静かに失敗するだけで原因に気付きにくい。UIを開いた時点で`getAttachedStorageLocation`+`container.size`を取得しておき、「現在のリスト」の各行について対象スロットが範囲内か判定する(`setupSlotRuleTab`の`slotValidityReason`)。範囲外の場合、行のラベルに`⚠ `(警告マーク)を付け、`tooltip`に理由(コンテナが見つからない/スロット数が足りない)を表示する。**(実機で発見): `CustomForm`のボタンのラベル文字列には`§`書式コードが効かない**(`§c`等をそのまま含めると、色が付かず`§c`という文字列がそのまま表示されてしまう)。`tooltip`側は他のUI(`statusListUi.ts`の`progressTooltip`等)と同じく`§`が効くため、色による強調はtooltip側でのみ行い、ラベル側は記号(`⚠`)のみに留めている。DDUIの`ButtonOptions`にはそもそも色を指定する項目自体が無い(`disabled`/`tooltip`/`visible`のみ)ため、ラベルの見た目を変える手段はテキスト内容(記号・書式コード)しか無く、そのうち書式コードはラベルでは機能しないと分かった。
- **ブロック定義**: 他のターミナル系と共通のジオメトリ(`geometry.terminal_panel_north`)を使い、テクスチャの画面部分を赤系にして見分けている(`BP/blocks/precision_terminal.json`、`tools/generate-placeholder-icon.mjs`の`precisionTerminalColorAt`)。`isTerminalLikeBlock`にも追加済み。
- **クラフトレシピ**(`BP/recipes/precision_terminal.json`): 他のターミナル系(基本ターミナルを中央に消費する積み上げ式)とは異なる独自の形。的(`minecraft:target`、ヒット位置で信号強度が変わる=精密さの象徴)・自動端末(`wh:auto_terminal`)・アメジスト欠片を中央列に縦一列で消費する(`pattern: [" B ", " C ", " A "]`。20章で中段の自動端末を3個から1個へ、四隅のアメジスト欠片2個を撤去するコスト見直しを行い、21章でさらに下段の的1個を削除して的=上のみ・アメジスト欠片=下のみの非対称な縦一列に整理した)。
- **既知の制約: クラフター(`minecraft:crafter`)は搬入出できない**(実機で確認): 精密ターミナルをクラフターに張り付けても、`dimension.getBlock(attachedLoc)?.getComponent("inventory")?.container`が`undefined`になり、「搬入先のコンテナが見つかりません」の警告が常に出る。同じ手順でチェストに張り替えると正常に動作することを実機で確認しており、Script APIがクラフターの`minecraft:inventory`コンポーネントを(現バージョン時点で)公開していないことが原因と考えられる。当初「クラフターの自動化」を主な動機の一つとして精密ターミナルを設計したが、この制約はアドオン側では回避できないため、**クラフター対応は見送り**、「スロットを指定できる自動ターミナル」という当初の要件(かまど等クラフター以外の複数スロット機器への対応)を達成した時点で本機能の実装を区切りとした。

### 引き出しUIのグリッド化(Chest-UI方式)の検証中(PoC)

もっとも使用頻度が高いUIである引き出しタブを、Chest-UIを参考にしたインベントリ風のアイテムアイコングリッドに作り替えられないか、設計とPoCを進めている。**まだ採否は決まっていない。**

**技術調査で判明したこと**: Chest-UIの実装をソースレベルで確認したところ、以下のような**Mojang非公式・非文書化のBedrock内部挙動**を複数組み合わせて実現していることが分かった。

- グリッド化自体は`ActionFormData`(新しいDDUIの`CustomForm`ではなく、1回のクリックで閉じる旧世代のフォームAPI)のタイトル文字列に`§c§h§e§s§t§...§r`という書式コードだけの目印文字列を仕込み、JSON UI側(`RP/ui/*.json`によるフォーム見た目の上書き)でそれを検知してレイアウトをリスト表示からグリッド表示に切り替える、というハック。
- アイテムアイコンの実描画は、本来ビーコン画面用のコントロールである`beacon.item_renderer`を転用し、Bedrockのレガシーな数値アイテムID(例: `minecraft:diamond` → `306`)を渡すことで行っている。この数値ID対応表(`typeIds.js`)は約2400行、コミュニティによる手動メンテナンスの成果物。
- スタック数・耐久値は、ボタンのラベル文字列先頭に`stack#01dur#00§r`のような目印を仕込み、JSON UI側の文字列演算(`'%.8s' * text`のような、printf書式を利用した部分文字列抽出のハック)で読み取って表示している。

これは本プロジェクトが過去に(二連チェスト判定などで)「非公開の挙動には依存しない」という原則で痛い目を見た反省と真正面から矛盾する。個人利用アドオンなので将来のアップデート互換性は考慮しなくてよいという方針は既にあるが、それとは別に「**そもそも今のゲームバージョンで動くか自体が未検証**」というリスクがある(数値IDベースの仕組みは廃止が進んでいる領域のため)。ユーザーとの相談の結果、視覚的な完成度を優先し、この非文書化の手法をそのまま移植する方針でPoCに進むことにした(安全側の代替案として「グリッドレイアウトのみJSON UIで実現し、アイコンは`ActionFormData.button()`の公式`iconPath`引数(実テクスチャパス)だけで済ませる」案も検討したが不採用)。

**PoCの構成**(`wh:test_chest_ui`アイテム、`chestUiPoc.ts`、`RP/ui/wh_chest_ui.json`):

- Chest-UIの`RP/ui/chest_server_form.json`を土台に、耐久値バー・スロット背景画像・プレイヤー自身のインベントリ表示部分を省いた最小構成に切り詰めて移植した(いずれもテクスチャアセットの追加が必要になるため、まずアイコン+スタック数+クリック検知という核となる仕組みだけを検証する)。
- 名前空間は`chest_ui`のままだと(実際にChest-UIリソースパックを併用した場合に)衝突しうるため`wh_chest_ui`に変更し、目印文字列も`§w§h§c§h§e§s§t§...§r`のように独自プレフィックスを付けた。
- レガシー数値IDは、動作確認用に10品目分だけ`typeIds.js`から転記した(本採用する場合は全品目分の対応表が必要になる。10章「未確定・要検証事項」参照)。
- `wh:test_chest_ui`を(素手同様に)使うと18マスのグリッドが開き、クリックしたスロットの品目をチャットに表示する。

**確認したいこと**: (1) 現在のゲームバージョンで実際にアイコン付きグリッドが正しく描画されるか、(2) クリックしたスロットを正しく検知できるか、(3) フォームを閉じて再表示するループの体感速度(検索のリアルタイム絞り込みを諦める代わりにこの方式を採用する前提のため)。

### 方向転換: 「他アドオンのアイテムが表示できない」問題とPoC第2段階(実在コンテナ方式)

`ActionFormData`のボタンを偽装するグリッド方式(数値ID方式・iconPath方式のどちらも)には、**事前に用意したアイテムごとの対応表に無い品目は正しいアイコンを付けられない**という共通の構造的限界があることが分かった。これは実装の不備ではなく、Chest-UI自身の実装(プレイヤーの実インベントリを表示する時でさえ同じ対応表を経由している)からも裏付けられる。倉庫アドオンの引き出しUIは「他アドオンのアイテムが入っていても正しく機能する」ことが必須要件のため、**この手法のファミリー全体を不採用とする**。

代わりに、**実在のコンテナ(バニラの`minecraft:inventory`)をアイコン描画用のキャンバスとして使う**方式を検証中。ポイントは以下:

- `common.item_renderer`(バニラのUIソース、Mojang公式の`bedrock-samples`リポジトリで確認済み)は`"type": "custom", "renderer": "inventory_item_renderer"`という**エンジンのネイティブなアイテム描画機能**で、`container_items`という、実際に開いたコンテナの中身をエンジンが自動的に反映する収集(コレクション)にバインドされる。対応表を一切経由しないため、どんなアイテム(他アドオン含む)でも常に正しく描画される。
- ただし、この描画が働くのは「プレイヤーが実際にコンテナを開いた瞬間」のみで、`ActionFormData.show()`のようにスクリプトから即座に呼び出す手段は無い(該当するAPIが存在しないことを確認済み)。そのため「引き出しUIを一発で開く」という現在の使用感は諦める必要がある。
- 中身は実在のコンテナなので、スクリプトから自由に`setItem`で書き込める。つまり「本物のチェストの中身をそのまま見せる」のではなく、「本物のコンテナに、ネットワークの在庫を表す代表アイテムを事前に詰めておき、それをプレイヤーに開かせる」という使い方になる(多くのBedrockアドオンの「仮想ショップ」機能と同じ発想)。

**PoC第2段階の構成**(`wh:test_chest_entity`アイテム、`chestEntityPoc.ts`、`BP/entities/test_chest_entity.json`):

- ブロックには`minecraft:inventory`を持たせられない(6章参照)ため、非表示エンティティ(6章で実機検証済みの技術)に`minecraft:inventory`を持たせる。
- `container_type`をバニラのトロッコ+チェスト用の`"minecart_chest"`にすることで、トロッコとしての見た目・挙動を一切持たせずに「右クリックで通常のチェストと同じ27マスUIが開く」ことを狙う(Mojang公式`bedrock-samples`の`chest_minecart.json`で確認したcontainer_typeの値を転用)。
- まだ見た目の上書き(JSON UIでの独自スキン)はしておらず、バニラ標準のチェストUIがそのまま開く想定。アイコン描画の正しさをまず確認するのが目的で、見た目のカスタマイズは次の段階。
- クリック検知は、公式イベント`world.afterEvents.entityContainerOpened`/`entityContainerClosed`(Mojang公式ドキュメントに存在を確認済み)を使い、開いた瞬間と閉じた瞬間のコンテナの中身を比較して差分をチャットに報告する簡易実装。

**確認したいこと**: (1) 独自の非表示エンティティに`container_type: "minecart_chest"`を付けるだけで、トロッコの見た目・挙動を伴わずに右クリックでチェストUIが開くか、(2) 開いたときに表示される10品目のアイコンがすべて正しく表示されるか(対応表を経由しないので、原理的には全品目で正しく表示されるはず)、(3) `entityContainerOpened`/`entityContainerClosed`イベントで開閉と中身の変化を正しく検知できるか。

**実機検証の結果と、実コンテナ方式の不採用。** (1)(3)は動作したが、(2)は当たり判定の小ささ等により右クリックの成否が不安定だった(`has_collision: true`+当たり判定拡大で改善見込み)。しかしそれ以前に、実用面で3つの重大な問題が判明した:

- **手持ちインベントリが一杯だと「取り出す」という選択操作自体が成立しない**。
- **個数の指定ができない**(「丸石を256個引き出し」のような任意の数量を1クリックで表現する自然な手段が無い)。
- **アイテム以外のUI部品(検索テキスト入力・トグル・スライダー)を配置できない**(バニラのコンテナ画面はアイテムスロットの羅列のみ)。

「取り出しを選択の合図にして、閉じた瞬間に回収する」という代替案(実際に他のBedrockアドオンの仮想ショップ機能で使われている手法)も検討したが、上記3つの問題自体は解決しないため、**実コンテナ方式は引き出しUIの主要な操作方式としては不採用とした**。

### PoC第3段階(不採用): `CustomForm`(DDUI)のレイアウト上書き

バニラの`server_form.json`(Mojang公式`bedrock-samples`で確認)には、`ActionFormData`用の`long_form`とは別に`custom_form`という制御があり、これは`ModalFormData`(トグル・スライダー・ドロップダウン等を持つ、リアクティブでない旧世代のフォームAPI)が使うものだと判明した。「`CustomForm`(DDUI)もここを経由していて、表示コントロールの生成方法(`factory`)はそのままに、並べ方(縦一列→密な配置)だけ上書きできるのでは」という仮説を立て、`long_form`の時と同じ「タイトルの目印文字列で分岐する」手法で検証した。

**結果: 仮説は誤りだった。** `CustomForm`のタイトルに目印文字列(書式コードのみで構成)を仕込んでも上書きは発火せず、さらに目印文字列自体が(`ActionFormData`の時と違って)書式コードとして解釈・除去されずにそのまま画面に表示されてしまった。これは`CustomForm`が`ActionFormData`/`ModalFormData`とは異なるテキスト描画経路を使っていることを示しており、`server_form.custom_form`を経由していないと判断できる。`CustomForm`(DDUI)のレイアウトをJSON UI側から上書きする手段は、少なくとも今回発見できなかった。

### PoC第4段階: `ActionFormData`によるアイコン無し・高密度テキストレイアウト

`DDUI`ではレイアウトの自由度(整列・情報密度)が制限されているという指摘を受け、アイコン表示は諦め(対応表問題を丸ごと回避できる)、代わりに`ActionFormData`+`server_form.long_form`の上書き(チェストの実験で機構自体は実証済み)で、**テキストだけの高密度レイアウト**(2列のボタンリスト)を試すことにした。リアクティブ性(ライブ検索・タブ切り替え・閉じずにカート追加)を失うことは既知のトレードオフとして受け入れる前提。

**PoCの構成**(`wh:test_dense_form`アイテム、`denseFormPoc.ts`、`RP/ui/wh_dense_form.json`):

- `wh_chest_ui.json`のグリッド機構(`type: "grid"`+`collection_name: "form_buttons"`)は流用しつつ、アイコン関連のコントロール(`beacon.item_renderer`・`item_icon`)は一切使わず、ボタンごとに`#form_button_text`をそのままラベル表示するだけのシンプルな構成にした。
- `grid_dimensions: [2, 12]`で24品目を2列×12行に配置。ダミーの在庫数付きで24品目を表示するテスト。
- `server_form.json`の`long_form`を3分岐(バニラ標準/チェストグリッド/高密度リスト)に拡張。目印文字列は`§w§h§d§e§n§s§e§r`。

**確認したいこと**: (1) 2列レイアウトが正しく描画されるか、(2) DDUIの1列リストと比べて実際に情報密度が上がっているか(体感)、(3) クリック検知が引き続き正しく動くか。

**実機確認済み**: 2列表示・クリック検知とも正常に動作した。

### PoC第4段階・第2ラウンド: リアクティブ性を失った状態での操作性の検証

見た目の検証ができたので、次に「リアクティブ性(ライブ検索・閉じずにカート追加)を失った状態で、実際の引き出しフロー全体(検索→一覧→数量指定→カートに追加→…→確定)がどこまで実用的に組めるか」を検証する。`ModalFormData`(テキスト入力・スライダーを持つが、送信するまで反映されない旧世代フォーム)と、アイコン無しグリッド表示(`ActionFormData`)を「閉じて再表示」で繋ぎ合わせてシミュレートする。

- **検索**: `ModalFormData`の`textField`でキーワードを尋ね、送信後に絞り込んだ結果をグリッドで表示する(1文字打つごとの絞り込みは無く、送信して初めて反映される)。
- **一覧**: グリッドの先頭2項目を「検索をやり直す」「引き出しを確定する(カート内容表示)」の特殊項目にし、残りを検索結果にする。
- **数量指定**: 品目をクリックすると`ModalFormData`の`slider`(1〜64、前回入力値をデフォルトに)で数量を尋ね、確定するとカートに加算して一覧に戻る。
- **カート**: サーバー側の一時変数(`Record<string, number>`)で保持し、現在のカート内容を一覧のタイトルにも表示する。

この一連の流れが実際に使い物になるか(操作の手数、体感速度、迷いやすさ)を実機で確認したい。

**実機確認の結果、不採用。** 「検索→一覧→数量指定→カート→確定」の一連の操作を試したところ、率直に「今のDDUIより悪い」という評価だった。検索のたびに`ModalFormData`を開き直す手間、数量指定のたびにフォームが切り替わる断続感が、情報密度の向上分を上回るデメリットになると判断された。

**(実機で発見された不具合の修正済み)** 初回実装では、フォームがバニラ標準の縦一列リスト表示のまま(グリッド化されず)、アイコンも市松模様(テクスチャ欠落)になっていた。原因は、`RP/ui/wh_chest_ui.json`にグリッド表示自体の定義は用意していたものの、**それを実際に呼び出す側の上書きファイル(`RP/ui/server_form.json`)を移植し忘れていた**こと。Chest-UI本体は、バニラの`server_form`名前空間の`long_form`コントロール(=`ActionFormData.show()`で実際に表示される画面本体)自体を、タイトルの目印文字列に応じてバニラの縦一列リストを隠し、代わりに`chest_panel`を表示するよう上書きしている。このファイルを`_ui_defs.json`に登録し忘れると、`wh_chest_ui.json`側の定義は一切参照されないため、常にバニラ標準の見た目(市松模様のアイコンを含む)のまま表示される。`RP/ui/server_form.json`を追加し、`§w§h§c§h§e§s§t`という目印文字列で同様に上書きするよう修正した。

**実機検証の結果: レガシー数値アイテムIDから公式`iconPath`方式へ切り替え。** `server_form.json`の修正後、グリッド表示・アイコン描画・クリック検知はいずれも実機で正しく動作することを確認できた。しかし10品目中1品目(`minecraft:apple`、数値ID`880`)だけ、明らかに別のアイテムのアイコンが表示される不具合が実機で見つかった。他の9品目(数値ID3〜523)はすべて正しく表示されており、`beacon.item_renderer`+レガシー数値ID自体の仕組みは今のゲームバージョンでも機能しているが、**`typeIds.js`(コミュニティによる非公式な対応表)の個々のエントリの正確性は品目によっては信用できない**ことが実証された(特に数値IDが大きい品目で起きやすいと推測される。`forms.js`側に`ID < 850`を境に特別な補正を入れている箇所があり、この境界を超えた領域は元々信頼性が低い可能性がある)。

これを受けて、アイコン描画の方式を、`ActionFormData.button(text, iconPath)`の**公式ドキュメントに明記されたiconPath引数**(実テクスチャパスの文字列)を使う方式に切り替えた。`wh_chest_ui.json`側では、`beacon.item_renderer`(非公式)の代わりに、`#form_button_texture`をそのまま画像として表示するだけの単純な`image`コントロール(`item_icon`)に置き換えた。グリッド化の仕組み(`server_form.json`によるバニラ画面の上書き、目印文字列によるレイアウト切り替え)自体は変更していない。数値IDテーブルという継続的なメンテナンスが必要な非公式アセットを丸ごと不要にできる点も利点。ただし、テクスチャパスの命名規則もアイテム(`textures/items/*`、識別子とファイル名が一致することが多く信頼できる)とブロック(`textures/blocks/*`、`oak_planks`→`planks_oak`のようなレガシー命名が残っている場合がある)で信頼度が異なるため、この点も実機での確認待ち。

### 総括: 引き出しUI大幅改修の調査結論

一連のPoC(第1〜4段階、計4方向)を経て、**Chest-UI風のグリッド化・レイアウト刷新は不採用とし、現行のDDUI(`CustomForm`)ベースの引き出しUIを維持する**という結論に至った。各方向性の不採用理由は以下の通り。

1. **`ActionFormData`グリッド+レガシー数値アイテムID**(`beacon.item_renderer`転用): 事前に用意した対応表に無いアイテム(他アドオンのものを含む)は正しいアイコンを付けられない。対応表自体の正確性も実機で疑わしいことが判明した(`minecraft:apple`が誤表示)。
2. **`ActionFormData`グリッド+公式`iconPath`**(実テクスチャパス): 同上の対応表問題が残る。失敗のしかたは「別アイテムに見える」ではなく「市松模様(テクスチャ欠落)」になるだけ安全だが、他アドオンのアイテムを正しく表示できない点は変わらない。
3. **実在コンテナ(非表示エンティティ+`minecraft:inventory`、`container_type: "minecart_chest"`)**: アイコン描画はエンジンのネイティブ機能に任せられるためどんなアイテムでも正しく表示できるが、(a)手持ちインベントリが一杯だと選択操作自体が成立しない、(b)個数指定の自然な手段が無い、(c)検索テキスト入力・トグル等のアイテム以外のUI部品を置けない、という3つの実用上の問題があり不採用。
4. **`ActionFormData`+アイコン無し高密度テキストレイアウト**(`ModalFormData`と繋ぎ合わせ): 見た目(2列レイアウト)は実現できたが、リアクティブ性(ライブ検索・タブ切り替え・閉じずにカート追加)を失った結果、実機での操作性が現行のDDUIより悪いと判断された。

また、`CustomForm`(DDUI)自体をバニラの`server_form.custom_form`(`ModalFormData`用の画面)経由でレイアウト上書きできないか検証したが、`CustomForm`はこの経路を通っていないことが判明し(目印文字列が書式コードとして解釈されず素の文字として表示された)、この案も不成立だった。

**結論として、情報密度の不足は、グリッド化ではなく「ボタンのツールチップを活用する」ことで補う方針とする**(次項参照)。

### DDUIコンポーネント一覧と実機で判明した制約

`@minecraft/server-ui`のDDUI関連APIを一通り確認・実機検証した(`wh:test_ddui_components`)。

**`CustomForm`のコンポーネント**:

| コンポーネント | 用途 | 主なオプション |
|---|---|---|
| `label(text, options?)` | 読み取り専用テキスト | `visible` |
| `header(text, options?)` | セクション見出し | `visible` |
| `button(label, onClick, options?)` | クリック可能なボタン | `tooltip`, `disabled`, `visible` |
| `divider(options?)` | 横罫線区切り | `visible` |
| `spacer(options?)` | 縦方向の空白 | `visible` |
| `textField(label, text: ObservableString, options?)` | テキスト入力 | `description`, `disabled`, `visible` |
| `slider(label, value: ObservableNumber, min, max, options?)` | 数値スライダー | `step`, `description`, `disabled`, `visible` |
| `toggle(label, toggled: ObservableBoolean, options?)` | ON/OFFスイッチ | `description`, `disabled`, `visible` |
| `dropdown(label, value: ObservableNumber, items: DropdownItemData[], options?)` | ドロップダウン選択 | 各項目に`label`/`value`/`description`、全体に`disabled`/`visible` |
| `closeButton()` | 右上×+下部に閉じるボタンを追加 | ー |

`min`/`max`/`step`等は数値だけでなく`ObservableNumber`も渡せ、動的に変更可能。ラベル系の引数はほぼ全て`ObservableString`/`ObservableUIRawMessage`/`string`/`UIRawMessage`を受け付け、Observableを渡すと値が変わるたびに自動更新される。

**`MessageBox`**(このアドオンでは未使用・今回発見): `CustomForm`とは別枠の、`body`+`button1`+`button2`だけのリアクティブな簡易確認ダイアログ。`show()`は`{ closeReason, selection }`を返す。確認ダイアログ(「本当に削除しますか？」等)に使える可能性がある。

**Observable系**: `ObservableString`/`ObservableNumber`/`ObservableBoolean`/`ObservableUIRawMessage`。いずれも`getData()`/`setData()`/`subscribe(callback)`/`unsubscribe(callback)`を持つ。コンストラクタで`{ clientWritable: true }`を渡すとプレイヤー操作による双方向バインディングになる(このアドオンで既に多用している)。`ObservableString`には`getFilteredText(player)`(不適切語フィルタ済みテキストの非同期取得)もある。

**実機で判明した制約(実機で発見された不具合の修正済み)**:

- **プレイヤーが同時に開けるフォームは1つまで**: `CustomForm`が開いたままの状態で別の`MessageBox`/`CustomForm`を`show()`しようとすると`FormVisibilityError`(「既に表示中のフォームを再度表示しようとした」)になる。`CustomForm`はボタンを押しても自動では閉じない(DDUIの「開いたまま更新される」設計のため)ので、別のフォームに切り替えたい場合は明示的に`form.close()`が必要。
- **`close()`の効果はクライアントに伝わるまで数tick必要**: `form.close()`した直後の同一tick内で次のフォームを`show()`すると、依然として失敗することがあった(`FormVisibilityError`と思われる未処理のPromise拒否で、エラーハンドリングを入れていないと症状が「何も起きない」ようにしか見えず原因究明が難しい)。`system.runTimeout(fn, 1)`で1tick遅らせることで解決した。
- **DDUIのメインテキスト(タイトル・ラベル・ボタン本体)は`§`書式コードを解釈しない**: 書式コードがそのまま文字として表示されてしまう(例: `§a`が「§a」という文字列として見える)。これは`CustomForm`のタイトルに目印文字列を仕込んだ際にも確認済みで、`ActionFormData`/`ModalFormData`とは異なるテキスト描画経路を使っていることを示唆している。`UIRawMessage`型にも色を指定する専用プロパティは無い。
- **ボタンの`tooltip`だけは`§`書式コード(色のみ)が効く**: 長文(実機で約300文字)でも省略されずに表示され自動折り返しされる、複数行(`\n`)も使える、行ごとに違う色も指定できる。ただし太字(`§l`)・斜体(`§o`)は効かない(色コードのみ対応)。

**今後の方針**: 情報密度を補いたい箇所には、メインテキストの書式ではなく**ボタンの`tooltip`**を活用する(在庫数・カテゴリ・補足説明等を色分けして詰め込める)。

## 8. MVPスコープ外（合意済み）

- ターミナル側での部分成功/失敗のハンドリング（`wh:partial`への記録はコントローラ側で行うが、ターミナルはそれを読み出して何かする処理を持たない）
- 「全部揃うか、揃わないなら何も取得しないか」を選べる引き出しタイプ
- グラフィックの作り込み（アイコン付きグリッドUIを含む。7章参照）

## 9. 将来の拡張アイデア（今は着手しない）

- 表示に出ない複雑な属性（本の中身、地図IDなど）でアイテムを区別したくなった場合: ネットワークごとに非表示・非当たり判定の専用エンティティ（見本帳）を持たせ、引き出し時に選択したアイテムの`clone()`を1個保管し、`isStackableWith`や自前の比較関数で照合する方式が候補。
- 「全部揃うか、何も取得しないか」の引き出しタイプ追加。
- コントローラUIの「状況」タブ(4章「引き出し・預け入れ・整理のキャンセル」参照)は、引き出し・預け入れ・整理のいずれにも対応済み。整理のキャンセル単位はリクエスト全体(品目単位のキャンセルは対応していない)。

（Chest-UI方式のグリッドUI化は、4方向のPoCを経て不採用と結論した。7章参照。情報密度はボタンのtooltip活用で補う方針。レンチの汎用設定ツール化(モード切替式)は実装済み。3章参照。）

## 10. 未確定・要検証事項

1. **強調表示パーティクルの寿命**: `minecraft:villager_happy`は発生後1〜2秒かけてフェードアウトするため、切断直後も一瞬残ってしまう(実機で確認・指摘あり)。候補: (a) RPに寿命の短い自作パーティクル効果を定義する(安定版APIのみで完結、リスク低、推奨)。(b) `@minecraft/debug-utilities`の`DebugBox`で`.remove()`により即座に消す(体験は最良だが、プレビュー版限定のバージョン表記のため通常版Minecraftではパック全体が読み込めなくなるリスクがある)。まだ着手していない。

(旧項目「レンチの編集モードの細部(別ネットワークのコントローラを右クリックした時に編集対象を切り替える)」「引き出しUIの具体的なワイヤーフレーム」は、どちらも実装・確定済みのため削除した。前者は3章、後者は7章参照。)

## 11. 技術メモ

- `@minecraft/server` 2.1.0 / `@minecraft/server-ui` 2.1.0、`min_engine_version` 1.21.0 を使用（プロジェクトに既存設定済み）。
- `ItemStack.isStackableWith(other)`: エンジン自身が「タイプ＋耐久値/エンチャント/名前などのカスタムデータ」を比較して判定するが、**最大スタックサイズが1のアイテム（道具・装備など）には常に`false`を返す**仕様のため、5章の`displayKey`方式ではこれに依存しない。
- `world.getDynamicPropertyTotalByteCount()`で動的プロパティの総バイト数を監視できる。明確な上限は非公開だが、個人利用規模では問題にならない見込み。
- ブロックの`minecraft:block.components`に`minecraft:inventory`は指定不可（6章参照、実機検証済み）。
- **(実機で発見された不具合の修正済み) `minecraft:custom_components`配列は廃止され、カスタムコンポーネントは他の標準コンポーネントと同じように`components`直下へ「フラット化」して書く必要がある(Custom Components V2、`@minecraft/server` 2.0.0以降で強制的に有効)**。このプロジェクトは`@minecraft/server` 2.1.0を使っているため、V1時代の`"components": {"minecraft:custom_components": ["wh:controller"]}`という書き方は無効になり、コンテンツログに`minecraft:custom_components component is not valid from 1.21.90 onward.`エラーと`Block custom component 'wh:controller' is not being used by a block`警告が出て、`onPlace`等が一切発火しなくなる(=コントローラを設置してもネットワークが作られない)という不具合が実機で見つかった。正しい修正は、`minecraft:custom_components`配列そのものを削除し、**そのカスタムコンポーネントのidentifierを`components`直下に直接キーとして追加する**(値は基本的に空オブジェクト`{}`。パラメータを渡したい場合はそこにJSONを書ける)という「フラット化」。例: `"components": {"minecraft:geometry": ..., "wh:controller": {}}`。原因の特定に手間取った経緯: 最初は単純な配置ミス(`components`内→`description`直下→`minecraft:block`直下)を疑い2回試したがどちらも警告が消えず、Web検索で公式ドキュメント(Scripting v2.0.0 Overview「Custom Components V2」の「Flattening」節)を確認して初めて「配置場所ではなく書き方(配列→フラット化)自体が変わった」ことが分かった。ブロック側(`BP/blocks/*.json`)・アイテム側(`BP/items/*.json`、速度/周期/範囲キット・レンチ・テスト用アイテム)の両方を同じ書き方に統一した(ドキュメント上はアイテムも同じ変更の対象だが、コンテンツログではブロック側のみ`[Json][error]`が出ていた)。**さらに、アイテム側の修正だけでは解消しなかった(`Item custom component 'wh:wrench' is not being used by an item`という警告が別途出た)**: `BP/items/*.json`は`format_version`が`"1.21.0"`(一部`"1.21.40"`)のままで、フラット化した書き方がその古いformat_versionでは認識されていなかった。全アイテムJSONの`format_version`をブロック側と同じ`"1.21.100"`に上げて解消した。**教訓**: `minecraft:custom_components`のフラット化は「書き方の変更」であると同時に、対象ファイルの`format_version`が新しい書き方に対応した値になっていることが前提になる。
- **アイテムの説明文(ツールチップの名前下に出るグレーの行)は`ItemStack.setLore()`で動的に付与する**(`itemDescriptions.ts`)。この結論に至るまで2つの不採用案があった。
  1. **(不採用) `ItemStack.setLore()`で所持品変化を監視して動的付与**: 最初の実装。`world.afterEvents.playerInventoryItemChange`(`includeItems`で対象アイテムのtypeIdを絞り込み、`ignoreQuantityChange: true`で個数変化だけの発火を除外)を監視し、対象アイテムでまだLoreが空なら1回だけ`setLore`する方式だったが、**アイテムがプレイヤーの実際の所持品になるまで説明が付かない**(クリエイティブインベントリの一覧パネルで見ている段階では表示されない)という指摘を受けて不採用にした。
  2. **(不採用) アイテム名のlangキーの値自体に`\n`と`§`書式コードを直接埋め込む、完全に静的な方式**: 参照した他アドオン(UtilityCraft)の実装調査で判明した手法(`item.wh:wrench=倉庫レンチ\n§7Shift+右クリックで使用\n\n...`)。`minecraft:display_name`が指すlangキーの値がそのままツールチップとして複数行表示されるためスクリプト不要でクリエイティブ一覧でも即座に表示され、1のLore方式の弱点を解消したかに見えたが、**この値はホットバーでアイテムを切り替えた時に画面上部へ一瞬表示されるポップアップにもそのまま使われる**ため、説明文全体(署名行まで含む)がホットバー切り替えのたびに大きく表示されてしまう不具合が実機で見つかった。「クリエイティブ一覧で見えない」より「毎回のホットバー切り替えで説明文が丸ごと出る」方が実害が大きいと判断し、Lore方式に戻すことにした。
  - **最終的に採用したLore方式は、1と同じ`world.afterEvents.playerInventoryItemChange`監視だが、対象アイテムを速度/周期キットの8種に加えコントローラ・ターミナル系・レンチにも広げてある**。名前(`item.wh:wrench`等)は短いまま変更せず、説明文だけをLoreとして分離して持たせることで、ホットバー切り替えのポップアップには名前だけが表示され、インベントリでのホバー時にのみLoreを含めた詳細が表示される(名前とLoreの表示箇所がBedrock側で明確に分かれているため)。「アイテムがプレイヤーの所持品になるまで説明が付かない」という1の弱点は残るが、個人利用規模の実害としては軽微と判断した。
  - **数値を含む説明文(速度強化キット・周期短縮キットのスループット/処理間隔)は静的な配列ではなく`() => RawMessage[]`という組み立て関数にした**(`ITEM_DESCRIPTIONS`の値は`RawMessage[] | (() => RawMessage[])`)。`getOrderThroughput(tier)`等、実際のテーブル参照関数をLore生成のたびに呼ぶことで、将来スループットのテーブル値を調整してもコード1箇所を直すだけで新規に付与される説明文に反映される(2の静的方式では実現できなかった、ユーザーからの要望)。ただし4章の`giveOrDropKits`等と同じく、**既にLoreが付与済みのスタックは遡って更新されない**。
  - **`RawMessage.with`は`RawMessage[]`ではなく`string[] | RawMessage`(型定義で確認済み)**で、翻訳先の文字列にネストしたRawMessageを渡すには`{ rawtext: [...] }`で包む必要があり、プレースホルダー(`%1$s`等)の構文にも依存する。今回はそれよりも、`RawMessage.rawtext`で書式コードの`{ text: "§9§o" }`とアドオン名の`{ translate: "wh.addon_name" }`を直接連結する方式にした(`ADDON_SIGNATURE_LINE`)。この行専用のlangキーは不要になり、アドオン名を改名したくなった時も`wh.addon_name`という共通の1キーを直すだけで、これを使う全アイテムの説明文に反映される(ユーザーからの要望: 「アドオンの名前を変える可能性が高いので、説明文中に直書きしない」)。
  - ターミナル系(基本/自動/在庫管理)は「概要1行(種別ごとに違う)+使い方1行(共通)」という同じ構造なので、`terminalFamilyLore(summaryKey)`という共通の組み立て関数に概要のlangキーだけを渡して使い回している。
  - 範囲キットの`buildRangeKitLore(tier)`も速度/周期キットと同じ組み立て関数方式(`getRangeForTier(tier)`を都度参照)。ただし`item.wh:range_kit.desc.summary`/`item.wh:range_kit.desc.range`の日本語本文は暫定の仮テキストで、他アイテムと同じく最終的な文面はユーザーが用意する運用のため、確定次第`en_US.lang`/`ja_JP.lang`を差し替える。

## 12. リリース手順(dev/prodパックの分離)

これまで`BP`/`RP`は1組のUUIDだけを持つ単一のパックとして開発していたが、リリース準備として、日常の開発・検証に使う**dev**パックと、実際に使う本番ワールド用の**prod**パックを、**同一のMinecraft環境に共存させられる**ようにした(同一ワールドで同時に有効化することは想定していないため、ブロック/アイテムの名前空間`wh:`はdev/prodで分けていない。UUIDだけを分ければ、Minecraft側は別パックとして正しく扱える)。

- **devパック(`BP`/`RP`)のUUIDは一切変更していない**。既に検証用ワールドに紐付いているUUIDを変えると、そのワールド側で「パックが見つからない」扱いになってしまうため。`header.name`にだけ「(Dev)」を追記し、パック一覧でprodと見分けられるようにした。`mise run install`(既存、変更なし)で今まで通り`development_*_packs`にシンボリックリンクする。
- **prodパックは、devの内容を丸ごとコピーし、`manifest.json`の識別情報(`header.uuid`/`modules[].uuid`/相互の`dependencies[].uuid`/`header.name`)だけ差し替えたビルド生成物**(`tools/build-release.mjs`)。UUIDは`tools/releaseConfig.mjs`に固定値として埋め込んであり、以後変更しない(理由はdevと同じ: 変えると既存のprodワールドとの紐付けが切れるため)。`module`の対応付けは配列の並び順ではなく`type`(`"data"`/`"script"`/`"resources"`)で照合しているため、将来`modules`の順序が変わっても壊れない。
- **`.mcaddon`として生成し、インストールもその`.mcaddon`経由で行う**(`mise run release`→`tools/build-release.mjs`が`dist/StorageNetworkBP.mcpack`・`dist/StorageNetworkRP.mcpack`を作り、その2つをまとめて`dist/StorageNetwork.mcaddon`にする。`mise run install:prod`→`tools/install-prod.mjs`がその`.mcaddon`をOS既定の関連付け(Minecraft)で開く、Windowsの`start`コマンド経由)。dev用の`tools/install.mjs`(シンボリックリンク配置)とは仕組みが根本的に異なるため別スクリプトに分離した。generate-and-symlinkではなくMinecraft本来のインポート経路を実際に通すことで、「配布物として実際にインポートできるか」を自分自身で検証できる。将来他人に`.mcaddon`を配布する可能性を見据えて、この生成物がそのまま配布物になる構成にしてある。
- **zipの生成は外部ライブラリを追加せず自前実装した**(`tools/zip.mjs`)。`package.json`にzip関連の依存が無く、PNG生成(`tools/generate-placeholder-icon.mjs`)も同様にzlib+手組みのチャンク書き込みで自前実装している前例があったため、同じ方針を踏襲した。CRC32のアルゴリズムはPNG生成スクリプトと共通(標準的なpoly `0xEDB88320`のテーブル方式)。圧縮はせず**ストア方式(無圧縮)**にすることで、Deflateエンコーダの実装コストを避けている(Minecraftのインポートは無圧縮でも問題なく読み込める)。`.NET`の`System.IO.Compression.ZipFile`で読み戻して構造とバイト内容が一致することを確認済み。

### CD(GitHub Actions、`.github/workflows/release.yml`)

mainブランチへのpushのたびに、自動でビルド・バージョン採番・GitHub Releaseの作成(`.mcaddon`をアセットとして添付)まで行う。

- **バージョン採番: patchを+1し、リポジトリへコミットして残す**。Minecraftはパックの更新を`header.version`の差分で判定するため、バージョン番号を変えないまま`.mcaddon`を作り直しても、既にインポート済みの環境では「更新」と認識されない可能性がある。そのため`tools/bump-version.mjs`(新規)が`BP/manifest.json`・`RP/manifest.json`の`header.version`・`modules[].version`・お互いを指す`dependencies[].version`(4種類の`"version": [a, b, c]`パターン、全ファイルで常に同じ値に揃える既存の手動運用を踏襲)のpatchだけ一括で+1し、CIがそのまま`git commit`(コミットメッセージ末尾に`[skip ci]`)して`push`する。
  - **`JSON.parse`→`JSON.stringify`で書き戻さなかった理由**: `manifest.json`は人力整形されたファイルで、バージョン配列は`[0, 3, 0]`のように1行にまとめて書かれている。素朴に`JSON.stringify(obj, null, 2)`で書き戻すと配列が複数行に展開され、変更していない`min_engine_version`等まで巻き込んで無関係な差分が大量に出てしまう。そのため対象の`"version": [a, b, c]`パターンだけを正規表現でその場置換する方式にした(`"min_engine_version"`は文字列として`"version":`という部分文字列を含まない=キー名の直前に`"`が来ないため、誤って一致しない)。
- **バージョン採番によるCIの無限ループを心配したが、対策は不要と判明**: GitHub Actionsは「デフォルトの`GITHUB_TOKEN`によるpushは、たとえ`on: push`のワークフローが設定されていても新しい実行を作らない」という再帰防止の仕組みを標準で持っている(公式ドキュメントで確認済み)。そのため、CIが`bump-version`のコミットをpushしてもワークフローが再度自身を起動することは無い。念のためコミットメッセージに`[skip ci]`も付けているが、これは保険であり必須ではない。
- **リリース方式は「コミットごとに新規作成」を採用**(ユーザーの選択、ローリング1本の`latest`更新ではなく)。バージョンが必ず単調増加するため、タグ名`v{version}`(例: `v0.3.1`)が衝突することはない。アセット添付には`softprops/action-gh-release`を使い、`tools/build-release.mjs`が生成する`dist/StorageNetwork.mcaddon`のみを公開する(`.mcpack`単体2つは中間生成物として添付しない)。
- **CIでも`npm run typecheck`を通してからビルド・コミットする**(ローカルの開発フローと同じゲート)。型エラーがあるコミットではバージョンが採番されず、壊れたリリースが公開されない。
- **`permissions: contents: write`が必須**(バージョン採番コミットのpush・リリース作成の両方に書き込み権限が要る)。連続pushでpatch採番が競合しないよう`concurrency`で同一グループの実行を直列化している。

## 13. ネットワークオブザーバー(`wh:network_observer`)

倉庫ネットワークの在庫状況をレッドストーン回路から読み取れるようにする、新しいブロック。フルブロックで1面だけに信号を出す(バニラのオブザーバーと同じ操作感)。

- **在庫モード**: 1品目+最大値を設定。信号強度はバニラの**コンパレーターのコンテナ充填率読み取りと同じ式**を使う(ユーザー承認済み): `在庫==0→0`、`在庫>=最大値→15`、それ以外は`floor((在庫/最大値)*14)+1`(`networkObserverProcessing.ts`の`stockSignal`)。
- **比較モード**: 2品目を設定。`品目1<品目2→0`、`==→7`、`品目1>品目2→15`(`compareSignal`)。

### 可変強度のレッドストーン出力: `minecraft:redstone_producer`

`power`(0〜15)が固定値のコンポーネントなので、動的な信号強度はこのアドオンで既に使っているカスタムブロックステート+`permutations`の手法(コントローラの`wh:speed_tier`等と同じ)で実現している。`wh:signal_strength`(0〜15の16値)ステートを持たせ、Tierごとの`permutations`で対応する`power`を設定する(`BP/blocks/network_observer.json`)。`format_version`は1.21.120以上が必要なため、`BP`/`RP`両方の`manifest.json`の`min_engine_version`を`[1, 21, 120]`に上げた。

### 設置方向: バニラのオブザーバーと同じ操作感にする

クリックした面基準の`minecraft:placement_position`(ターミナル系で使用)ではなく、**`minecraft:placement_direction`トレイト**(`enabled_states: ["minecraft:facing_direction"]`)を使う。これはプレイヤーの向いている方向(上下含む6方向)に基づいて設置され、バニラのオブザーバー/ディスペンサー/ピストン等と同じ挙動になる(`minecraft:cardinal_direction`は4方向・水平のみのため今回の用途には合わない)。

信号出力はバニラのオブザーバーと同じく、設置時に正面が向いた方向の**逆(背面)**から出す。`wh:signal_strength`用の16 permutationsとは**別に**、`facing_direction`ごとの`minecraft:transformation.rotation`を設定する6 permutationsを用意し、`redstone_producer`側は常にローカル座標系で`strongly_powered_face: "south"`・`transform_relative: true`を指定しておくことで、実際の出力方向が回転に応じて自動的に読み替えられるようにしている(異なる状態次元の`permutations`は独立に評価され、コンポーネントごとにマージされる。既存の`terminal.json`が「他のコンポーネントを再宣言せず`minecraft:transformation`だけ書く」形で成立していることが、この重ね合わせの前提を裏付けている)。

回転値は当初Minecraftの標準的なyaw規則(yaw0=south、yaw90=west、yaw180=north、yaw270=east)から類推して導出したが、**実機ではnorth/southは合っていたもののeast/west(Y軸回転)が逆になっていた**(180°回転は符号を反転しても結果が同じため、north/southだけでは食い違いが表面化しなかった)。east/west用の回転を符号反転して解消した。この時、同じ理屈がX軸回転(up/down)にも及ぶだろうと考えup/downも一緒に符号反転したが、**これは誤りだった**: 実機ではup/downは最初の値(符号反転前)が正しく、Y軸回転の符号違いがそのままX軸に当てはまるわけではなかった。up/downは元の値に戻して解消した。最終的な値:

| `facing_direction` | 背面(出力方向) | `rotation` |
|---|---|---|
| north | south | `[0, 0, 0]` |
| south | north | `[0, 180, 0]` |
| east | west | `[0, -90, 0]` |
| west | east | `[0, 90, 0]` |
| up | down | `[90, 0, 0]` |
| down | up | `[-90, 0, 0]` |

**実機で動作確認済み**: 「独立した2つの状態次元の`permutations`が両方同時に適用される」という前提、`transform_relative`の意味論は実機で確認できた。回転値はY軸(east/west)のみ符号反転が必要で、X軸(up/down)は当初の値のままで正しかった(**Y軸とX軸で回転の正負の規則が異なる**ことが実機で判明した。今後同様のブロックを作る際は、軸ごとに個別に実機確認する必要がある)。

**出力面の見た目を区別する**(実機での指摘を受けて追加、3回試行して現在の方式に落ち着いた): 6面とも同じテクスチャだと、実際に信号を確認するまでどちらが出力面か分からず紛らわしいという指摘があった。
- **1回目の実装(不採用)**: `minecraft:block`直下のベース`components`にローカル`"south"`面だけ専用テクスチャを割り当て、「`material_instances`も`redstone_producer`の`transform_relative`と同じく`minecraft:transformation`の回転に追従するはず」という前提で実装した。実機ではこの前提が誤りだったばかりか、レッドストーン信号自体も出なくなってしまった(ベースの`components`に`material_instances`を追加したことが`redstone_producer`側の解決にも影響したと見られるが、因果関係は未特定)。
- **2回目の実装(不採用)**: ベースの`material_instances`は`"*"`のみに戻し、代わりに`facing_direction`ごとの6つの`permutations`(`minecraft:transformation`と同じ場所)に、その向きで実際に出力面となる**ワールド座標系の面名**を明示的に指定した`material_instances`を追加した。信号は復活したが、**実機ではどの向きに設置しても常に同じ面(最初の`north`用permutationの面)にテクスチャが付いてしまった**。`minecraft:material_instances`は(`minecraft:transformation`と違い)`permutations`をまたいだ状態ごとの切り替えにそもそも対応していないと見られる。
- **3回目の実装(採用)**: `minecraft:material_instances`によるテクスチャの出し分けを諦め、**terminal.jsonで既に実機確認済みのBox UV方式**(1枚のテクスチャに6面分を焼き込み、ジオメトリ全体を`minecraft:transformation`で回転させる)に切り替えた。専用のカスタムジオメトリ`geometry.network_observer`(`RP/models/blocks/network_observer.geo.json`、16×16×16の単純な立方体、`uv: [0, 0]`によるバニラ標準のBox UV展開)を新設し、`minecraft:geometry.full_block`から差し替えた。テクスチャ生成(`tools/generate-placeholder-icon.mjs`)は`makeBoxUvColorAt`(既存の`terminalFamilyColorAt`と共通の仕組み)を使い、ローカル`south`面(`redstone_producer`の`strongly_powered_face`と同じ面)だけに同心円(目)のモチーフを乗せ、他の5面は無地の金属ケースにする。ジオメトリ自体の回転は`minecraft:transformation`で(実機確認済みの)`redstone_producer`と同じ仕組みを使うため、信号の出る面とテクスチャの目印が一致する。

### 即時/定期の再計算トリガ

- **コントローラによる引き出し/預け入れ(=ネットワークの在庫が実際に増減する瞬間)は直ちに再計算する**。`orderProcessing.ts`の`processNetworkOrders`・`depositProcessing.ts`の`processNetworkDeposits`の戻り値を`void`から`boolean`(そのtickで1個以上のアイテムが実際に配送/搬入されたか)に変え、`networkProcessing.ts`の`startNetworkProcessingLoop`がどちらかtrueなら`recalculateNetworkObservers`(新規`networkObserverProcessing.ts`)を直ちに呼ぶ。
- **プレイヤーによる手動でのストレージ出し入れは、5サイクルごとの定期再計算で反映される**。「サイクル」はこのネットワークの処理サイクル(周期アップグレード軸のTierに応じて可変)単位で数える(tick単位ではない)。ネットワークIDごとに「前回再計算からの経過サイクル数」を`Map`(`lastProcessedTick`と同じ発想)で数え、5に達したら再計算する(直ちに再計算した場合はこのカウンタもリセットする)。
- `recalculateNetworkObservers`は、対象ネットワークの`scanCatalog`を1回だけ呼び、最大3基のオブザーバー全てに使い回す(`depositProcessing.ts`の`buildStorageIndex`と同じ、ネットワークにつき1回のスキャンで済ませる考え方)。

### 接続・データモデル

`network.ts`にストレージ/ターミナルと同じ形で`observers: Vector3[]`・`toggleObserver`/`removeObserver`を追加し、`wrench.ts`の`handleBuildModeUse`に3つ目の分岐として組み込んだ(範囲チェックは他の2種と共通、新規接続時のみ`network.observers.length >= 3`なら「最大3基まで」のメッセージで拒否する)。**最大接続数はMVPでは固定値**で、将来コントローラのアップグレード軸(4章「グレード管理」と同じ枠組み)にする構想はあるが今回は実装していない。範囲アップグレード軸のTierダウン時の自動切断(`controllerAxes.ts`の`pruneRangeAxisMembers`)にもオブザーバーを含めた。

- **(実機で発見された不具合の修正済み・15章の配達ターミナル追加時に発覚) オブザーバー機能追加前に作られたネットワークで`network.observers`が`undefined`になる**: `observers`フィールドは`createNetwork`が新規作成するネットワークにしか入らないため、この機能を追加する前から存在していたネットワーク(永続化データ)には`observers`キー自体が無い。`recalculateNetworkObservers`の`network.observers.length`で`TypeError`が発生するだけでなく、**全ネットワークを走査する`findMembership`(`network.ts`)が`network.observers.some(...)`で例外を投げるため、レンチによるあらゆる接続/切断操作(このネットワークに限らず、例外が起きた時点で走査自体が打ち切られるため実質全操作)が無反応になる**という分かりにくい不具合になっていた(配達ターミナルを接続しようとして「反応がない」という形で顕在化した)。`getNetwork`(`network.ts`、ネットワークデータを読み込む唯一の入口)で読み込み時に`observers`が無ければ`[]`へ正規化するよう修正し、以後の`setNetwork`で自然に補完されるようにした。新しいフィールドをデータ構造に追加する際は、既存の永続化データに対する後方互換性(読み込み時の正規化)を必ず考慮する必要がある、という教訓。

設定(モード・対象品目・最大値)は`storageSettings.ts`と全く同じ発想の非表示エンティティ`wh:observer_settings`(新規`observerSettings.ts`)に持たせる。UI(`observerUi.ts`)は素手での右クリックで開くCustomFormで、モード切替トグル・品目1/品目2の検索(在庫モードでは品目1のみ使う)・最大値を持つ。ターミナル系と違い引き出し/預け入れを行わないため「状況」タブは無い。

- **(実機で発見された不具合の修正済み) 設定が保存されない**: `observerSettings.ts`はストレージ/ターミナルの設定エンティティと同じ`ensureSettingsEntity`パターンで`dimension.spawnEntity("wh:observer_settings", ...)`を呼んでいたが、**このエンティティ自体をBP/RP双方に定義するのを忘れていた**(`storage_settings`/`terminal_settings`と違い`BP/entities/observer_settings.json`等が存在しなかった)。未定義のエンティティタイプへの`spawnEntity`は失敗するため、`setObserverSettings`が静かに失敗し続けていた。UI側はラベル(`item1Label.setData()`等)を`persist()`より先に更新していたため、見た目には設定が反映されたように見えても実際には保存されていない、という分かりにくい不具合になっていた。`BP/entities/observer_settings.json`・`RP/entity/observer_settings.json`・`RP/models/entity/observer_settings.geo.json`(空のbones、完全に不可視)・`RP/render_controllers/observer_settings.rc.json`を、`storage_settings`と全く同じ内容で追加して解消した。

- **モードの説明ラベルは選択中のモードの分だけ表示する**(実機での指摘を受けて修正): 当初は在庫モード/比較モードの説明を常に両方表示していたが、選択していない方の説明が紛らわしいという指摘を受け、`isStockMode`(在庫モードの説明)・`showItem2`(`!isStockMode`、比較モードの説明と品目2欄で共用)で出し分けるようにした。
- **品目1/品目2の切り替えはトグルではなくドロップダウン**にした(`form.dropdown("設定先", targetSlot, [{label:"品目1",value:0},{label:"品目2",value:1}])`)。検索結果をタップした時、`targetSlot`の値(0/1)でどちらに設定するかを決める。
- **最大値はスライダーではなくテキスト入力**にした(`form.textField`+`ObservableString`、数値としてパースして保存)。スライダーでは正確な値や数万単位の大きな値を指定しづらいという指摘を受けた(精密ターミナルのスロット番号入力と同じ理由・同じパターン)。不正な入力(数値としてパースできない、1未満)の場合は保存せず直前の値を維持する。
- **(実機で発見された不具合の修正済み) 切断後も信号が残る**: レッドストーン出力がある状態でオブザーバーをネットワークから切断(手動切断・範囲アップグレードのTierダウンによる自動切断・**コントローラ自体の破壊**のいずれも)すると、切断後は`recalculateNetworkObservers`の対象(`network.observers`)から外れるため、二度と再計算されず**切断時点の出力値がそのまま残ってしまう**不具合があった。`networkObserverProcessing.ts`に`resetObserverSignal(dimension, loc)`(該当ブロックの`wh:signal_strength`を0に戻すだけの関数)を追加し、`wrench.ts`の手動切断(`toggleObserver`が`"disconnected"`を返した時)・`controllerAxes.ts`の`pruneRangeAxisMembers`(範囲外による自動切断)・**`controllerBlock.ts`の`onPlayerBreak`(コントローラ破壊による`destroyNetwork`の直前、`network.observers`全件)**の3箇所から呼ぶようにした。`destroyNetwork`はネットワークのデータを削除するだけでメンバーのブロック自体には触れないため、オブザーバー(実体としてワールドに残り続けるブロック)は明示的にリセットしないと直らない、という点を見落としていた。ブロック破壊(オブザーバー自身の`onPlayerBreak`)の場合はイベント発火時点で既にブロック自体が無くなっているため、この処理は不要(`resetObserverSignal`はブロックが存在しなければ何もしない)。

### クラフトレシピ

ユーザー指定、自動端末と同じ`[" A ", "BCB", " D "]`型: 上=`wh:terminal`(ネットワーク接続の意味)、左右=`minecraft:comparator`×2(比較機能の比喩)、中央=`minecraft:redstone_block`(駆動源)、下=`minecraft:observer`(観測機能の比喩)。

## 14. 自動処理のレッドストーン制御

自動端末・在庫管理ターミナル・精密ターミナルの3種は、定期チェックループにより人間の操作なしで引き出し/預け入れ注文を発行し続ける。これをレッドストーン信号でオン/オフしたいという要望があり、以下の設計にした。

### (不採用) 当初案: 張り付いた先のブロックで受信

ターミナル系のブロックはフルブロック形状ではない(`minecraft:placement_position`で任意の面に張り付く板状の見た目)ため、隣接ブロックからの通電をターミナル自身が受ける形は直感的でないという指摘があった。そこで当初は、**ターミナル自身ではなく、張り付いた先のブロック(搬入出先の実コンテナ、`getAttachedStorageLocation`で既に求めている座標)で信号を受ける**方式にした(バニラの「壁掛けのレッドストーントーチは、張り付いている裏のブロックが通電すると消える」という挙動との類推)。

**(実機で発見された不具合により不採用)**: `Block.getRedstonePower(): number | undefined`は「ブロックにレッドストーン電力の概念が適用されるか」自体がブロックの種類に依存するAPIで、チェスト等の**普通のコンテナブロックでは値が定義されない**(常に`undefined`)ことが実機検証で判明した。値が定義されるのは、バニラの配線対応ブロック(レッドストーンダスト・レバー・ホッパー等、内部的に自分の通電状態を保持しているブロック)か、`minecraft:redstone_consumer`コンポーネントを明示的に持つカスタムブロックだけで、チェストのような「redstoneの影響を受けない」ブロックはそもそも通電状態を保持していない。結果として3種すべてで「レッドストーン入力しても自動引き出しが止まらない」不具合になっていた。

### (採用) ターミナル自身に`minecraft:redstone_consumer`を持たせて自身の位置で受信

上記の制約により、「張り付いた先」という間接的な受信方式は技術的に成立しない。そこで、`isTerminalLikeBlock`のうち自動端末/在庫管理ターミナル/精密ターミナルの3ブロック定義(`BP/blocks/auto_terminal.json`等)に`"minecraft:redstone_consumer": { "min_power": 0 }`を追加し、**ターミナル自身の位置**で`getRedstonePower()`が値を持つようにした。このコンポーネントは`format_version`/`min_engine_version`が1.26.0以上必要(実機のMinecraftバージョン1.26.4403.0で対応済みのため両ファイルとも1.26.0へ引き上げた)。

- ターミナルは`minecraft:placement_position`で設置面をクリックした側の空間(=支持ブロックとは別の、独立したブロック座標)を占有しているため、レバーや配線をそのブロック座標に直接接触させれば、見た目が薄い板であっても通常の壁掛けレバー/ボタンと同じ感覚で通電できる。
- `terminalBlock.ts`の共通ヘルパー:
  ```ts
  export function isRedstoneLocked(block: Block): boolean {
    return (block.getRedstonePower() ?? 0) > 0;
  }
  ```
- **誤爆リスクの再検討**: 当初懸念していた「搬入出先の実コンテナが別回路で偶然通電される」リスクは、受信位置をターミナル自身に変えたことで解消された(コンテナ自体は今回の判定に関与しない)。設定トグルを設けず常時有効とする方針は変更なし。

### 定期チェックへの組み込み

自動端末(`autoOrderCheck.ts`)・在庫管理ターミナル(`inventoryCheck.ts`)・精密ターミナル(`precisionTerminalCheck.ts`)の3つの定期チェックループそれぞれで、そのターミナルを処理する直前に`isRedstoneLocked(block)`を判定し、trueならそのターミナルの処理を丸ごとスキップ(`continue`)する。既存のキュー・スループット予算・FIFO・完了通知の仕組みには一切手を入れていない。

- **止まるのは新規発行のみ**: このチェックは「新しく引き出し/預け入れラインを発行するかどうか」の判断だけをスキップする。通電前に既に発行済み(キュー投入済み)のラインは、コントローラ側のtick処理(4章)でそのまま配送/搬入が継続される。
- **対象は3種のみ**: 通常ターミナル(`wh:terminal`)は定期チェックによる自動発行そのものが無い(プレイヤーの手動操作のみ)ため対象外。手動UIをレッドストーンでロックする機能も検討したが、要望が無く現時点では計画も無い。ネットワークオブザーバー(13章)は信号を**出す**側であり、そもそも「自動処理を止める」対象になる自動発行の仕組みを持たないため対象外。

## 15. 配達ターミナル(`wh:delivery_terminal`)

通常のターミナルと全く同じ引き出しUI(`terminalUi.ts`の`showOrderUi`をそのまま使う)を持ちながら、引き出したアイテムを**注文したプレイヤーへ優先的に直接**届けるターミナル。ID接頭辞は`DLV-`。

### 配送先の決定: 位置判定はしない

当初は「配送処理が実行される時点で、注文したプレイヤーがターミナルの正面に立っているか」を毎回判定し、立っていればプレイヤーのインベントリへ、いなければ従来通り張り付いた先のストレージへ届ける案を検討していた。この案は`getAttachedStorageLocation`(搬入先=クリックした面と逆側)と対称な「前面座標」(クリックした面と同じ側)を計算し、実機での可視化(判定結果をアクションバーで常時表示)も含めて設計まで進めていたが、**ユーザーの指摘により方針転換した**: 「ターミナルは右クリックしないと注文できない=注文が成立した時点で、そのプレイヤーは必ず正面にいたことが保証されている」ため、**配送処理の時点で改めて位置を判定する必要が無い**。この転換により、位置判定ロジック(`getFrontLocation`/`isPlayerInFront`)自体が不要になり、それを可視化するためのアクションバー表示の仕組みも合わせて不要になった(可視化すべき「判定」そのものが無くなったため)。

- **搬入先の優先順位**(`orderProcessing.ts`の`processNetworkOrders`): 注文したプレイヤー(`order.playerName`)がオンラインなら、位置に関わらず常にそのプレイヤーの`minecraft:inventory`コンテナを`destContainer`として使う。オフラインなら、従来通り張り付いた先のストレージのコンテナを使う(=通常のターミナルと同じ基本動作)。プレイヤーがオンラインの場合、張り付いた先にストレージが無くても(存在しなくても)配送できる。
- **既存資産の再利用**: `storageScan.ts`の`extractFromStorages(dimension, network, key, amount, destContainer)`は`destContainer`を汎用の`Container`型として扱う実装になっており、ブロックのコンテナと全く同じコードでプレイヤーのインベントリコンテナ(`player.getComponent("inventory")?.container`、`EntityInventoryComponent.container`もブロックと同じ`Container`型)をそのまま渡せた。新しい抽出ロジックは一切書いていない。
- **満杯時の挙動は既存のまま**: プレイヤーのインベントリが満杯で入りきらない場合も、`extractFromStorages`が「1個も入らなければ打ち切り」という既存の挙動をそのまま返すため、既存の「不足」記録(`wh:partial`)にそのまま乗る(ユーザー確認済み、アタッチ先満杯時と同じ扱いでよい)。
- **スループット予算・FIFO・完了通知・キャンセルは無改造**: 搬入先を決める1箇所だけの分岐追加で済んだため、既存のキュー処理には一切手を入れていない。

### ブロック定義・レシピ

他のターミナル系と全く同じ構造(`geometry.terminal_panel_north`、`minecraft:placement_position`、6方向permutations)。画面色を緑系(`tools/generate-placeholder-icon.mjs`の`deliveryTerminalColorAt`)にして他の4種と見分けている。`isTerminalLikeBlock`(`terminalBlock.ts`)にも追加済みで、レンチでの接続対象・引き出し処理側の生存確認のどちらも他の4種と同じ扱いになる。

クラフトレシピ(`BP/recipes/delivery_terminal.json`)は当初、自動端末/在庫管理ターミナルと同じ「+」型`[" A ", "BCB", " D "]`の積み上げ式(中央=`wh:terminal`)にしていた。自動端末/在庫管理ターミナルの左右=リピーター・下=時計という「時間・周期」の象徴は、配達ターミナルには当てはまらない(独自の定期チェックループを持たず、既存の引き出し処理に相乗りする仕組みのため)ため、代わりに「特定の相手を識別して、どこにいても届ける」というテーマの素材にしていた(上=`minecraft:name_tag`、左右=`minecraft:ender_pearl`×2、下=`minecraft:redstone`)。20章のレシピ見直しで、中央の消費先を`wh:terminal`から`wh:auto_terminal`に変え、`minecraft:name_tag`(上)+`wh:auto_terminal`(中央)の2種のみへ大幅に簡略化した(`pattern: [" A ", " B ", "   "]`)。21章のレシピ見直しで、中央の消費先を`wh:auto_terminal`から`wh:terminal`へ再び差し替えた上で、下段に`minecraft:leather`を追加した(`pattern: [" A ", " B ", " C "]`。`unlock`も`wh:auto_terminal`から`wh:terminal`へ戻している)。

### 進捗のアクションバー表示と「1プレイヤー同時1件まで」の制約

配達が進行中か完了したかが分かりにくい(完了メッセージはチャットに流れるだけで見落としやすい)という指摘を受け、配達中はアクションバーに進捗を表示するようにした。

- **1プレイヤーにつき同時に1件までという制約を追加した**: アクションバーは1行しか表示できないため、同じプレイヤー宛の配達が複数同時進行すると、どちらの進捗を表示すべきか一意に決められない。この制約は**配達ターミナル発の注文にのみ**適用され、通常ターミナル・自動端末等の出し入れには一切影響しない(ユーザー確認済み)。制約は**プレイヤー単位でネットワーク横断**(複数の配達ターミナル・複数ネットワークにまたがっても、そのプレイヤー宛の配達は合計1件まで)で判定する。
- **判定方法**(`orderProcessing.ts`の`hasActiveDeliveryOrderFor`): `getAllNetworks()`で全ネットワークを走査し、各ネットワークの発行待ち(`getIssuing`)・処理中(`getOrders`)の注文の中に、指定プレイヤー宛かつ搬入先が配達ターミナルのものが無いかを調べる。「注文がissuing/ordersに存在する」こと自体が「まだ`finalizeOrder`されていない=未完了」を意味するため、追加の状態管理は不要だった。
- **UI側の拒否**(`terminalUi.ts`): 引き出しタブの確定ボタン(`setupTab`)の`onConfirm`コールバックを、戻り値で送信成功/拒否を表現する形(`(lines) => boolean`)に変更した。配達ターミナルで`hasActiveDeliveryOrderFor`がtrueの場合は送信せずエラーメッセージを返し、確定ボタンのハンドラは戻り値がfalseの時はカートを空にせずフォームも閉じない(プレイヤーが少し待って再度「確定」を押せるようにするため)。
- **進捗表示**(新規`deliveryProgress.ts`の`startDeliveryProgressLoop`): 全ネットワークの発行待ち+処理中の注文のうち搬入先が配達ターミナルのものを抽出し、該当プレイヤーへ集計進捗(注文内の全ラインの`delivered`/`requested`合計)を`player.onScreenDisplay.setActionBar`で表示する。1プレイヤー同時1件の制約により、この表示が複数の注文で競合することは無い。
  - **更新間隔は周期アップグレード軸の最速Tier(T4)に合わせる**(ユーザー指摘): `delivered`の値自体はコントローラの処理サイクル間隔(周期アップグレード軸のTierに応じて可変、`networkProcessing.ts`の`getCycleIntervalTicks`)より速くは変化しないため、それより短い間隔で表示を更新しても実際には値が変わっていない同じ数値を描き直すだけで無意味。`getCycleIntervalTicks(getAxisMaxTier(CONTROLLER_CYCLE_AXIS))`(=T4の間隔、現状10tick)を更新間隔に使うことで、無駄な更新を避けつつ実際に値が変わりうる最短間隔には確実に追従する。ハードコードせず軸定義から動的に導出しているため、将来周期軸のTier数や各Tierの間隔テーブルを調整しても自動的に追従する。
- **完了表示**(`orderProcessing.ts`の`finalizeOrder`): 既存の完了通知(`wh:notify_on_complete`トグルが有効な場合の`player.sendMessage`)と同じ条件下で、配達ターミナル発の注文であればアクションバーにも完了(または一部不足)を表示する。専用の設定は増やさず既存のトグルに相乗りしている。進捗ループは注文がissuing/ordersから消えた後は対象にしないため、この完了表示を上書きしない。

## 16. アイテム検索をクライアントロケールの表示名でもできるようにする

引き出し/預け入れ/ウィッシュリスト等の検索(`entry.label`でのマッチング)は、カスタム名(`nameTag`)が無い限り生の`typeId`(例: `minecraft:diamond`)にしかマッチせず、プレイヤーが画面で見ている翻訳済みの名前(例: 「ダイヤモンド」)で検索できないという指摘を受けて改善した。

### サーバー側は任意プレイヤーの翻訳済み文字列を本来知る手段を持たない

`{translate: key}`(RawMessage)はクライアント側で解決される仕組みで、サーバースクリプトが特定プレイヤーの言語での実際の文字列を得る標準的な手段は無い。ただし`Player.clientSystemInfo.locale`(`@minecraft/server`)でプレイヤーのクライアントロケール自体(`"ja_JP"`等)は取得できる。そこで、**ロケールごとの`localizationKey -> 表示名`の対応表を静的データとしてこのアドオン自身が持ち、そのプレイヤーのロケールで引く**方式にした。

### 対応表の生成: vanilla分とアドオン自身の分を分離

- **vanilla分**(`scripts-src/vanillaItemNames.json`): Minecraft本体が実際に持つリソースパックのlangファイル(このマシンにインストール済みのMinecraft本体、`.../data/resource_packs/vanilla/texts/{en_US,ja_JP}.lang`)から、`^(item|tile|block)\.[^=]+\.name=(.*)$`にマッチする行(1localeあたり1891件)を抽出して生成する(`tools/generate-vanilla-item-names.mjs`)。**CI(GitHub Actions)にはMinecraft本体が無い**ため、このスクリプトは`npm run build`には組み込まず、開発者がローカルで手動実行して結果をコミットする運用にした(Minecraftのアップデートで新アイテムが増えた時などに再実行する)。
- **アドオン自身の分**(`scripts-src/addonItemNames.json`): 当初はこちらもvanilla分と同じ「事前生成してコミット」方式にする案だったが、**このアドオンに新しいアイテムを追加するたびに生成コマンドを手動で再実行し忘れると、その新アイテムだけ検索できないまま古びる**という指摘を受けて設計変更した。アドオン自身の`RP/texts/{en_US,ja_JP}.lang`はMinecraft本体を必要とせずリポジトリ内だけで完結するため、`tools/generate-addon-item-names.mjs`を**`npm run build`のたびに毎回実行するステップ**(`build:item-names`)にし、常に最新の状態を保証している(CIでも問題なく動く)。
  - **正規表現がvanilla分と異なる**: このアドオンのlangファイルは、ブロック(block_placerアイテム含む)は`tile.wh:xxx.name`(vanillaと同じ、`.name`接尾辞あり)だが、純粋なアイテムは`item.wh:xxx`(`.name`接尾辞なし、`BP/items/*.json`の`minecraft:display_name.value`と実際に一致させている)という2つの命名規則が混在している。素朴に「`item.`/`tile.`で始まる行」を全部拾うと、`item.wh:wrench.desc.1`のような説明文キーまで誤って対応表に入ってしまうため、識別子の直後にさらにドット区切りが続かない形(`tile\.wh:[a-z0-9_]+\.name`または`item\.wh:[a-z0-9_]+`)だけを対象にする専用の正規表現にした。
- **`scripts-src/addonItemNames.json`はビルドのたびに再生成されるが、コミットは必要**: `itemIdentity.ts`がこのファイルを`import`するため、`npm run typecheck`はビルド実行前でもこのファイルの存在を要求する(`resolveJsonModule`を`tsconfig.json`に追加した)。CIも`typecheck`→`build`の順で走る(`.github/workflows/release.yml`)ため、生成物であっても`BP/scripts/main.js`のような出力専用ファイルとは扱いが異なり、コミットが必須になる。

### 検索ロジック(`scripts-src/itemIdentity.ts`の`matchesSearchQuery`)

既存の`label`一致(typeId/カスタム名)に加えて、`vanillaItemNames.json`/`addonItemNames.json`から検索プレイヤーのロケールでの表示名を引き、そちらにもマッチすればヒットとする(OR条件)。カスタム名が付いていても、素の(隠れた)アイテム名でも検索できる。対応する言語データが無いロケール(現状en_US/ja_JP以外)や、**他アドオン(サードパーティ)のアイテム**(Script APIには他のリソースパックのlangファイルを実行時に読む手段が無いため、この対応表には含まれない)は、従来通り`label`一致のみへ自動的にフォールバックする(検索の裾野が狭まるだけで壊れない)。

引き出し/預け入れ(`terminalUi.ts`)・ウィッシュリスト(`autoTerminalUi.ts`)・在庫目標(`inventoryTerminalUi.ts`)・スロット設定(`precisionTerminalUi.ts`)・オブザーバー設定(`observerUi.ts`)の5箇所すべての検索処理を、`entry.label.toLowerCase().includes(q)`から`matchesSearchQuery(entry, q, locale)`に置き換えた。`locale`は各UIの入口(`showXxxUi(player, block)`)が持つ`player.clientSystemInfo.locale`を、検索を行う内部関数へ新しい引数として渡している。

- **(ユーザー要望により追加) ひらがな/カタカナを区別しない**: 「ダイヤモンド」を「だいや」で検索できるようにしたい、という要望を受け、比較前にカタカナをひらがなへ正規化する`normalizeKana`を追加した。全角カタカナ(U+30A1-U+30F6)とひらがな(U+3041-U+3096)はUnicode上0x60だけ離れているだけなので、該当範囲の文字コードから0x60を引くだけで変換できる(長音記号「ー」等、対応するひらがなが無い文字はそのまま残るが実害は無い)。既存の`toLowerCase()`(英数字の大小区別をしない)と同じ「検索クエリ・比較対象の両方を正規化してから`includes`する」という発想の延長として素直に組み込めた。

### スクリプトバンドルのサイズへの影響

vanilla分(約1891件×2locale)+アドオン分を合わせても200KB弱で、esbuildバンドルに直接importして組み込んでいる(`BP/scripts/main.js`は約157KBから約408KBに増えた)。個人利用アドオンの規模では実用上問題にならないと判断した。

## 17. 搬入出パッド(`wh:io_pad`)

**プレイヤーが上に乗ると、設定した所持数を維持するように定期的にネットワークとアイテムを出し入れする**床置きブロック。自動端末(6章)の「目標数を維持する」という発想を、対象を「アタッチ先のストレージ」から「パッドの上に乗ったプレイヤーの所持品」に変えて適用したもの。ID接頭辞は`IOP-`。

- **目標設定**(`PadTargetLine`、形はWishlistLine/StockTargetLineと同じだが対象が違うため別の型): 引き出しUIの検索と同じ「検索+スライダー+増減トグル+ページャー+現在のリスト(タップで削除)」構成(`ioPadUi.ts`の`setupPadTargetTab`)で、プレイヤーがパッドの上で維持したい所持数を品目ごとに設定する。保存先は他のターミナル系と同じく`terminalSettings.ts`の非表示エンティティ(`wh:pad_targets`)。在庫管理ターミナル(7章)と同じ「空に設定」トグルも用意しており、ONの間は検索結果のタップが目標所持数を0に上書き設定する専用動作になる(通常の「減らす」操作では0まで減らした瞬間にリストから除去され「常にこの品目を持ち歩かず預け入れる」状態を維持できないため)。実装当初は検索欄をスライダーより先に配置していたが、「検索欄は検索結果一覧の直前に置く」という他のターミナル系検索UI共通の規約(7章)から外れていたため、この対応の際に他と同じ並び順(スライダー→空に設定→増減トグル→検索欄→検索結果)に揃えた。
- **検索候補にプレイヤーの手持ちも含める**(ユーザー要望): 他のターミナルの検索UIは全てネットワーク在庫(`scanCatalog`)のみが検索対象だが、搬入出パッドは目標の対象が「プレイヤーの所持数」であるため、ネットワークにまだ無い品目(プレイヤーが拾ったばかりでネットワーク未登録のもの等)にも目標を設定できないと不便。そこで`showIoPadUi`でプレイヤーの現在のインベントリを`scanContainerCatalog`でスキャンし、ネットワーク在庫のカタログと`mergeCatalogs`(`ioPadUi.ts`)でマージしてから検索・ページングに渡すようにした。マージは`displayKeyEquals`(`itemIdentity.ts`)で同一品目(typeId+カスタム名)を判定し、両方に存在する品目は1行にまとめて数量を合算して表示する(検索結果に同じ品目が2行出るのを避けるため)。目標リスト側(`PadTargetLine`)・定期チェック(`padCheck.ts`)は元々プレイヤーの所持数だけを見ており搬入出先の解決もネットワーク在庫の有無に依存しないため、検索候補を広げるだけで処理側の変更は不要だった。
- **モード**(`PadMode`、`wh:pad_mode`): 「搬入出」「搬入のみ」「搬出のみ」の3択(設定タブの`dropdown`)。目標設定とは独立した軸で、目標超過分の預け入れ・目標未達分の引き出しのうち、どちらを実際に行うかを制御する。「搬入のみ」は超過分の預け入れだけ(不足があっても引き出さない)、「搬出のみ」はその逆。
- **定期チェック**(`padCheck.ts`、他の自動系と同じ間隔(コントローラの周期Tierに追従。18章参照)・`isRedstoneLocked`でのロックにも対応): ネットワークの`terminals`のうち`wh:io_pad`について、パッドの真上に立っているプレイヤーを検出し、各プレイヤーのメインインベントリ(`scanContainerCatalog`)を目標リストと比較して、モードに応じて`submitOrder`/`submitDeposit`を送る。重複送信の防止は`hasPendingOrderFor`/`hasPendingDepositFor`をそのまま使う(ターミナル位置+品目ベースの判定のため、同じパッドに複数プレイヤーが同時に乗って同じ品目を待つ稀なケースでは早い者勝ちになるが、他の自動系と同じ粒度としてMVPでは許容している)。
- **「プレイヤーがブロックの上に乗っているか」の判定**(`playersStandingOn`): このアドオンで「プレイヤーの連続座標」を判定条件に使うのは初めて(既存の`locEquals`はブロック座標同士の一致判定のみを想定)。`dimension.getPlayers({ location, maxDistance })`で近傍候補を広めに取ってから、パッドのXZセル内(`Math.floor`で一致)・Y方向はパッドの上に乗っているとみなせる範囲(ジャンプ等の多少の浮きを許容)に厳密に絞り込む。
- **搬入出中のアクションバー表示**(`ioPadProgress.ts`、ユーザー要望): 配達ターミナルの進捗表示(`deliveryProgress.ts`、「配達中... X/Y個」)と同じ仕組み・同じ更新間隔(周期軸の最速Tierの間隔。値が変わりうる最短間隔にちょうど追従する)で、搬入出パッドの引き出し/預け入れの進捗を`player.onScreenDisplay.setActionBar`に表示する。配達ターミナルとの違いは、配達ターミナルが常に引き出し(配達)方向のキューしか持たないのに対し、搬入出パッドは「搬入出」モードで**同一プレイヤーが同時にOrderとDepositRequestの両方**を抱えうる点(たとえば丸石は余っているので預け入れつつ、鉄インゴットは足りないので引き出す、という状態が同時に起こる)。そのため`deliveryProgress.ts`のように都度`setActionBar`を呼ぶと後勝ちで片方の表示が消えてしまうため、プレイヤーごとに「搬出中... X/Y個」(Order)・「搬入中... X/Y個」(DepositRequest)の行を集約してから、そのtickで1回だけ`setActionBar`(複数行は`\n`結合)する設計にした。
- **完了時のアクションバー表示**(ユーザー要望): 配達ターミナル専用だった「配達完了!」のアクションバー一撃表示(15章、`orderProcessing.ts`のfinalizeOrder)と同じ場所に、`wh:io_pad`向けの分岐(「搬出完了!」/一部不足時「搬出完了(一部不足があります)」)を追加した。預け入れ側(`depositProcessing.ts`のfinalizeDeposit)は元々**どのターミナルにも完了通知の仕組みが無かった**(チャットメッセージすら送っていない)ため、io_padに限定して新規に追加した(「搬入完了!」/一部不足時「搬入完了(一部搬入できませんでした)」)。他のターミナルの預け入れ完了時の挙動(無通知)は変えていない。この非対称(引き出しは元々全ターミナル共通でチャット通知があったが、預け入れには無かった)は今回の対応で埋めていない意図的なスコープ限定で、預け入れの完了チャット通知を一般に追加するかどうかは別途要望があれば検討する。

### 搬入出先の解決: 配達ターミナル(15章)と同じ分岐方式、ただしフォールバックなし

搬入出パッドには「張り付いた先」(`getAttachedStorageLocation`)という概念が無く、搬入出先は常に「そのリクエストを送ったプレイヤー(`order.playerName`/`request.playerName`)のインベントリ」になる。配達ターミナルが既に「注文したプレイヤーがオンラインならインベントリへ優先的に届ける」という実績のある分岐(`orderProcessing.ts`/`depositProcessing.ts`)を持っていたため、それと同じ場所に**もう一段階前段の分岐**として追加した: `wh:io_pad`の場合は`getAttachedStorageLocation`を呼ばずに、常にプレイヤーのインベントリコンテナを`destContainer`/`sourceContainer`として使う。

- **フォールバック無しでよい理由**: 配達ターミナルは「プレイヤーが見つからなければ張り付いた先のストレージへ」というフォールバックを持つが、パッドには張り付いた先という代替搬入出先がそもそも存在しない。プレイヤーが見つからなければ`destContainer`/`sourceContainer`は`undefined`のままになり、これは既存の「搬入(出)先が無い」処理(該当ラインを`shortfall`として確定し`wh:partial`/`wh:deposit_partial`に記録した上でキューから外す。オーダー処理の`if (!destContainer) { finalizeOrder(...) }`)にそのまま乗る。
- **プレイヤーがパッドから離れてもキューが詰まらない**: この「搬入(出)先が無ければ即座に確定して次へ進む」処理は、「ターミナルブロック自体が読み込めない(チャンク未読み込み等)」場合の`break`(FIFOを守るためそのtickはそこで諦めて次tickに再試行)とは異なる経路であり、プレイヤーがオフライン/パッドから離れているだけでは他の引き出し/預け入れの処理を妨げない。配達ターミナルの設計(15章)がこの安全性を先に実証していたため、パッドの実装にあたって新たな安全対策を考える必要は無かった。

### ブロック定義

他のターミナル系(`geometry.terminal_panel_north`の薄い壁掛けパネル)と違い、**プレイヤーが上に乗れる床置きのフルブロック**である必要があるため、コントローラと同じ`minecraft:geometry.full_block`(通常の当たり判定、`minecraft:placement_position`のような設置面トレイトは無し)を使う。テクスチャもコントローラと同じ「フルブロック1枚のテクスチャが6面すべてに使われる」方式(`tools/generate-placeholder-icon.mjs`の`ioPadColorAt`、中央に搬入(内側シアン)/搬出(外側ピンク)を表す二重の同心リング)にしており、専用のアイテム定義・アイコンテクスチャは無い(controllerと同じく、ブロックに紐づく暗黙のアイテムをそのまま使う。`RP/textures/item_texture.json`にエントリが無いのはこのため)。`isTerminalLikeBlock`(`terminalBlock.ts`)に追加済みで、レンチでの接続対象・引き出し/預け入れ処理側の生存確認のどちらも他のターミナル系と同じ扱いになる。

アイテムの説明文(`itemDescriptions.ts`)は、他のターミナル系が共有する「ストレージに設置し、ネットワークに接続して使用します」(`item.wh:terminal_family.desc.usage`)がストレージへの張り付きを前提にしており搬入出パッドには合わないため、専用の使い方説明(`item.wh:io_pad.desc.usage`、「ネットワークに接続し、上に乗って使用します。」)を用意した。

## 18. 自動系定期チェックの間隔をコントローラの周期Tierに合わせる(ユーザー要望)

自動端末(`autoOrderCheck.ts`)・在庫管理ターミナル(`inventoryCheck.ts`)・精密ターミナル(`precisionTerminalCheck.ts`)・搬入出パッド(`padCheck.ts`)の4つの定期チェックは、当初は全て固定100tick(5秒)間隔の独立した`system.runInterval`だった(7章の各節、17章参照。将来可変化する想定であることは7章の自動端末の節に元々書かれていたが、着手していなかった)。搬入出パッドで「パッドに乗ってから搬入出が始まるまでラグがある」というユーザー指摘を受けて原因(検知の定期チェック自体が固定5秒間隔であること)を説明したところ、「接続されているコントローラの頻度に合わせられないか」という要望があり、対応した。パッドだけを直すと4系統中1つだけ検知のタイミングが違う挙動になり一貫性を損なうため、4つ全てに適用した。

- **実装**(`networkProcessing.ts`の`startCycleAlignedLoop`): 本処理ループ(`startNetworkProcessingLoop`、4章)が元々持っていた「基準ループ(`BASE_LOOP_INTERVAL_TICKS`=5tick)+ネットワークごとの前回処理tickを記憶するMap」というパターンを、`callback: (network, dimension) => void`を受け取る関数として切り出した。コントローラの周期短縮キットのTierに応じたサイクル間隔(`CYCLE_TICKS_BY_TIER`=[100, 80, 50, 25, 10]、4章参照)は`getCycleIntervalTicks(getAxisTier(...))`で求める。呼び出し側(4つの定期チェック)は「ネットワーク1件分の処理」(`checkNetworkX(network, dimension)`)だけを実装し、`startCycleAlignedLoop(checkNetworkX)`と書くだけでよくなった。呼び出しごとに専用の`lastProcessedTick`Mapを新設するため、4つの定期チェックは(間隔の計算式が同じになっても)引き続き互いに独立したループとして動作する(本処理ループとも独立)。
- **本処理ループ自体もこの共通化の恩恵を受けた**: `startNetworkProcessingLoop`自身も`startCycleAlignedLoop`を使うようリファクタリングし、`dimension`の解決(`world.getDimension`)を1箇所に集約した。4つの定期チェックの各`checkNetworkX`関数も、以前は自分で`world.getDimension(network.dimensionId)`していたのをやめ、コールバック引数として受け取るだけになった(`padCheck.ts`の`playersStandingOn`も、`dimensionId: string`を受け取って内部で`world.getDimension`する二度手間をやめ、`checkNetworkPads`が既に持っている`dimension`を直接渡すように変更した)。
- **効果**: 検知の遅延も、実際の搬入出処理と同じだけコントローラのグレードに応じて速くなる。周期短縮キットT4(10tick=0.5秒)なら検知間隔も0.5秒になり、搬入出パッドの体感ラグ(17章、検知待ち+処理サイクル待ちの合計)が大きく縮む(Tier0基準の最悪ケース約10秒 → T4なら約1秒)。
- **(追記)25章の広告モデルへの移行で、4つの定期チェックは独立した`startXCheckLoop`を持たなくなり、`processNetworkOrders`/`processNetworkDeposits`(本処理ループ)から直接関数として呼ばれる形になった。検知と処理が別々のタイマー呼び出しですらなくなり、同一tickの同一関数呼び出し内で完結するため、この章で縮めた「検知待ち+処理サイクル待ち」のラグはさらに実質ゼロに近づいている。**



## 19. 個数指定スライダーを2のべき乗刻みに統一(ユーザー要望)

通常のターミナル(引き出し/預け入れの数量)・自動端末(ウィッシュリスト目標)・在庫管理ターミナル(在庫目標)・精密ターミナル(スロット目標)・搬入出パッド(所持数目標)の5箇所にある「スライダーで個数を指定し、検索結果の行をタップ(連打)して増減する」UIは、いずれも`min=1, max=64, step=1`の線形スライダーだった。「毎回スライダーを細かく操作するのが面倒」「64段階を細かく動かすより、行を連打した方が速い」というユーザー指摘を受け、スライダーの刻みを`1, 2, 4, 8, 16, 32, 64, 128, 256, 512, 1024`の2のべき乗(11段階)に変更し、デフォルト値も1から64に変更した。

- **実装**(`quantitySlider.ts`の`setupQuantitySlider`): DDUIの`slider(label, value, min, max, { step })`は`step`が固定の数値増分のみで、等比数列(2のべき乗)を直接表現する手段が無い。そのため内部的には`0`〜`10`(`QUANTITY_STEPS.length - 1`)という**インデックス**をスライダー本体(`ObservableNumber`、`min=0, max=10, step=1`)に束縛し、呼び出し元へ返す実際の個数(`ObservableNumber`)はそのインデックスから`QUANTITY_STEPS[index]`で計算する。段数が64→11に減るため、目的の値までドラッグする際に必要な位置合わせの精度そのものも下がる(=大まかにドラッグするだけで狙った桁に届きやすくなる)。
- **表示ラベルの動的更新**: スライダーの`label`引数は`ObservableString`を渡せる(`CustomForm.slider`の型定義で確認済み)。インデックスの`subscribe`で「{ラベル文言}: {実際の個数}」という文字列に書き換えることで、スライダーの見出し文言に常に実際の個数(1、2、4、…、1024)が表示されるようにした。ただし、**スライダー自体が持つ生の数値インジケータ(仮に描画されるなら0〜10のインデックス値)がクライアント側でどう表示されるかは、この場では実機確認できていない**(SliderOptionsに表示フォーマットを上書きする手段は無い)。ラベル文言側は実際の個数を正しく示すが、万一インデックス値もどこかに表示され紛らわしい場合は、実機確認の上で追加対応が必要になる可能性がある。
- **5箇所で共通の実装に統一**: `terminalUi.ts`(通常のターミナルの引き出し/預け入れ数量)・`autoTerminalUi.ts`(ウィッシュリスト目標)・`inventoryTerminalUi.ts`(在庫目標、「空に設定」トグルとの`disabled`連携も維持)・`precisionTerminalUi.ts`(スロット目標)・`ioPadUi.ts`(パッドの所持数目標、同じく「空に設定」との連携を維持)の5箇所全てで、個別に`new ObservableNumber(1, ...)` + `form.slider(...)`を書いていたのを`setupQuantitySlider(form, label, tabVisible, { disabled? })`という1行の呼び出しに置き換えた。呼び出し元が読み取る値(検索結果行タップ時の増減量)は返り値の`ObservableNumber`から`.getData()`で取得する点は変更していない。

## 20. ターミナル系レシピの見直し(ユーザー要望)

基本ターミナル・精密ターミナル・配達ターミナルの3種のクラフトレシピを、ユーザーから提示されたコスト配分(素材と配置のマス目単位での指定)に基づいて全面的に更新した。あわせて、搬入出パッド(17章)には元々クラフトレシピが存在しない抜け漏れがあったため、この対応で新設した(`BP/recipes/io_pad.json`)。具体的な意図(なぜその素材配分にしたか)はユーザー指定のコスト見直しによるもので、本章では変更点の記録に留める。

- **基本ターミナル**(`BP/recipes/terminal.json`): 中央の消費材を`minecraft:comparator`から`minecraft:redstone`(レッドストーンパウダー)へ変更し、上段四隅の鉄インゴット2個を削除した(`pattern: ["ABA", "BCB", "ADA"]` → `[" B ", "BCB", "ADA"]`)。`unlock`もレシピから外れた`minecraft:comparator`から`minecraft:redstone`へ変更した。
- **精密ターミナル**(`BP/recipes/precision_terminal.json`): 中段で消費する自動端末を3個から1個(中央のみ)へ減らし、四隅のアメジスト欠片2個を削除した(`pattern: ["ABA", "CCC", "ABA"]` → `[" B ", "ACA", " B "]`)。`unlock`(`wh:auto_terminal`)は変更していない。
- **配達ターミナル**(`BP/recipes/delivery_terminal.json`): 中央の消費材を`wh:terminal`から`wh:auto_terminal`へ変更した上で、`minecraft:ender_pearl`×2と`minecraft:redstone`を削除し、`minecraft:name_tag`(上)+`wh:auto_terminal`(中央)の2種類のみに簡略化した(`pattern: [" A ", "BCB", " D "]` → `[" A ", " B ", "   "]`)。`unlock`も`wh:terminal`から`wh:auto_terminal`へ変更した。
- **搬入出パッド**(`BP/recipes/io_pad.json`、新設): 上=`minecraft:heavy_weighted_pressure_plate`(鉄の重量感圧板)、中央=`wh:auto_terminal`の2種類(`pattern: [" A ", " B ", "   "]`)。感圧板は「プレイヤーが上に乗って使う」という搬入出パッド自体の操作方法(17章の`playersStandingOn`)と素材面で符合している。`unlock`は他の3種と同じ理由で`wh:auto_terminal`にした。
- **共通してユーザー指定を反映するにあたっての実装判断**: いずれの新レシピも、行(row)が完全に空(3マスとも未使用)になる場合は`pattern`の要素をその分だけ省略せず`"   "`(半角スペース3つ)として明示的に残す形にした。既存の全レシピが例外なく3行を書き切るスタイルだったため、それに合わせて統一している(Bedrockの`recipe_shaped`自体は2行以下のパターンも許容するが、この採用は見送った)。

## 21. ターミナル系レシピの再見直し(ユーザー要望)

20章の見直しからさらに、自動端末・在庫管理ターミナル・精密ターミナル・配達ターミナル・搬入出パッドの5種のクラフトレシピを、ユーザーから提示されたコスト配分(素材と配置のマス目単位での指定)に基づいて更新した。基本ターミナル・コントローラ・ネットワークオブザーバーの3種は今回の指定と現状のレシピが一致していたため変更していない。20章と同じく、具体的な意図(なぜその素材配分にしたか)の記録はユーザー指定によるもので、本章では変更点の記録に留める。

- **自動端末**(`BP/recipes/auto_terminal.json`): 左右のレッドストーンリピーター2個を削除し、上=ドロッパー/中央=基本ターミナル/下=時計の縦一列のみに簡略化した(`pattern: [" A ", "BCB", " D "]` → `[" A ", " C ", " D "]`。`B`=リピーターのキー自体を削除)。`unlock`(`wh:terminal`)は変更していない。
- **在庫管理ターミナル**(`BP/recipes/inventory_terminal.json`): 自動端末と同じ変更(左右のレッドストーンリピーター2個を削除し、上=エンダーアイ/中央=基本ターミナル/下=時計の縦一列のみに簡略化。`pattern: [" A ", "BCB", " D "]` → `[" A ", " C ", " D "]`)。`unlock`(`wh:terminal`)は変更していない。
- **精密ターミナル**(`BP/recipes/precision_terminal.json`): 下段の的1個を削除し、代わりにその位置へ元々中段左右にあったアメジスト欠片のうち1個分を移動する形で、上=的/中央=自動端末/下=アメジスト欠片の非対称な縦一列に整理した(`pattern: [" B ", "ACA", " B "]` → `[" B ", " C ", " A "]`)。`unlock`(`wh:auto_terminal`)は変更していない。
- **配達ターミナル**(`BP/recipes/delivery_terminal.json`): 中央の消費材を20章で`wh:auto_terminal`に変えていたのを`wh:terminal`へ戻し、下段に`minecraft:leather`を追加した(`pattern: [" A ", " B ", "   "]` → `[" A ", " B ", " C "]`)。`unlock`も`wh:auto_terminal`から`wh:terminal`へ戻した。
- **搬入出パッド**(`BP/recipes/io_pad.json`): 下段に`minecraft:hopper`を追加した(`pattern: [" A ", " B ", "   "]` → `[" A ", " B ", " C "]`)。`unlock`(`wh:auto_terminal`)は変更していない。

## 22. 自動系ターミナルの「明らかに達成できない注文」を事前に抑制する(ユーザー要望)

**(25章の広告モデルへの移行により、この章で導入したクランプ機構は自動端末・精密ターミナル・搬入出パッドについては不要になり削除されている。25章参照。以下は当時の設計判断の記録として残す。)**

自動端末・精密ターミナル・搬入出パッドの定期チェックは、「アタッチ先(または所持品)の不足数」だけを見て`submitOrder`しており、**ネットワークにその品目が実際にあるかを確認していなかった**。品目がネットワークに無い(または足りない)場合、`processNetworkOrders`の`extractFromStorages`が搬入先を全ストレージ走査した末に0件しか取り出せず、その注文はshortfallとして即終了する。次のチェック周期になると`hasPendingOrderFor`が再びfalseに戻るため、**存在しない品目に対して同じ無駄な注文を周期ごとに延々と出し続ける**という問題があった(ユーザー指摘)。在庫管理ターミナルは元々ネットワーク全体のカタログを基準に注文量を決めているため、この問題は無い。

- **対応**(`autoOrderCheck.ts`のcheckShortfalls、`precisionTerminalCheck.ts`のcheckSlot、`padCheck.ts`のcheckPlayer): アタッチ先/所持品の不足分から注文候補を出した後、`submitOrder`する前にネットワーク全体のカタログ(`scanCatalog`)を確認し、**要求量を実際のネットワーク在庫にクランプ**する(`Math.min(shortfall, availableInNetwork)`)。クランプ後が0以下ならその品目は今回注文しない。処理までの間に他のターミナルが同時に同じ品目を消費してズレることは許容する(ユーザーの要望通り、完璧な正確性は求めていない)。
- **scanCatalogの呼び出しは「候補が実際にある時だけ」に絞った**: 不足数の判定自体(アタッチ先/所持品との比較)はネットワーク走査が要らないため先に行い、注文候補が1件も無ければネットワーク在庫の確認自体を行わない(`candidates.length === 0`なら早期return)。目標を満たしている定常状態では、この対応の前後でscanCatalogの呼び出し回数は変わらない(=追加コストはゼロ)。
- **`networkCatalogCache.ts`(新設)によるtick単位の重複排除**: 自動端末・在庫管理ターミナル・精密ターミナル・搬入出パッドの4つの定期チェックは、いずれも`networkProcessing.ts`の`startCycleAlignedLoop`で同じ基準間隔・同じサイクル間隔式(18章)で動くため、同じネットワークに複数種のターミナルが同居していると、同じtickに複数の定期チェックが同じネットワークの`scanCatalog`を重複して呼び出しうる。ネットワークIDをキーに`system.currentTick`単位でキャッシュすることで、同じtick内では最初の1回だけ実際にスキャンし、以降はMapの参照を返すだけにした。在庫管理ターミナルの既存の`scanCatalog`呼び出し(元々ネットワーク単位で1回にまとめていたもの)もこのキャッシュ経由に統一した。

### パフォーマンスへの影響評価(ユーザーからの依頼)

`scanCatalog`はネットワーク内の全ストレージの全スロットを舐めるため、大規模なネットワーク(例: ラージチェスト30個=1620スロット)ほどコストが高い。この対応でその呼び出し頻度がどう変わるかを評価した。

- **定常状態(全ての目標を満たしている)**: 追加コストは無い。候補が無ければ`scanCatalog`自体を呼ばないため(上記の早期return)。
- **品目がネットワークに存在せず注文が成立しない状態が続く場合**: 従来は「注文をsubmit→次の処理サイクルで`extractFromStorages`が全ストレージ・全スロットを走査した末に0件で失敗→shortfall記録→次のチェック周期でまた同じ注文をsubmit」という無限ループが、コントローラの周期短縮キットのTierに応じた間隔(18章、最速0.5秒)で回り続けていた。`extractFromStorages`自体が`scanCatalog`と同程度のコスト(全ストレージ・全スロットの走査)を持つため、**今回の対応は「処理失敗時に払っていたのとほぼ同じ走査コストを、チェック時点に前倒しして払う代わりに、注文キュー(`wh:orders`/`wh:issuing`)への書き込み・shortfall記録(`wh:partial`)・完了通知処理といった周辺コストを丸ごと削減する」形になり、総コストは同等かそれ以下になる見込み**。加えて、同一ネットワーク内で複数の定期チェックが同時に候補を持つケースは`networkCatalogCache.ts`で重複排除される。
- **品目がネットワークに存在し、実際に注文が成立していく状態**: この場合が唯一の純増分になる。従来は成立する注文についてはチェック時点でネットワーク走査を行っていなかったため、`scanCatalog`1回分がチェック周期ごとに新たに発生する(処理サイクル側の`extractFromStorages`は従来通り別途発生する)。ただし発生するのは「そのネットワークで実際に品薄が解消されるまでの間」に限られ、目標を満たせば定常状態に戻って追加コストも無くなる。
- **総括**: 大規模ネットワーク×最速Tier(0.5秒間隔)×多数の品目が同時に品薄、という組み合わせが続く場合が最も負荷が乗るケースだが、そのようなケースでは元々`extractFromStorages`によるフル走査が処理サイクル側で頻発している状況でもあるため、体感できるほどの追加負荷にはなりにくいと考えている。もし実機で目に見えるラグが出るようであれば、`networkCatalogCache`の保持期間をtickより長くする(例: 数tickに1回だけ再スキャン)といった追加の緩和策を検討する。

## 23. 「空に設定」トグルを個数指定スライダーに統合(ユーザー要望)

在庫管理ターミナル・搬入出パッドが持っていた「空に設定」トグル(19章)は、ONの間だけ検索結果のタップの意味を「増減」から「目標を0へ直接上書き」に切り替える、スライダー・増減トグルとは別建てのコントロールだった。これをスライダー自体の一番左のステップとして統合し、「空に設定, +1, +2, +4, ..., +1024」という並びの1つのコントロールに一本化した(符号(+/-)は従来通り一つ下の「増やす/減らす」トグルで切り替える)。

- **実装**(`quantitySlider.ts`): `setupQuantitySlider`に`emptyOption?: boolean`を追加した。当初は`QUANTITY_STEPS`の**先頭に**特別な値`0`を追加した配列をスライダーのインデックスに束縛する実装にしていたが、それだと以降の全ステップのインデックスが1つずつズレて`QUANTITY_STEPS[n]`が`2^n`ではなく`2^(n-1)`を指すことになり、「表示される数字が2^nとズレるのが気持ち悪い」という指摘を受けた。そこで、空に設定を配列の先頭要素にするのではなく**インデックス-1**として扱う(`emptyOption`がtrueの時だけスライダーの`min`を`0`ではなく`-1`にする)方式に変更し、通常ステップのインデックスと指数の対応(`QUANTITY_STEPS[n] === 2^n`、`n`は0以上)を`emptyOption`の有無に関わらず常に保つようにした。インデックスから実際の値への変換は`valueAtIndex`に集約し、負のインデックスなら空に設定用の特別な値`0`を返す(`QUANTITY_STEPS`の最小値は1でありスライダーの通常ステップに0が現れることは無いため、**返り値の`amount`が0であること自体を「空に設定が選ばれている」の判定に使える**)。呼び出し元が`disabled`等に直接使いやすいよう、`amount === 0`を都度比較させるのではなく、`isEmptySelected: ObservableBoolean`として明示的に返す形にした(返り値は`ObservableNumber`単体から`{ amount, isEmptySelected }`のオブジェクトへ変更)。デフォルト位置は`emptyOption`の有無に関わらず`QUANTITY_STEPS.indexOf(64)`(=6)で求めるため、空に設定を追加しても64がデフォルトのまま保たれる(19章の「デフォルト64」の方針を維持)。
- **ラベル表示**: スライダーのラベル(`ObservableString`)は、空に設定が選ばれている間は「{ラベル文言}: 空に設定」、それ以外は従来通り「{ラベル文言}: {個数}」を表示する。
- **呼び出し元(`inventoryTerminalUi.ts`/`ioPadUi.ts`)の変更**: 独立していた`emptyMode`トグル(`form.toggle("空に設定", emptyMode, ...)`)を削除し、`setupQuantitySlider(..., { emptyOption: true })`の返り値`isEmptySelected`をそのまま使うように置き換えた。増減トグルの`disabled`も`emptyMode`から`isEmptySelected`に差し替えただけで、検索結果タップ時の分岐ロジック(`if (isEmptySelected.getData()) { ... }`)自体は変更していない(空に設定時に目標を0へ直接上書きする挙動は従来と同じ)。スライダー自体を無効化する仕組み(旧`disabled: emptyMode`)も不要になった(空に設定はスライダーの1ポジションになったため、両立を気にする必要が無い)。
- **自動端末にも拡張(ユーザー要望)**: 当初は在庫管理ターミナル・搬入出パッドの2種のみ`emptyOption: true`にし、通常のターミナル・自動端末・精密ターミナルの3種は元々「空に設定」を持っていなかったため対象外にしていた。しかし実機確認で「自動端末・精密ターミナルだけスライダーの最小値が0のままで、-1(空に設定)が出ない」という指摘を受け、両者にも一旦`emptyOption: true`を追加した。自動端末(`autoTerminalUi.ts`のsetupWishlistTab)は在庫管理ターミナルと全く同じパターンで、`isEmptySelected`が真の間は検索結果タップでウィッシュリストの目標を0に直接上書きする(=そのアタッチ先は常に空にする=常に全量預け入れる、という指定になる)ため、そのまま定着させた。
- **精密ターミナルは拡張を撤回して専用ボタンに戻した(ユーザー指摘)**: 精密ターミナルには元々**スロット単位で「目標をなしに設定する」という専用ボタン**(`upsertSlot({ slotIndex, targetAmount: 0, collect: ... })`)があった。一度はこれをスライダーの「空に設定」に統合し、検索結果タップ時に`isEmptySelected`が真なら(タップした品目に関わらず)スロットを空にする形へ一本化したが、**「あるアイテムを0にする」(在庫管理ターミナル・自動端末・搬入出パッドの空に設定)と「そのスロットを0にする」(精密ターミナル)は意味が異なる**という指摘を受けた。前者は品目を軸にした指定、後者はスロット番号を軸にした指定であり、検索結果(品目の一覧)をタップして操作する形は後者の意味に合わない(そもそも品目を選ぶ意味が無い操作になってしまう)。そのため精密ターミナルのみ`emptyOption`を外して元の専用ボタンへ戻し(結果、通常のターミナル(引き出し/預け入れの数量)・精密ターミナルの2種が「空に設定」機能を持たない)、19章で追加した「検索欄の直前にスロット/回収/数量スライダーを置く」レイアウトへ「目標を空に設定する」ボタンを最下部に復元した。この経緯から、**「空に設定」をスライダーへ統合できるのは、対象が品目(検索結果の行と1対1対応するもの)である場合に限る**という設計上の教訓が得られた。

## 24. 精密ターミナルで1エントリに複数スロットを指定できるように(ユーザー要望)

精密ターミナルのスロットルールは、1エントリにつき1スロットしか指定できなかった。同じ品目を複数スロットで維持したいケース(例: かまど3台分の燃料スロットを1つの目標でまとめて管理したい)に対応するため、「スロット番号」欄に`1,2,3`(カンマ区切り)・`1-3`(範囲)・`1,3-5,7`(組み合わせ)のような書式で複数スロットを指定できるようにした。目標数量はグループ全体の合計として扱い、実際の搬入時に対象スロットへできるだけ均等に分配する(ユーザー要望の例: 3スロットに30個の目標を設定し、ネットワークに20個しか無い場合、7,7,6個に分配される)。

- **データモデル**(`state.ts`): `PrecisionSlotLine.slotIndex: number`を`slotIndices: number[]`に変更した。当初は`OrderLine.slotIndex?: number`も`slotIndices?: number[]`に変更する形にしていたが、25章の広告モデルへの移行で精密ターミナルがOrderLine/DepositLineのキューを一切経由しなくなったため、`OrderLine`/`DepositLine`からスロット関連フィールドはどちらも削除されている(現状の型は25章参照)。
- **入力/表示のパース・整形**(`slotRange.ts`、新設): `parseSlotIndices(text)`が入力書式(単一値・カンマ区切り・`a-b`範囲・その組み合わせ)を展開済みの`number[]`にパースする(不正な入力は`undefined`を返し、呼び出し元は何もしない。既存の単一スロット時代の「無効なら無視」方針を踏襲)。`formatSlotRange(indices)`は逆に、ソート済みの配列を連続区間ごとに`a-b`へまとめたカンマ区切り文字列に整形する(例: `[1,2,3,5,7]` → `"1-3,5,7"`)。保存は常に展開済みの配列で行い、元の入力文字列(表記の揺れ・順序)は保持しない。`precisionTerminalUi.ts`(入力・現在のリスト表示)と`statusListUi.ts`(「状況」タブのツールチップ表示)の両方から使う。
- **均等分配のアルゴリズム**(`storageScan.ts`): 単純に配列の先頭スロットから順に満たしていくと、ネットワーク在庫が要求量に足りない場合に後方のスロットだけ0のまま、という偏った結果になってしまう(例: 3スロットに10個ずつ要求しているのに在庫が20個しか無い場合、先頭から満たすと10,10,0になる)。そこで2段階の処理にした: (1) `countAvailable`でネットワーク内の実在庫を(取り出さずに)数え、実際に配分可能な総量を先に確定させる。(2) その総量をn個のスロットへできるだけ均等に配分する。(3) 配分が決まったスロットごとに、既存の単一スロット搬入関数`extractFromStoragesIntoSlot`を呼ぶ(新しい低レベルの搬入ロジックは追加していない、既存プリミティブの組み合わせで実現)。この一連の処理を`extractFromStoragesIntoSlots`としてまとめている。各スロットの空き容量(スタック上限)によっては計画通り届かないことがあるが、それは物理的な制約として許容する(「可能な限り均等に分配する」というユーザー要望通り)。
  - **(実機で発見された不具合の修正済み) 単純な`evenSplit`では特定スロットが振動する**: 当初は(2)を毎回`evenSplit(amount, n)`(動かす量をn等分、端数は先頭スロットから+1)で行っていた。これは「全スロットが空の状態から1回で満たす」場合は正しいが、**既に一部のスロットが埋まっている状態(前回のサイクルで一部だけ届いた、等)では「動かす残り量」をスロットの現在の残量を無視してn等分してしまう**ため、残り1個をたまたま先頭スロットへ機械的に積み、その結果先頭スロットが「あるべき量」を1個超過し、回収(`reconcileSlotGroupDeposit`)がその1個を削って戻し、次のサイクルでまた同じ1個が先頭スロットへ積まれる…という**無限振動**を引き起こすことが実機で見つかった(ユーザー報告: `1-3,6-9`の7スロットに目標64個を設定した際、2マス目が9個と10個の間で振動し続けた)。原因は「動かす量」の再分配であって「各スロットのあるべき残量」を見ていなかったこと。`evenSplit`による均等割りを、**各スロットの現在値を見ながら最も少ないスロットから順に水準を合わせて埋めるwater-filling(`levelFill`)**に置き換えて修正した。`levelFill`は「全スロットが空」の場合は従来の`evenSplit`と同じ結果になる(例: 20個を3スロットに配分 -> 7,7,6)一方、一部が既に埋まっている場合も、既に上限に達したスロットへは追加せず、不足しているスロット同士でavailableを分け合うため、収束後は振動しない。`extractFromStoragesIntoSlots`の引数も、動かす量(amount)ではなく**各スロットの上限(cap、`evenSplit(targetAmount, スロット数)`)とスループット予算(budget)**を受け取る形に変更し、現在値の取得(`destContainer.getItem`)を関数内で行うようにした(呼び出し元の`reconcileSlotGroupWithdrawal`は、回収側と全く同じ`evenSplit(targetAmount, slotIndices.length)`でcapを求め、矛盾する品目が入っている対象外スロットを除いたものを渡すだけになった)。
- **補充**: グループ内の各スロットについて、空または目標品目と一致するスロットだけを「補充してよいスロット」として抽出し、その合計不足分を対象スロットへ均等分配して搬入する。当初はこれを1本の`OrderLine`として`submitOrder`していたが、25章の広告モデルへの移行後は`precisionTerminalCheck.ts`の`reconcilePrecisionTerminalWithdrawal`が`extractFromStoragesIntoSlots`を直接呼ぶ形になり、`OrderLine`もキュー投入も経由しない(重複送信防止の`hasPendingSlotOrderFor`も、キュー自体が無くなったため不要になり削除した)。
- **回収(collect)は複数スロット化しても物理スロット単位のまま**: 回収(`insertSlotIntoStorages`)は1スロット分の中身しか扱えず、そもそも「複数スロットから集めた分をどう配分するか」という悩みが無い(ネットワーク側の受け皿は基本的に無制限とみなしているため)。そのため各スロットの「あるべき量」を`evenSplit(targetAmount, スロット数)`で求め(補充と同じ配分ロジックを流用)、各スロット独立に「その量を超えた分、または目標外の品目」を搬入する。当初は`DepositLine`(単一の`slotIndex`)として`submitDeposit`していたが、これも25章の移行後は`reconcilePrecisionTerminalDeposit`が`insertSlotIntoStorages`を直接呼ぶ形になった。
- **(解消済み) 補充と回収が同じ周期で同時に走る件**: 当初はここで「各スロットの『あるべき量』は目標をスロット数で単純に均等分配した値であり、スロットごとの実際の現在量の偏りまでは考慮しない」ため、例えばスロット1だけ既に多めに入っている状態で目標を再設定すると、補充(不足しているスロット向け)と回収(スロット1の超過分)が同じチェック周期で同時に走ることがある、と「実用上は問題無い」として対応を見送っていた。しかし上記の振動バグ修正(`levelFill`の導入)で、補充側も回収側と全く同じ`cap`(`evenSplit(targetAmount, スロット数)`)を基準に「現在値が`cap`未満のスロットだけ」を対象にするようになったため、**回収の条件(現在値が`cap`を超過)と補充の条件(現在値が`cap`未満)は同じ`cap`値に対して排他的になり、同一スロットで両方が同時に発火することは無くなった**(空スロットは回収側がそもそも対象外にしているため元々競合しない、目標外の品目が入ったスロットは回収のみが対象で補充からは除外されるため同様に競合しない)。結果的にこの限界は解消されている。
- **既知の限界(未解消・ただし警告は追加済み)**: **異なる2つのエントリが物理的に同じスロットを含む場合(スロット範囲の重複、例: エントリAが`1-3`、エントリBが`3-5`を指定するとスロット3を両方が奪い合う)の防止は実装していない**(ユーザー要望により防止は行わず、重複を避けて設定するかはユーザー側の判断に委ねている)。ただし警告表示のみユーザー要望で追加した(`precisionTerminalUi.ts`のsetupSlotRuleTab): `findOverlaps()`が`slots`配列全体を走査し、同じスロット番号を含むエントリが2つ以上あればそのスロット番号→該当エントリ一覧のMapを返す。「スロット設定」タブの説明文のすぐ下に、重複がある間だけ表示されるラベルを追加し(`form.button`に空のonClickを渡し、`{visible, tooltip}`のみを使う「現在のリスト」の削除ボタンと同じ「ツールチップ付きラベル」パターンを流用。`form.label`のTextOptionsにはtooltipが無いため)、ラベル本体には重複しているスロット番号の概要(`formatSlotRange`)のみを、tooltipにはスロット番号ごとにどのエントリ同士が重複しているかの詳細を表示する。エントリの追加・更新・削除(`upsertSlot`/削除ボタン)のたびに再計算し、タブを開いた時にも再計算する。
- **既存データの移行**(`terminalSettings.ts`): 既存ワールドに保存済みのスロットルールは旧形式(`slotIndex: number`単体)のままなので、`getPrecisionSlots`の読み込み時に`slotIndices: [旧slotIndex]`へ変換する後方互換シムを追加した。保存(`setPrecisionSlots`)は常に新形式で書き戻される。

## 25. 目標系ターミナルを「広告モデル」へ移行(ユーザー提案・第1段階)

目標系ターミナル(自動端末・在庫管理ターミナル・精密ターミナル・搬入出パッド)は、22章までの設計では「専用のチェックループが不足/超過を計算 → `submitOrder`/`submitDeposit`で`wh:orders`/`wh:deposits`キューに固定量を積む → 別サイクルで`processNetworkOrders`/`processNetworkDeposits`がFIFOで消化する」という2段階方式だった。この方式には、チェック時点のスナップショットと実際の搬入時点との間にズレが生じる(22章の対症療法が必要になった根本原因)、`hasPendingXFor`系の重複防止が必須になる、といった構造的な問題があった。

議論の結果、コントローラが行う操作を**「引き出し」「預け入れ」「目標数の要求」の3種**に分けることにした。「引き出し」(通常のターミナルの手動カート・配達ターミナル)・「預け入れ」(通常のターミナルの手動カート)は、プレイヤーが「指定した数だけ追加で欲しい/手放したい」という一回性の要求であり、目標値に近づけるロジックで扱うのは意図に反する(ユーザー指摘)ため、22章までの固定量キュー方式のまま維持する。一方「目標数の要求」(自動端末・在庫管理ターミナル・精密ターミナル・搬入出パッド)は、目標値自体が既に`terminalSettings.ts`に永続化されているため、**キューに固定量を積む代わりに、コントローラの処理サイクルがその場で目標設定を直接読み、必要な分だけ動かす**(広告モデル)方式に置き換えた。目標値が実世界のコンテナの中身と直接比較されるため、複数サイクルにまたがる継続も`delivered`のような別カウンタ無しに自然に成立する(コンテナの中身自体が進捗を表す)。この段階では「引き出し」「預け入れ」には手を付けていない(第2段階として別途判断する)。

### 予算の共有と処理順序

`processNetworkOrders`/`processNetworkDeposits`は、それぞれ**まず既存の固定量FIFOキューを今まで通り消化し、余った予算だけを目標系の直接処理に回す**(プレイヤー起点の引き出し/預け入れを優先させるため)。引き出し方向・預け入れ方向で独立した予算を持つ設計(4章)はそのまま維持している。

### 新設: `targetReconciliation.ts`

`network.terminals`を1回だけ走査し、typeIdに応じて各ターミナル種別の直接処理関数へ振り分けるディスパッチャ。`reconcileAllTargetWithdrawals`/`reconcileAllTargetDeposits`の2関数を持ち、走査順は`network.terminals`の登録順(既存のFIFOと同じ「早い者勝ち」特性を維持する。ラウンドロビン等の公平性改善は今回のスコープ外とし、後続の課題とした)。レッドストーンロック(`isRedstoneLocked`)の判定は4種で共通のためここに一括した(以前は各チェックループが個別に判定していた)。`orderProcessing.ts`は`extractFromStorages`系を、`depositProcessing.ts`は`insertIntoStorages`系を使うため、循環参照を避けて`targetReconciliation.ts`が4つの端末別モジュール→`storageScan.ts`を参照し、`orderProcessing.ts`/`depositProcessing.ts`が`targetReconciliation.ts`を参照する向きにしている。

`reconcileAllTargetDeposits`のstorageIndex/depositTargets引数は、呼び出し元(`depositProcessing.ts`)が固定量の「預け入れ」FIFO向けに既に構築済みならそれを渡して使い回し、無ければここで初めて構築する(ネットワーク全体のスロットを走査するコストがあるため、必要な時だけ払う)。**depositTargets(Drain指定されたストレージを除外したリスト)は単なる最適化ではなく省略不可**であることに注意した実装にしている: 渡さないと`insertIntoStorages`側のデフォルト(`network.storages`全体)にフォールバックしてDrain指定を無視してしまうため、未指定時は必ずここで構築するようにしている。

### 4つの端末別モジュールの改修

`autoOrderCheck.ts`/`inventoryCheck.ts`/`precisionTerminalCheck.ts`/`padCheck.ts`は、それぞれ「shortfall/excessを計算して`submitOrder`/`submitDeposit`する」チェック関数を、「shortfall/excessを計算し、budget内で`extractFromStorages`/`insertIntoStorages`系を直接呼んで実際に搬入出する」`reconcileXWithdrawal`/`reconcileXDeposit`関数に置き換えた(戻り値: 消費した予算)。

- **重複防止(`hasPendingXFor`系)は不要になり削除**: 何も"発行"しないため二重発行という概念が無い。
- **22章のネットワーク在庫クランプは不要になり削除**: `extractFromStorages`自体が実在庫以上は取り出さないため、事前確認が不要。3ファイルのロジックが単純化された。
- **`networkCatalogCache.ts`は維持**: 在庫管理ターミナルは元々ネットワーク全体の在庫数が比較基準そのものなので、直接処理後も必要(クランプ用途ではなく本質的な比較用途)。
- **精密ターミナルの複数スロット均等分配(24章)はそのまま流用**: `evenSplit`/`extractFromStoragesIntoSlots`の呼び出しタイミングが変わっただけ。
- **搬入出パッド**: `playersStandingOn`で既にプレイヤーのインベントリ`Container`を手にしているため、`order.playerName`を介した搬入先解決を経由せず直接使える(以前より単純になった)。withdraw/deposit方向で別々のエントリ関数(`reconcileIoPadWithdrawal`/`reconcileIoPadDeposit`)に分割し、パッドの搬入出モード("both"/"deposit_only"/"withdraw_only")による制御はそれぞれの早期returnで表現している。
- **レッドストーンロックの反映が即時になった**(副次的な改善): 以前はロック後も既にキュー投入済みの注文は最後まで処理されていたが、新方式では毎サイクル`isRedstoneLocked`を再評価するため、ロックした瞬間に止まるようになった。

### `orderProcessing.ts`/`depositProcessing.ts`

- `processNetworkOrders`/`processNetworkDeposits`: 既存のFIFO消化ループの直後に、残り予算があれば`reconcileAllTargetWithdrawals`/`reconcileAllTargetDeposits`を呼ぶ形にした。`processNetworkDeposits`は元々`requests.length === 0`なら早期returnしていたが、目標系の直接処理が後段に控えるため、この早期returnは削除し、代わりに`storageIndex`/`depositTargets`の構築を`requests.length > 0`の時だけ行う形にして(FIFOが空でも目標系側で遅延構築される)、既存の「無駄なら構築しない」という最適化の精神は維持した。
- `hasPendingOrderFor`/`hasPendingSlotOrderFor`/`hasPendingDepositFor`/`hasPendingSlotDepositFor`を削除(呼び出し元が無くなったため)。
- **搬入出パッド専用の分岐がFIFO側から丸ごと消えた**: 搬入出パッドはもう`Order`/`DepositRequest`のFIFOキューに乗ることが無いため、`processNetworkOrders`の搬入先解決(`order.playerName`からプレイヤーを探す分岐)・`finalizeOrder`/`finalizeDeposit`の完了通知(「搬出完了!」「搬入完了!」)は、いずれも到達しないコードになっていたため削除した。配達ターミナルは「引き出し」のままなので、対応する分岐(オンラインなら位置に関わらずインベントリへ届ける等)は変更していない。
- `finalizeOrder`/`finalizeDeposit`(完了通知・shortfall記録)は「引き出し」「預け入れ」専用のまま。目標系はfinalizeを経由しなくなったため、自動端末等で今まで出ていた「引き出し#XXXの受け取り準備ができました」という(元々やや不自然だった)通知は目標系では出なくなった。

### `main.ts`

`startAutoTerminalCheckLoop()`・`startInventoryTerminalCheckLoop()`・`startPrecisionTerminalCheckLoop()`・`startPadCheckLoop()`の呼び出しと関連importを削除した(処理は`startNetworkProcessingLoop`経由の`processNetworkOrders`/`processNetworkDeposits`内に統合されたため)。検知と処理が同一tickの同一関数呼び出し内で完結するようになり、18章で対応した「パッドに乗ってから搬入出が始まるまでのラグ」もさらに縮んだ(18章末尾に追記した)。

### `ioPadProgress.ts`の再設計

以前はキュー(`Order`/`DepositRequest`)の`delivered`/`requested`を読んで進捗(「搬出中... X/Y個」)を表示していたが、目標系がキューを持たなくなったため参照先が無くなった。表示専用のロジックとして、パッドの上に乗っているプレイヤーごとに、目標設定(`getPadTargets`)と現在の所持品(ライブスキャン、`scanContainerCatalog`)を直接比較し、目標未達/超過の品目があれば「現在N/目標M 品名」の行(不足は§a、超過は§bで色分け)をアクションバーに表示する形に作り直した(実際の搬入出は行わない、表示専用の軽量な比較)。`playersStandingOn`は`padCheck.ts`からexportして共有した。`setActionBar`が`RawMessage`配列を受け付ける(`text`/`translate`混在可)ことを利用し、カスタム名の無い品目もローカライズされた表示名で出せるようにしている。配達ターミナルの`deliveryProgress.ts`は「引き出し」のままなので変更していない。

### 状況(UI)への影響

`terminalSettings.ts`・各ターミナルの設定UI(`autoTerminalUi.ts`等)は目標の読み書き先として今まで通り機能するため無改修。ただし副作用として、目標系4種の「状況」タブ(引き出し/預け入れの進行中一覧、`setupOrderStatusSection`/`setupDepositStatusSection`)は今後常に空になる(キューに何も積まれなくなるため)。空でも壊れはしないため、タブ自体の削除は今回のスコープ外とし、別途判断することにした。同じ理由で`statusListUi.ts`のツールチップが持っていたスロット表示(`slotIndex`/`slotIndices`)分岐も、参照するデータが二度と現れなくなったため削除した。

### 今回のスコープ外(第2段階以降の課題)

- **「引き出し」「預け入れ」への広告モデルの適用**: 議論はしたが、「今何も欲しくない」通常のターミナル・配達ターミナルも含めて毎サイクル全ターミナルをスキャンするコストが新たに発生するため、目標系の移行結果を見てから判断することにした。
- **目標系の中でのラウンドロビン等の公平性改善**: 現状は`network.terminals`の登録順を維持しているため、慢性的に予算が不足するネットワークでは後方のターミナルが割を食う可能性がある(22章までのFIFOと同じ特性であり新規の劣化ではない)。

## 26. 精密ターミナルに未指定スロットの一括回収を追加(ユーザー要望)

24章の複数スロット指定・25章の広告モデル移行に続く追加要望。「ルールで指定していないスロットは常に空にしたい」という要望に対応した。例えばスロットが5つあるストレージで1,4,5スロットにルールを設定した場合、2,3スロットを常に回収する(=これらのスロットに目標なし+回収ONのルールを設定したのと同じ意味)。

なお、直前に対応した「スロット重複の警告表示」(異なるエントリが同じスロットを指定した場合、防止はしないがどのスロットが重複しているかを警告する)は24章の該当箇所に追記済みのためここでは繰り返さない。

- **設定の永続化**(`terminalSettings.ts`): `getPrecisionCollectUnspecified`/`setPrecisionCollectUnspecified`(`wh:precision_collect_unspecified`)を追加した。デフォルトは`false`(在庫管理ターミナルの自動預け入れ(21章)と同様、意図せずアタッチ先の中身を全部持っていかれる事故を避けるため、明示的にONにする方式)。
- **UI**(`precisionTerminalUi.ts`のsetupSettingsTab): 「設定」タブに「未指定スロットの回収」トグルを追加した(在庫管理ターミナルの「自動預け入れ」トグルと同じ配置・作り)。スロット単位の設定である「スロット設定」タブではなく、ターミナル全体に効く設定のため「設定」タブに置いた。
- **処理**(`precisionTerminalCheck.ts`のreconcilePrecisionTerminalDeposit): 既存のルールベースの回収処理(collectがtrueのルールを順に処理)を終えた後、設定がONかつ予算が残っていれば、アタッチ先コンテナの全スロット(`container.size`)からどのルールの`slotIndices`にも含まれないスロット番号を集め、`{ slotIndices: 未指定スロット, targetAmount: 0, collect: true }`という仮想ルールとして`reconcileSlotGroupDeposit`にそのまま渡す。`reconcileSlotGroupDeposit`は元々、目標なし(`targetAmount<=0`または`itemTypeId`未指定)のルールに対しては`hasTarget`がfalseになり、スロットの中身の品目を問わず全量を余剰とみなす(23章の「空に設定」と同じ挙動)ため、新しい低レベルロジックを追加せず既存の関数をそのまま再利用できた。明示的なルールの回収を優先するため、未指定スロットの処理は必ず最後に(残り予算の範囲で)行う。
- **スロット重複の警告との関係**: 未指定スロット自体は定義上どのルールとも重複しない(ルールに含まれていないスロットだけを対象にするため)、上記の重複警告とは独立した機能。

## 27. コントローラに「起動」スイッチを追加(ユーザー要望)

アドオンをアップデートする際に、ネットワークの定期Tick処理(引き出し・預け入れ・整理・オブザーバー再計算・進捗表示)を一時的に完全停止できるようにしたいという要望を受けて追加した。デフォルトはON(既存ワールドをこのバージョンへ更新した直後、設定エンティティが未生成の間もこの既定値が使われるため、今まで通り動き続ける。「デフォルトOFF」だと更新のたびにネットワークが停止してしまい本末転倒なため)。

- **設定の永続化**(`controllerSettings.ts`): `getControllerEnabled`/`setControllerEnabled`(`wh:enabled`)を追加した。他の設定(`getNotifyOnOrganizeComplete`等)と同じ、コントローラ専用の非表示エンティティに持たせる方式。
- **UI**(`controllerUi.ts`): 「設定」タブの先頭に「起動」トグルを追加した。
- **Tick処理側のゲート**: このネットワークに関わる定期ループ(`system.runInterval`)全てで、ループの先頭(ネットワークごとのループの内側)に`getControllerEnabled`のチェックを追加し、OFFなら即座に`continue`してそのネットワーク分を丸ごと飛ばすようにした。対象は次の3箇所:
  - `networkProcessing.ts`の`startCycleAlignedLoop`(引き出し・預け入れ・整理・オブザーバー再計算をまとめて行う本処理ループ本体。25章の広告モデル移行後は、自動端末・在庫管理ターミナル・精密ターミナル・搬入出パッドの目標系の直接処理もこの中で行われるため、ここを止めれば全ターミナル種別の実際のアイテム移動が停止する)。
  - `deliveryProgress.ts`の`startDeliveryProgressLoop`(配達ターミナルの進捗アクションバー表示)。
  - `ioPadProgress.ts`の`startPadProgressLoop`(搬入出パッドの進捗アクションバー表示)。
  - この3つ以外の定期ループ(`wrench.ts`のレンチハイライト表示、`statusListUi.ts`のUI開いている間の状況タブ再描画)はネットワークの搬入出そのものとは無関係(ワールド全体のプレイヤー操作補助・UIセッション単位の再描画)のため対象外とした。
  - `lastProcessedTick`(`networkProcessing.ts`)はOFFの間更新しないため、再度ONに戻した直後は次の基準ループ(最短5tick)で確実に再開する(取りこぼした周期分をまとめて処理しようとするような「追いつき」処理は行わない、単に止まっていた地点から素直に再開するだけ)。
- **スコープ**: OFFの間もターミナルUIからの手動操作(通常のターミナルの引き出し/預け入れカート送信、コントローラの「倉庫の整理」ボタン等)自体は禁止していない。送信されたリクエストはキューに積まれるが、Tick処理が止まっているため処理されず、そのままONに戻るまで待機する(実世界の「電源が落ちている間は投入だけできる」という感覚に近い、意図的な設計)。

## 28. 個数指定スライダーに「増やす/減らす」の符号を表示(ユーザー要望)

「増やす/減らす」トグルを持つUI(通常のターミナルの引き出しカート、自動端末のウィッシュリスト、在庫管理ターミナルの在庫目標、搬入出パッドの所持数目標)では、スライダーのラベルが「数量: 54」のように符号無しの数字だけを表示しており、今どちら方向の操作になるのかがトグルの文言("増やす"/"減らす")でしか分からなかった。トグルの状態に応じてスライダー自体のラベルにも符号を出してほしいという要望を受け、「数量: +64」「数量: -64」のように表示するようにした。

- **実装**(`quantitySlider.ts`): `setupQuantitySlider`のoptionsに`signSource?: ObservableBoolean`を追加した。渡された場合、そのObservableBooleanの値(true=増やす/false=減らす)に応じて`+`/`-`をラベルの数値の前に付ける。スライダー自体の値が変わった時に加えて、`signSource`側が変化した時(トグルを押した瞬間)にもラベルを再計算するようsubscribeしている。`signSource`を渡さない呼び出し元(精密ターミナル。24章で述べた通り、目標を直接上書きするだけで増減の概念が無く「増やす/減らす」トグル自体を持たない)は符号無しの従来通りの表示のままになる(後方互換)。「空に設定」(23章)が選ばれている間は、増減ではなく絶対値への上書きのため符号を付けない。
- **呼び出し元の変更**(`terminalUi.ts`/`autoTerminalUi.ts`/`inventoryTerminalUi.ts`/`ioPadUi.ts`): 元々`setupQuantitySlider`より前に定義されている既存の`increaseMode`(ObservableBoolean)をそのまま`signSource`として渡すだけで済んだ(トグル自体は既存のものを流用、新しいステートは増やしていない)。

## 29. 「Drain」の表示名を「格納禁止」へ変更(ユーザー要望)

プレイヤーに見える文言としての「Drain」(倉庫レンチのモード名・チャットメッセージ)を「格納禁止」に変更した。関数名・型名・動的プロパティキー(`getDrain`/`setDrain`/`wh:drain`/`ToolMode`の`"drain"`値・`drainLocs`等)は変更していない(ユーザー要望により文言のみが対象、内部の識別子はそのままでよいため)。

- **変更箇所**: `toolModeUi.ts`の`MODE_LABELS.drain`(レンチのモード選択メニューに出る名前、"Drain指定" → "格納禁止指定")、`wrench.ts`の編集開始時のヒントメッセージ("Drain指定を切り替えてください" → "格納禁止指定を切り替えてください")、および指定/解除時のメッセージ("このストレージをDrain対象に指定しました"/"Drain指定を解除しました" → "このストレージを格納禁止に指定しました"/"格納禁止指定を解除しました")の3箇所。
- **変更していないもの**: コード中のコメント(`// Drain指定の有無は...`等)は、隣接する識別子(`getDrain`等)がそのまま「Drain」を名乗り続けるため、コメントだけ表記を変えるとコードと食い違って分かりにくくなる。そのため識別子・コメントは一貫して「Drain」のまま残した。

## 30. 「吸い込みパッド」(wh:suction_pad)を追加(ユーザー要望)

ホッパーのように、ブロック上に落ちているアイテムエンティティを定期的に回収してネットワークへ預け入れるパッドを新設した。搬入出パッド(18章・25章)と同じ「ターミナル」として`network.terminals`に接続する(レンチの構築モードで通常のターミナルと同様に接続/切断できる。`terminalBlock.ts`の`isTerminalLikeBlock`に`SUCTION_PAD_BLOCK_ID`を追加しただけで、レンチ側の接続ロジック(`wrench.ts`)は無改修で対応できた)。

- **他の目標系ターミナルとの違い**: 自動端末・在庫管理ターミナル・精密ターミナル・搬入出パッドはいずれも「目標を設定し、目標に近づくように動かす」方式だが、吸い込みパッドは目標という概念を持たず、**回収範囲内にあるアイテムは無条件にすべて回収する**(ユーザー要望通り、ホッパーに近い動作)。そのため、プレイヤーが設定する項目が一切無く(UIも持たない)、`terminalSettings.ts`への設定の読み書きも無い。搬出方向(ネットワークからの引き出し)も持たないため、`targetReconciliation.ts`の`reconcileAllTargetDeposits`(25章の広告モデル、預け入れ方向)にのみ`SUCTION_PAD_BLOCK_ID`を登録し、`reconcileAllTargetWithdrawals`側には登録していない。予算(スループット)は他の目標系と同じ、預け入れ方向の共有プールから消費する。
- **回収範囲**(`suctionPadCheck.ts`): ブロック上面の中央から見て上方向に0.3ブロック離れた点を中心とする半径1.5ブロックの球状範囲(ユーザー指定値)。`dimension.getEntities({ type: "minecraft:item", location, maxDistance })`で範囲内のアイテムエンティティを検索する(`padCheck.ts`の`playersStandingOn`と同じ`location`+`maxDistance`のAPIパターン)。ユーザーから「後で調整するかもしれない」との指定があったため、`PICKUP_CENTER_HEIGHT_OFFSET`(0.3)・`PICKUP_RADIUS`(1.5)として定数化してある。
- **アイテムエンティティからの回収方法**(`storageScan.ts`の新関数`insertItemStackIntoStorages`): 既存の`insertIntoStorages`/`insertSlotIntoStorages`はどちらも「取り出し元のコンテナのスロット」を前提にしており、アイテムエンティティ(`EntityItemComponent`)には使えなかった。`EntityItemComponent.itemStack`は読み取り専用のプロパティで、スロットという概念も無く、直接量を減らす手段が無いため(Bedrock APIの制約)、次の手順にした: (1) エンティティの`itemStack`を`clone()`し、予算内で搬入を試みる量に`amount`を調整してから`insertItemStackIntoStorages`でネットワークへ挿入する(中身はinsertIntoStoragesと同じ、優先ストレージ順に`container.addItem`していくロジックだが、戻す先のコンテナが無いので単に挿入できた量を返すだけ)。(2) 1個も入らなければ何もしない(エンティティはそのまま残る、次サイクルで再度回収を試みる)。(3) 1個以上入った場合は元のエンティティを`remove()`し、全量入らなかった分だけ`dimension.spawnItem()`で同じ座標に再度アイテムエンティティとして落とし直す(全量入った場合は何も落とさない)。
  - **(実機で発見された不具合の修正済み) 大量投入時にアイテムが跳ね続ける件**: `dimension.spawnItem()`は通常のドロップと同じ「ふわっと跳ねる」初速を付けて新しいエンティティを生成する。大量のアイテムを一度に投げ込むと、1サイクルの予算では全量を回収しきれず(3)の「残り分の再生成」が毎サイクル発生し続けるため、その初速のせいで再生成のたびにアイテムが跳ねているように見えていた(ユーザー報告)。`Entity.clearVelocity()`(`applyImpulse`の対になる、初速を即座に0にするAPI)を`spawnItem`の戻り値に対して呼ぶことで、再生成された残り分がその場に留まるようにして解消した。
- **見た目**(`BP/blocks/suction_pad.json`/`tools/generate-placeholder-icon.mjs`): 回収範囲が常に真上(20章参照、向きの概念を持たない)固定であることに合わせ、渦模様は上面だけに出す(ユーザー指摘: 「ネットワークオブザーバーとは異なり、常に上面だけが対象なので、どのようにおいても上面だけでよい」)。ネットワークオブザーバーは信号を出す面が回転する(Box UVで面ごとに描画を切り替える専用ジオメトリ`network_observer.geo.json`が必要だった)のに対し、吸い込みパッドは`minecraft:geometry.full_block`のまま`minecraft:material_instances`に`"up"`だけ別テクスチャ(`wh:suction_pad_top`)を指定するだけで済み、専用ジオメトリやBox UVの回転対応は不要だった。側面/底面は`"*"`(`wh:suction_pad_side`)で、上面と同じ枠(ベベル+四隅のリベット)だけの無地にし、渦模様(中心へ向かうほど明るくなる同心リング、琥珀色。搬入出パッドのローズ/ティールの二重リングとは色調で見分けが付く)は上面にのみ描く。
- **レシピ**(`BP/recipes/suction_pad.json`、ユーザー指定): 縦一列にホッパー(上)・自動端末(中央)・レッドストーンパウダー(下)。`unlock`は他の自動端末を使うレシピ(搬入出パッド・精密ターミナル等)と同じく`wh:auto_terminal`。
- **レッドストーンロック**: `minecraft:redstone_consumer`を持たせただけで、`targetReconciliation.ts`側の共通の`isRedstoneLocked`判定(全ターミナル種別で共通)がそのまま効く(専用コードの追加は不要)。

## 31. ネットワーク編集中のハイライトをパーティクルからワイヤーフレームエンティティへ変更(ユーザー要望)

ネットワーク編集中(レンチの構築/格納禁止モード)、接続済みメンバーを示す表示は、これまで各メンバーの少し上にパーティクル(`minecraft:villager_happy`/`_angry`)を10tickごとに撃つだけの方式だった(`wrench.ts`)。「上に別のブロックがあると完全に隠れてしまい判別できない」という指摘を受け、置き換えを検討した。

### 検討過程

- **案1: 半透明の膜でブロックを覆う**(ユーザーの初期アイデア): rangeIndicator.ts(21章)と同じ、非表示エンティティ+専用ジオメトリ+`entity_alphablend`の技術を使えば実装は難しくないと判断したが、「1メンバーにつき6面分のエンティティ」という素朴な実装ではエンティティ数がメンバー数(倉庫の特性上、数百規模を想定)に比例して膨れ上がる懸念をユーザーから指摘された。
- **他アドオンの実装調査(ユーザー要望で2回実施)**:
  - 1回目(ネットワーク系アドオンに限定): `SIsilicon/WorldEdit-BE`(選択範囲のバウンディングボックスの12辺だけをパーティクルで描く、対象が単一の連続領域だから成立する手法)、`Fluffyalien1422/bedrock-energistics-core`/`asn`(ネットワーク系アドオンだが、そもそも世界内でのハイライト表現をしておらずUIで見せる設計)を調査。散らばった多数のブロックを軽量にハイライトする決定版は見つからなかった。
  - 2回目(「ネットワーク系に限らず、広くブロックを強調するアドオン」という条件で再調査、ユーザー指摘): `mcbe-mods/EdgeRender`(作者: lete114、MITライセンス)という、ブロックの強調表示だけに特化したライブラリアドオンを発見。「ハイライトしたい1ブロックにつき非表示の専用エンティティを1体スポーンし、立方体の12本の辺だけを表すジオメトリを持たせ、隣接するハイライトブロックとの間の内側の辺をMolang変数で自動的に隠す(smart edge culling)」という手法を採用しており、これを参考にすることにした。

### 採用した設計

- **エンティティ数はメンバー数に比例したまま**: EdgeRenderの手法は「辺の描画本数」を減らす技術であり、エンティティの個体数そのものは減らない(隣接ブロック同士を1つの直方体に併合してエンティティ数自体を減らす案も検討したが、併合ロジックの複雑さに見合うかは実測してから判断すべきと考え、今回は見送った)。各エンティティ自体は物理演算・当たり判定・AIを持たない極小コスト(`minecraft:collision_box`実質無効化、`minecraft:physics`無効、`minecraft:damage_sensor`で無敵化。rangeIndicator.tsのrange_wall/range_ceilingと同じボイラープレート)なので、rangeIndicator.tsの「ネットワーク1つにつき6エンティティ」より遥かに軽量になるはず、という判断で試作した。実測して重ければ、併合による削減を追加検討する。
- **Molang変数の書き込み方式はEdgeRenderと変えた**: EdgeRenderは`entity.playAnimation()`の`stopExpression`でMolang変数を書き込む手法を使っている(コメントから、古いエンジンバージョン(1.18.x)との互換性のためと推測)。このアドオンは`minecraft:entity_properties`(`Entity.setProperty`/`Entity.getProperty`、`client_sync: true`で `q.property('id')`からMolangで読める)が使えるエンジンバージョンのみ対象にしているため、アニメーションを経由しない、より簡潔なこちらの方式を採用した(`BP/entities/member_highlight.json`の`description.properties`)。
- **12本の辺の可視性判定ロジック(`scripts-src/memberHighlight.ts`の`edgeVisible`)はEdgeRenderの`fv()`関数をそのまま移植**した: ある辺を挟む2方向の隣接がそれぞれメンバーかどうかと、その対角のマスがメンバーかどうかの3つの真偽値から、辺を表示すべきかを判定する。
- **表示色は2色**(`wh:drain`プロパティで切り替え、テクスチャは`array.wh_member_highlight_colors[q.property('wh:drain')]`で選択): 構築モードは全メンバー(コントローラ・ストレージ・ターミナル・オブザーバー)を緑、Drainモードはコントローラ+全ストレージを対象に、格納禁止指定済みのストレージだけ赤(元のパーティクル方式の`villager_happy`/`villager_angry`の使い分けを踏襲)。
- **同期範囲はプレイヤー単位ではなくネットワーク単位**: 元のパーティクル方式は`world.getPlayers()`をループしプレイヤーごとに撃っていたが、パーティクルと違い今回のエンティティは永続化されるため、複数人が同じネットワークを編集していても二重にスポーンしないよう、rangeIndicator.tsの`syncRangeIndicator`と同じ「ネットワーク単位で都度収束させる」設計(`scripts-src/memberHighlight.ts`の`syncMemberHighlight`)にした。モード(構築/Drain)は「そのネットワークを編集中の誰か1人」のツールモードを採用する(`editingSession.ts`の`currentHighlightMode`。複数人が異なるモードで同時編集している稀なケースでは、先に見つかった1人が優先される)。
- **即時反映+定期的な自己修復のハイブリッド**: 接続/切断・Drain指定変更・ツールモード切り替えの各操作(`wrench.ts`/`toolModeUi.ts`)の直後にその場で`syncMemberHighlight`を呼んで即座に反映しつつ、rangeIndicator.tsの`syncRangeIndicator`と同じ10tickごとの定期実行(`wrench.ts`の`reconcileAllIndicators`、旧`reconcileAllRangeIndicators`から改名)でも呼び直し、ログアウト等での取りこぼしを自己修復する(rangeIndicator.tsと全く同じ設計思想)。
- **`editingSession.ts`と`memberHighlight.ts`の依存方向**: 「誰が編集中か」を調べる`findAnyEditor`/`currentHighlightMode`は、`memberHighlight.ts`ではなく`editingSession.ts`側に置いた。`memberHighlight.ts`が`editingSession.ts`(の`getEditingNetworkId`)に依存すると、`editingSession.ts`が`memberHighlight.ts`の`syncMemberHighlight`を呼ぶ(`endEditingSession`内)ため循環importになってしまうため。
- **(追加対応) 二連チェストの継ぎ目が途切れて見える件**(ユーザー指摘): 二連チェストは中身を共有するもう半分が`network.storages`に現れない(1台だけが登録される、24章より前からの既存の設計)ため、そのままではハイライトの輪郭線がちょうど2つのチェストの継ぎ目を横切ってしまい、片方だけ強調されているように見えてしまっていた。`memberHighlight.ts`の`withPhysicalPair`が、`network.ts`の`findPhysicalStoragePair`(ネットワーク登録の有無に関係なく純粋に物理的な隣接を見る、wrench.tsのDrain機能等でも使われている既存関数)で見つかる方も同じdrain値でハイライト対象に加えるようにし、隣接判定(`edgeVisible`)が2マス分をひとつながりとみなして継ぎ目の辺を自動的に隠すようにした。
- **(追加対応) 二連チェストはどちらの半分を右クリックしても切断できるように**(ユーザー指摘): 上記の対応で両方の半分が強調表示されるようになった結果、「見た目上は両方接続されているように見えるのに、実際にネットワークへ登録されているのは片方だけなので、登録されていない方を右クリックしても『既にネットワークに含まれています』と言われるだけで切断できない」という分かりにくさが表面化した。Drainモード(`handleDrainModeUse`)は元々`resolveStorageMembership`でどちらの半分をクリックしても登録済みの方へ解決する作りだったが、構築モードの接続/切断(`handleBuildModeUse`)は`block.location`をそのまま使っていたためこの解決をしていなかった。`findAdjacentConnectedStorage`で「クリックしたのは未登録の半分だが、隣が登録済み」と分かった場合は、新規接続の試みとしてではなく、その登録済みの半分を切断する操作として扱うようにした(範囲チェックより前で判定し、切断は範囲外でも常に許可するという既存方針を維持)。設定エンティティの後始末(`removeStorageSettingsEntity`)は両方の座標に対して行う(通常の切断パスと同じ)。
- **(追加対応) ターミナル系(薄い板)はフルブロックではなく張り付いている面1枚分の枠だけを表示**(ユーザー指摘: 「ターミナルは薄い形であるにもかかわらず、強調はフルブロックのサイズで表示されている」): 通常ターミナル・自動端末・在庫管理ターミナル・精密ターミナル・配達ターミナルは、`minecraft:placement_position`(`minecraft:block_face`ステート)でクリックした面に張り付く薄い板状のブロックで(`RP/models/blocks/terminal.geo.json`参照、cube size `[14,14,1]`)、フルブロックの搬入出パッド・吸い込みパッドとは見た目が異なる。`terminalBlock.ts`に`isThinTerminalBlock`(対象5種の判定)と`getBlockFacing`(`minecraft:block_face`ステートの値を返す。既存の`getAttachedStorageLocation`もこれを使うようリファクタした)を追加した。
  - **当初の実装(面1枚分の4辺だけを流用)とその見直し**: 最初は、立方体の1つの面の外周が12本の辺のうち4本(縦2本+その面の上下2本、例えばnorth面なら`vert_wn`・`vert_en`・`top_n`・`bottom_n`)で構成されることを利用し、既存の12辺ジオメトリからその4本だけをオンにする実装にした。これは新規ジオメトリが不要という利点があったが、実機で試したところ2つの問題が見つかった: (1) 強調される面の向きが実際の張り付き面と逆になっていた、(2) 面1枚分の枠だけだと、フルブロックのメンバー(ストレージ等)が隣接メンバーで5面を隠されて1面だけ露出しているケースと見分けが付かない(ユーザー指摘: 「ちゃんと直方体にしてほしい」)。
  - **実際に薄い直方体(14×14×2程度)のジオメトリを追加して解決**: `member_highlight.geo.json`に、6方向(north/south/east/west/up/down)それぞれの薄い直方体を表す12本の辺をまとめた専用ボーン(`thin_north`等、1ボーンにつき12個の`cubes`。方向ごとに辺のON/OFFを個別に切り替える必要が無いため、12辺のcube群を1ボーンにまとめて済ませている)を追加した。フルブロック用の12ボーン(`vert_en`等)とは完全に独立したボーン群であり、どちらを見せるかは新設した`wh:is_thin`(bool)プロパティで切り替える(`part_visibility`の各条件に`!q.property('wh:is_thin')`/`q.property('wh:is_thin')`を追加)。どの方向を見せるかは新設した`wh:facing`(enum: north/south/east/west/up/down)プロパティで選ぶ。
  - **向きの反転**: `getBlockFacing`で得た値をそのまま`wh:facing`に使うと逆になったため(理論上は一致するはずだが実機の見え方を優先し原因の深追いはしていない)、`memberHighlight.ts`の`OPPOSITE_FACING`で反対向き(north↔south、east↔west、up↔down)に変換してから設定するようにした。
  - **隣接ブロックとの併合(`computeEdgeProperties`のedgeVisible)は行わない**: ターミナルは孤立した薄板であり、密集して敷き詰められる想定のストレージとは性質が異なるため、方向だけで決め打ちする単純な方式のままにした。
  - **表示位置はブロックの外側の面に揃えており、実際の板の位置(ブロック中心付近、`terminal.geo.json`の`origin: [-7,1,7]`)より外側にずれる**が、これは意図的な単純化(ハイライトの視認性を優先し、ブロックの外側の面を基準にする。ピクセル単位の位置合わせまではしない)。

## 32. 「リモート配達ターミナル」アイテムを追加(ユーザー要望)

あらかじめ倉庫コントローラと「リンク」しておいたアイテムを、そのネットワークの近く(コントローラから一定距離以内)であればどこでも取り出して使い、配達ターミナルと同じ動作(引き出したプレイヤーのインベントリへ優先的に直接届く)を行える新アイテム`wh:remote_delivery_terminal`を追加した。距離をアップグレードキットで伸ばす機能は将来の課題とし、今回は32ブロック固定のMVPとした。

### 既存の「引き出し」の仕組みとの衝突と解決

既存の`Order`は「`terminal: Vector3`は必ず実在するブロックの座標である」という前提を随所に持っている(`orderProcessing.ts`が`dimension.getBlock(order.terminal)`でブロックを取得し、`network.terminals`への登録有無を確認し、張り付いた先のストレージを既定の搬入先にし、`terminalSettings.ts`の場所キーの非表示エンティティから名前・通知設定を読む、等)。リモート配達ターミナルは実体を持たないアイテムなのでこの前提に乗らない。単純にUIだけ足すのではなく、`Order`型と`orderProcessing.ts`を最小限拡張して受け入れられるようにした。

- **`state.ts`**: `Order`に`remote?: { notifyOnComplete: boolean; terminalName?: string }`を追加した。`terminal: Vector3`は既存コードとの互換のため引き続き必須のままにし、リモート注文では**コントローラの座標**を入れる(実ブロックとしては一切参照しない、単なる記録・表示用途)。`notifyOnComplete`/`terminalName`は、位置キーの設定エンティティを経由できない代わりに、**注文時点の設定をそのままOrderへスナップショット**して持たせる(あとから設定が変わっても、既に発行済みの注文の通知挙動は変わらない。この点は既存の位置ベースの設定とは微妙に異なる挙動だが、実害は無いと判断した)。
- **`orderProcessing.ts`**: `processNetworkOrders`の搬入先解決を`order.remote`の有無で分岐。リモート注文は「`network.terminals`登録チェック→`dimension.getBlock`→張り付いた先」という一連のブロック依存処理を全てスキップし、注文したプレイヤーがオンラインならそのインベントリを直接使う。**オンラインでない場合、張り付いた先のような代替の搬入先が無いため`destContainer`は`undefined`のままにし、既存の「搬入先が無い→`finalizeOrder`で不足確定」という安全なフォールバック経路にそのまま合流させた**(新しいエラー処理を追加する必要が無く、ネットワーク在庫は一切減らないためアイテム消失のリスクも無い)。`finalizeOrder`の通知判定・完了アクションバー表示条件、`hasActiveDeliveryOrderFor`(1プレイヤー同時1件までの制約)にも同様に`order.remote`の分岐を追加した。`deliveryProgress.ts`の進捗アクションバー表示フィルタにも追加した。
- **アイテム自身に設定を持たせる、このアドオン初めてのパターン**: 他の全ターミナルは、ブロック位置をキーにした非表示エンティティ(`terminalSettings.ts`)に設定を持たせているが、リモート配達ターミナルには対応する位置が無い。`ItemStack`自身も`getDynamicProperty`/`setDynamicProperty`を持つため(`scripts-src/remoteDeliveryTerminalItem.ts`)、そこへ直接`wh:linked_network`(リンク先ネットワークID)・`wh:notify_on_complete`・`wh:terminal_name`を持たせた。`ItemComponentUseEvent`系の`itemStack`は読み取り専用のため、変更後は`player.getComponent("equippable")?.setEquipment(EquipmentSlot.Mainhand, updated)`で手に持っているスロットへ明示的に書き戻す必要がある(`updateHeldItem`ヘルパーに集約)。設定変更・注文送信のたびに「今まさに持っているアイテム」を都度取得し直す方針にし、UIを開いた時点のitemStackへの参照をtickをまたいで保持しない(手放す/持ち替える等の想定外の状態変化を安全側で無視できる)。
- **リンク**: このアイテムを持ってコントローラを右クリック(`onUseOn`、`block.typeId === "wh:controller"`)すると、`findNetworkByController`で見つけたネットワークIDを保存する。初回リンク時のみ名前を自動採番する(他の`generateXName()`と同じ命名規則、`generateRemoteDeliveryTerminalName`)。再リンクしても名前は変わらない。
- **リモート使用**: 空中で使用(`onUse`)。倉庫レンチ(`wrench.ts`)の`AIR_USE_RAYCAST_DISTANCE`raycastと同じ「視線の先にブロックが無い場合だけ」ガードを流用し、`onUseOn`との二重発火を避けた。リンク未設定/リンク先ネットワーク消失(コントローラ解体等)/範囲外(`network.ts`の既存`isWithinNetworkRange`を32ブロックで呼ぶだけ、新しい距離判定ロジックは追加していない)をそれぞれ個別のメッセージで拒否し、条件を満たせばUIを開く。
- **UI(`remoteDeliveryTerminalUi.ts`)**: 「引き出し」「状況」「設定」の3タブ(通常ターミナルの「預け入れ」タブは、張り付いた先のコンテナが無いリモート端末には意味が無いため無し)。「引き出し」タブは`terminalUi.ts`の`setupTab`(元々ファイル非公開だったのでexportした)をそのまま再利用した。カート・検索・ページャーの実装がブロック非依存のシグネチャだったため、そのまま流用できた。「状況」タブも`statusListUi.ts`の`setupOrderStatusSection`を再利用し、`order.remote !== undefined && order.playerName === player.name`で絞り込む。「設定」タブは名前・完了時通知に加え、位置を持たないアイテムなのでレンチでの切断に相当する「リンクを解除する」ボタンを持つ。
- **見た目**(`tools/generate-placeholder-icon.mjs`の`remoteDeliveryTerminalIconColorAt`): 当初は配達ターミナルのフラットアイコン生成関数(`flatTerminalIconColorAt`)を青系の配色で使い回していたが、ユーザーから「もっとポータブル感のある見た目に」との要望を受け、壁面パネル調ではなく倉庫レンチと同じ透過背景の「手持ちのタブレット端末」シルエットに作り直した。試行錯誤があった: (1) 本体を縦長にし右上に同心の弧3本で無線信号を表現したが、16x16の低解像度では弧が潰れて水色の塊にしか見えなかった、(2) 弧をアンテナ(縦線+先端の点)に置き換えたが、本体の枠にアンテナが食い込み上端が開いた「コップ」のように見えてしまった。最終的に装飾は諦め、タブレットらしい比率(幅:高さ≒3:4)の閉じた枠+画面+下部ボタンという、確実に読み取れる単純な形に落ち着いた。
- **レシピ**(`BP/recipes/remote_delivery_terminal.json`、ユーザー指定): 縦横十字にエンダーパール4個、中央に配達ターミナル。`unlock`は配達ターミナルを使う他のレシピに倣い`wh:delivery_terminal`。
- **状況タブにリンク先IDを表示→コントローラの命名機能へ発展**(ユーザー要望): 当初は「接続されている倉庫コントローラのIDを見られるようにしてほしい」との要望を受け、状況タブの先頭に生の`network.id`(`generateId()`が生成するランダムな英数字文字列)を表示していたが、実機で見ると日本語の説明文と英数字が混ざって文字化けのように見えて読みにくく、プレイヤーに優しくないと指摘された。そこで、**コントローラにもターミナルと同じように名前を付けられるようにし**(`controllerSettings.ts`の`getControllerName`/`setControllerName`、`controllerUi.ts`の「設定」タブに「名前」欄を追加)、リモート配達ターミナルの状況タブにはIDではなくその名前を表示するように変更した。
  - **常に読める名前がある、という前提を保証する**: ターミナルの`getTerminalName`は未設定なら`undefined`を返す(呼び出し元が「名前未設定」を許容する前提)のに対し、コントローラの`getControllerName`は**未設定ならその場でランダムな名前(`generateControllerName`、他の`generateXName`と同じ命名規則)を生成し、永続化してから返す**(戻り値は`string`で`undefined`にならない)。これは、この機能を追加する前に設置された既存ワールドのコントローラにも後から自動的に名前が付くようにするため、かつリモート配達ターミナルの状況タブのような「常に何か読める名前が要る」用途を安全に満たすため。名前欄を空欄に変更した場合も、次に`getControllerName`が呼ばれた時点で新しくランダムな名前が生成される(空のままにはならない)。
  - コントローラUI自体のタイトルバー(`倉庫: ${controllerName}`)・リンク時のメッセージ(`「${controllerName}」にリンクしました`)にも同じ名前を使うようにし、表示の一貫性を持たせた。

## 33. コントローラに「リモート操作」アップグレード軸を追加(+長距離使用時のチャンク常時読み込み)(ユーザー要望)

32章のリモート配達ターミナルは32ブロック固定でしか使えないMVPだった。今回、「速度」「周期」「範囲」と並ぶ4本目のアップグレード軸`wh:remote_access_tier`(Cタイプ、T1〜T4)を追加し、Tierに応じて利用可能距離を広げられるようにした(T1: 64 / T2: 256 / T3: 同一ディメンション内どこでも / T4: 異なるディメンションを含めどこでも)。あわせて、ユーザーから「長距離で使う場合、倉庫のあるチャンクが読み込まれていない可能性がある」との指摘を受け、T1以上を装着している間はコントローラ周辺のチャンクを常時読み込み状態にする機能も追加した。

### 軸の追加自体は既存の仕組みをそのまま再利用

`UpgradeAxis`(`upgrade.ts`)・`upgradeKitItemComponent`(`upgradeKit.ts`)は軸に依存しない汎用実装のため、新しい`CONTROLLER_REMOTE_ACCESS_AXIS`を`controllerAxes.ts`に追加し`CONTROLLER_AXES`配列に含めるだけで、両ファイルとも無改修で新軸に対応できた(21章の設計通り)。`BP/blocks/controller.json`の`states`に他の3軸と同じ書式で`"wh:remote_access_tier": [0, 1, 2, 3, 4]`を追加した。

各Tierは「距離」(`distance`)と「別ディメンションからの使用可否」(`crossDimension`)という2つの独立したパラメータを持つ(`REMOTE_ACCESS_TABLE: RemoteAccessTierConfig[]`、index=tier、0=未装着時の現状維持)。当初は距離だけの単純なテーブル(T1: 64 / T2: 256 / T3: 1024 / T4: 同一ディメンション内どこでも)だったが、ユーザーから「T3は同一ディメンション内どこでも、T4は異なるディメンションを含めどこでも」という仕様変更の要望を受け、2パラメータ化した:

```
{ distance: 32,       crossDimension: false }, // tier0
{ distance: 64,       crossDimension: false }, // tier1
{ distance: 256,      crossDimension: false }, // tier2
{ distance: Infinity, crossDimension: false }, // tier3: 同一ディメンション内どこでも
{ distance: Infinity, crossDimension: true },  // tier4: 異なるディメンションを含めどこでも
```

`distance`が`Infinity`のTier(T3・T4)は、`network.ts`の既存`isWithinNetworkRange`(各軸`Math.abs(...) <= range`の単純比較)にそのまま渡せば「常に範囲内」として機能するため特別扱いは不要。`crossDimension`は同一ディメンション内かどうかの判定(後述)に使う独立したフラグで、T4だけが`true`になる。

### リモート配達ターミナル側の距離判定を新軸から取得するよう変更、あわせて別ディメンションのバグを修正

`remoteDeliveryTerminalItem.ts`の固定値`REMOTE_DELIVERY_RANGE = 32`を廃止し、`onUse`・`remoteDeliveryTerminalUi.ts`の確定時の再チェックの両方で使う共通関数`checkRemoteDeliveryAccess(player, network)`を新設した。

**あわせて発見したバグを修正**: 従来の距離判定は`isWithinNetworkRange(network, player.location, range)`のみで、**プレイヤーが別ディメンションにいる場合のチェックが無かった**(座標だけを比較していたため、別ディメンションでたまたま近い座標にいると誤って通ってしまう可能性があった)。`checkRemoteDeliveryAccess`は`player.dimension.id === network.dimensionId`を先に確認し、一致しない場合は現在のTierの`crossDimension`が`true`(T4)でなければ拒否する。T4で別ディメンションから使う場合は、座標の比較自体が無意味なため距離チェック自体を行わない(`isWithinNetworkRange`を呼ばずに素通りさせる)。同一ディメンションの場合は従来通り`getRemoteAccessRangeForTier(tier)`の距離で`isWithinNetworkRange`をチェックする(T3/T4は`distance`が`Infinity`のため実質常に通る)。

**Tierの取得元をプレイヤーではなくコントローラ自身のディメンションに変更**: 当初`getAxisTier(player.dimension, ...)`としていたが、T4装着時はプレイヤーがコントローラと別のディメンションにいる状態で呼ばれうる(そもそも`player.dimension`がコントローラのブロックとは無関係になる)ため、`world.getDimension(network.dimensionId)`(ネットワーク自身のディメンション)からTierを取得するように修正した。

### 長距離使用時のチャンク常時読み込み(新規`remoteAccessChunkLoading.ts`)

`orderProcessing.ts`のFIFO消化処理は`dimension.getBlock(loc)`でストレージ/ターミナルのコンテナを取得する作りのため、そのチャンクが未読み込みだと該当ブロックが取得できず、注文が届かないまま不足として確定してしまう(サイレントな失敗)。T1以上の「リモート操作」アップグレードを装着している間は、Bedrock Script APIの`world.tickingAreaManager`(`/tickingarea`コマンド相当)でコントローラ周辺を常時読み込み状態にし、この問題を解消する。

- 範囲は「範囲」軸が取りうる**最大**Tier分(`MAX_MEMBER_RADIUS = 16`マス)をコントローラ中心に固定でカバーする。現在の範囲軸Tierではなく最大値を使うのは、範囲軸を後から上げても再計算不要にするため(倉庫の実体(ストレージ等)は範囲軸の制約内にしか存在しえないため、リモート操作軸のTier(プレイヤー側がどこまで離れて使えるか)とは無関係にこのサイズで十分)。Y方向は`dimension.heightRange`(ディメンションごとに異なるため動的に取得)。
- `syncTickingAreaForNetwork(dimension, network, active)`: `active`が`true`なら`wh_remote_${network.id}`という識別子で(無ければ)`hasCapacity`を確認した上で`createTickingArea`する。`false`なら(存在すれば)`removeTickingArea`する。`createTickingArea`は非同期でエラーを投げうる(`TickingAreaError`: 容量上限超過等)ため、失敗時はコンソール警告のみに留め、機能自体は握りつぶして継続する(致命的にしない)。
- `removeTickingAreaForNetwork(networkId)`: `controllerBlock.ts`の`onPlayerBreak`から`destroyNetwork`の直前に呼び、コントローラ解体時にticking areaを残さない。
- `reconcileAllRemoteAccessChunkLoading()`: ワールド起動時に全ネットワークを走査し、現在のTierと実際のticking area有無を一致させる自己修復関数(ticking areaがワールド再読み込みをまたいで正しく保持されるか不確実なため)。`main.ts`から起動時に1回だけ呼ぶ(ticking areaの変化は軸のTier変更時の`onTierChanged`で即座に反映されるため、起動時の自己修復だけで十分)。

**循環import回避**: `remoteAccessChunkLoading.ts`は意図的に`controllerAxes.ts`をimportしない(`active: boolean`を呼び出し元から受け取るだけにする)。逆に`controllerAxes.ts`は`remoteAccessChunkLoading.ts`をimportする、という一方向の依存関係にした(31章の`editingSession.ts`/`memberHighlight.ts`と同じ回避パターン)。

**`system.run`での遅延実行**: `world.tickingAreaManager`はスクリプト読み込み直後のタイミングでは呼べない(restricted-execution mode)ため、`main.ts`の起動時自己修復呼び出しは`system.run(() => reconcileAllRemoteAccessChunkLoading())`で次tickまで遅らせている。

### 見た目・レシピ

キットアイテム(`wh:remote_access_kit_tier1`〜`4`)は`range_kit_tier*.json`(BP)・lang・`itemDescriptions.ts`の`buildRangeKitLore`と全く同じ構成で複製した(値は`getRemoteAccessRangeForTier`から動的取得。T4は`Infinity`をそのまま表示せず「同一ディメンション内どこでも」に置き換える`formatRemoteAccessDistance`を用意した)。アイコンは`tools/generate-placeholder-icon.mjs`に速度(菱形)/周期(リング)/範囲(四角い枠)に続く4つ目の形として、中央の十字+外周4方向の短い目盛りからなる照準(レティクル)風の`upgradeKitCrossColorAt`を追加した。

レシピはユーザー指定の「Cタイプ」(範囲軸と同じ進行シンボルの階段: 鉄→ダイヤモンド→ネザライトインゴット→ヘビーコア)と意味シンボル(T1: エンダーアイ/T2: 氷/T3: 雪玉/T4: リカバリーコンパス)を、4章「クラフトレシピ: アップグレードキット・コントローラ」記載のTierごとの固定パターンにそのまま当てはめて作成した(Tier1/2: `["A A","BCB","A A"]`、Tier3: `[" A ","ABA"," C "]`、Tier4: `["ABA","ACA","ADA"]`でA=Tier1の意味シンボル固定)。

## 34. 「液体ポンプ」(`wh:liquid_pump`)を追加(ユーザー提案・調査を経て実装)

ユーザーから「液体の輸送」機能の提案があり、まず技術調査を行った。(1) ワールドの水源/溶岩源ブロックは`Block.setType`によるブロック書き換えで、(2) 大釜に溜まった液体は専用の`BlockFluidContainerComponent`(`minecraft:fluid_container`、`fillLevel`/`getFluidType()`/`setFluidType()`を持つ)で、どちらもScript APIから直接操作できることを確認した。相談の結果、次の方針で実装した:

- **対象ブロック**: フルブロックとして設置し(ユーザー要望。搬入出パッド・吸い込みパッドと同じ)、設置時にプレイヤーが向いていた方向(`minecraft:facing_direction`)が指す面の、その1マス先。バルブ柄のテクスチャが表示される面の奥にある液体を汲み出すイメージ(「ブロックの背後にある液体を対象にする」というユーザーの表現通り)。
- **UXパターン**: 自動巡回型。吸い込みパッドと同様、設置してネットワークに接続するだけで毎サイクル自動的に動作し、設定は持たない。
- **スコープ**: 水・溶岩のみ(ポーション/粉雪/染料入り大釜は対象外)。
- **動作方向**: 汲み出しのみ。ネットワークの満タンバケツを消費して液体を戻す「注ぎ込み」は今回のスコープ外。
- **バケツ消費**: ネットワーク内の空バケツ(`minecraft:bucket`)を1個消費して満タンバケツに変換する(サバイバルの実感に近づけるため。空バケツが無い間は汲み出しが起きず、プレイヤーが供給する必要がある)。

### ブロック本体はフルブロック+向き付きテクスチャ

`wh:liquid_pump`は当初、既存のターミナル系(`terminal.json`)と同じ「薄い板を壁に張り付ける」方式で試作したが、ユーザーから「フルブロックにしたい」との要望を受けて設計を変更した。搬入出パッド・吸い込みパッドと同じ`minecraft:geometry.full_block`を使い、代わりに`network_observer.json`と同じ`minecraft:placement_direction`トレイト(`enabled_states: ["minecraft:facing_direction"]`、設置時にプレイヤーが向いていた方向を6方向のブロックステートとして記録する)を採用した。

`network_observer.json`はモデル自体を回転させて特定の面に別テクスチャを見せていたが、液体ポンプはフルブロック(`minecraft:geometry.full_block`)のままなので回転は不要で、より単純に**`minecraft:material_instances`をpermutationsで丸ごと差し替える**方式にした(搬入出パッド/吸い込みパッドが上面だけ別テクスチャにしている構成の、向き固定版ではなく向き可変版):

```json
{
  "condition": "q.block_state('minecraft:facing_direction') == 'north'",
  "components": {
    "minecraft:material_instances": {
      "*": { "texture": "wh:liquid_pump_side" },
      "north": { "texture": "wh:liquid_pump_face" }
    }
  }
}
```
(6方向分を用意。`wh:liquid_pump_face`がバルブ柄、`wh:liquid_pump_side`が無地の金属面。)

- `liquidPumpCheck.ts`の`getTargetLiquidLocation`が`block.permutation.getAllStates()["minecraft:facing_direction"]`を読み、対応する方向ベクトル(`FACING_DIRECTION_VECTORS`)だけポンプの位置からずらして対象位置を求める。バルブ柄のテクスチャが向いている面の1マス先を汲み出す、という直感的な対応にした。
- `wrench.ts`のリンク処理・`network.terminals`への登録は`terminalBlock.ts`の`isTerminalLikeBlock`に`LIQUID_PUMP_BLOCK_ID`を加えるだけで既存の枠組みにそのまま乗った(フルブロックなので`isThinTerminalBlock`には加えていない。搬入出パッド・吸い込みパッドと同じ扱い)。
- フルブロックのため、搬入出パッド・吸い込みパッドと同様に専用のBP/items定義は不要(ブロックの識別子がそのままアイテムのtypeIdになる)。

### レッドストーン入力による動作停止(ユーザー要望)

14章「自動処理のレッドストーン制御」で確立した方式をそのまま適用した: `BP/blocks/liquid_pump.json`に`"minecraft:redstone_consumer": { "min_power": 0 }`を追加し(このコンポーネントは`format_version`/`min_engine_version`が1.26.0以上必要なため、`format_version`を1.26.0へ引き上げた。`io_pad.json`/`suction_pad.json`と同じ)、フルブロックであるポンプ自身の位置で通電を受ける。搬入出パッド・吸い込みパッドと同じくフルブロックのため、14章で薄い板系ターミナルが直面した「張り付いた先のブロックでは受信できない」という制約(`Block.getRedstonePower()`は普通のコンテナブロックでは値を持たない)自体が問題にならず、素直にブロック自身へ`redstone_consumer`を付けるだけで済んだ。

`processNetworkLiquidPumps`(`liquidPumpCheck.ts`)のループに、既存の`isRedstoneLocked(block)`(`terminalBlock.ts`)チェックを追加し、通電中のポンプは`processLiquidPump`を呼ばずスキップするようにした。搬入出パッド・吸い込みパッドは`targetReconciliation.ts`のディスパッチループが一括でこのチェックを行っているが、液体ポンプは`targetReconciliation.ts`に乗らない独立処理(前述)のため、`processNetworkLiquidPumps`側で個別に同じチェックを行っている。

### 液体の検出と変換(新規`liquidPumpCheck.ts`)

`detectExtractableFluid(block)`が対象ブロックを判定する:

- ワールドの水源/溶岩源ブロック: `block.permutation.getAllStates()["liquid_depth"] === 0`(ソースのみ。流れている水/溶岩はバニラのバケツと同じく対象外)。
- 満タンの大釜: `block.getComponent("minecraft:fluid_container")`の`fillLevel === FluidContainer.maxFillLevel`(中途半端な水位は対象外)かつ`getFluidType()`が`Water`/`Lava`(ポーション/粉雪は対象外)。

変換本体`processLiquidPump`は、まず対象の液体を判定 → `consumeFromStorages`(新規、後述)で空バケツを1個消費 → `insertItemStackIntoStorages`(吸い込みパッドと同じ既存関数)で満タンバケツの格納を試みる、という順序で処理する。**格納に失敗した場合(ネットワーク満杯)は、消費した空バケツを戻して中断し、ワールドの液体には一切手を付けない**。この順序(ワールドを書き換える前に、必ず先に格納の成否を確認する)を守ることで、ネットワーク満杯時に「バケツだけ消えて液体もアイテムも生成されない」というアイテム消失を防いでいる。

**新規`consumeFromStorages`(`storageScan.ts`)**: 既存の`extractFromStorages`(ストレージから取り出して`destContainer`へ届ける関数)とほぼ同じループだが、届け先を持たずスロットの中身をそのまま減らす/削除するだけの版。「ネットワークから搬入先を必要とせず消費するだけ」という処理はこのアドオンで初めてだったため新設した。

### スループット予算に乗せない独立処理

引き出し・預け入れ・整理(25章「広告モデル」)は共有スループット予算・`targetReconciliation.ts`のディスパッチに乗っているが、液体ポンプは乗せていない。対象が固定1マスのため「1台につき1サイクル最大1個」という上限が自然に付き、他の目標系ターミナルのような共有予算制が無くても暴走しないと判断したためである。`networkProcessing.ts`の`startNetworkProcessingLoop`(既存の引き出し/預け入れ/整理と同じサイクル)に`processNetworkLiquidPumps`(`network.terminals`を`LIQUID_PUMP_BLOCK_ID`で絞り込み、各ポンプに`processLiquidPump`を1回ずつ試すだけ)を追加し、戻り値(いずれかのポンプが変換を行ったか)を既存の`ordersChanged || depositsChanged`と同様にネットワークオブザーバーの即時再計算トリガーに含めた。

### 見た目

`tools/generate-placeholder-icon.mjs`に、搬入出パッドの上面/側面(`suction_pad_top`/`suction_pad_side`)と同じ2枚テクスチャ構成で、`liquidPumpFaceColorAt`(バルブ/パイプの開口部、同心円、水色の穴)と`liquidPumpSideColorAt`(無地の金属パネル)を追加した。「液体を汲み出す機器である」ことと、どちらの面を対象の液体に向けて設置すればよいかが一目で分かるようにする狙い。

### 今回のスコープ外(将来の課題)

- **注ぎ込み方向**: ネットワークの満タンバケツを消費して液体をワールド/大釜に戻す動作は、waterloggedブロックとの整合や設置先の空き確認など検討事項が増えるため見送った。
- **ポーション/粉雪/染料入り大釜**: アイテム側にNBT的な効果情報を持たせる必要があり、このアドオンの「アイテムはtypeId+個数だけ見る」という既存のスタック管理方針と相性が悪いため見送った。
- **レシピ**: 未定。別途相談の上で追加する。
